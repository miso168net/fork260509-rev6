# 005-role-menu-crud — role＋menu 管理 CRUD 寫端 brainstorm（階段 0）

- 日期：2026-09-22｜狀態：範圍與 BACKLOG 承載五題（Q1～Q5）＋設計三段十節已過 user 核可；同日 grill-with-docs 輪（G1～G4：以 BACKLOG 分類體檢為主核本刀是否把可處理項納全）已收斂、共識經 user 確認、本檔修訂另顆落地。★2026-09-23 grill 第二輪（user 決定 SDD 階段以較強模型從頭重做，重做前再 grill 一輪：八鏡唯讀查證＋逐鏡對抗複核＋完備性評審，134 筆 findings 經複核 130 成立、4 不確定、0 推翻；拍板 12 題〔R1 十題＋Q4b 追問＋R2 一題〕＋工程修正併入各節）已收斂、共識經 user 確認、本檔第二次修訂另顆落地；下一步＝**手動** `/speckit-specify`（本檔為其 input；不自動觸發——否則 before_specify hook 不跑、分支不建、spec 落 default）。
- 一句話：把 upstream 的角色頁與選單頁自 demo 殼接成真——17 支端點（role CRUD 6＋menu CRUD 7＋roleHome 2＋getMenuTree＋getAllPages；ROUTES 22→39、GET 7／POST 6／DELETE 4）、前後端同刀、CDP 對照 rev5 22080；同刀落地 006 要消費的三件底座（選單域 advisory 序列化域、casbin rebuild-swap 判定面同步、授權歸檔寫入面＋reason gate）；憲法一次 MINOR 1.4.0→1.5.0（島 H 五條入憲〔H3 增補受保護選單不可停用、不可改父＝rev6 增補〕＋§III.2 用途 (ii)＋BL-00098／BL-00118／BL-00119 三條併入）；ADR 六支。
- 交付價值：超管首次能真的建角色、建選單、刪選單並確定被刪選單的授權在判定面立即失效（不是等重啟）；006 三維授權治理的資料軸心（角色、選單、按鈕碼聯集）與三件底座就位、屆時純消費；BACKLOG 開放 41 條中 19 條隨本刀收、目標零新 BL（已知態一律走 ADR）。

> 輸入：`docs/ops/NOTES.md` 下一步（005 條）、憲法 §I.5／§I.7（進場規則、承襲指針表 H 列、島 E 條文）／§III.2（用途 (i) 列形、表外宣告、生成檔紀律）／§IV／§V.2～§V.3、`docs/ops/BACKLOG.md` 開放 41 條與滯後卷 4 條（逐條對觸發欄）、主線 BACKLOG 分類體檢工作檔（2026-09-22 跨機交接版、gitignored、不入帳；桶 B 十一條＋收刀前體檢清單為本檔 §2 底稿、grill 輪以之為主核）、ADR-00014／ADR-00015／ADR-00026／ADR-00034／ADR-00039／ADR-00040／ADR-00041、`deploy/grafana-provisioning/alerting/rules.yml`（casbin-reload 規則錨）、rev6 碼面現況（`envelope.rs` 之 `PageRes`、`handler/common.rs` 兩件、`auth/enforce.rs` 終態句、`facade/sys_ip_rule.rs` 之 ILIKE、`m0002` 政策列、entity 四表欄集、`tests/wire_schema.rs` 十二 definition、`tests/common` 四支守衛）、rev5 `rev5:005-role-menu-crud` 全套（brainstorm／spec／plan／research／data-model／contracts／tasks；`rev5:ADR 0048`～`rev5:ADR 0052`、`rev5:ADR 0060`）與其收刀後動本域檔之 commit（rust：`rev5:f841b04`／`rev5:47e8a67`／`rev5:c0067ae`／`rev5:55bb3e3`／`rev5:6ee82e2`／`rev5:e02251d`；base-web：`rev5:1597a671`／`rev5:ae1ac0c9`／`rev5:854a72ee`／`rev5:84f283c7`；唯讀、凍結 SHA 外層 7eab28a／base-web 9833308／rust-api 92919b9）。

## 0. 拍板紀錄（全數 user 拍板 2026-09-22／2026-09-23、一題一問；首選項皆為建議）

| 題 | 拍定 | 要點 |
|---|---|---|
| Q1 刀範圍 | **承 rev5 拆刀形全射程；BACKLOG 能收的條目逐條列入** | rev5 沿 α 縫（grant／revoke 寫 casbin_rule）把「刀 A＝role＋menu」拆成 005（CRUD 面）與 006（grant 面）；rev6 承其射程＝16 支端點＋三底座＋島 H 入憲＋前端兩頁接真（兩顆授權 modal 一行不動）。已核實零 migration 零 seed 成立：16 支端點 seed 政策列全在（12～16／21～25／27～31／34／35／64／65，後兩列 protected）、`sys_role.role_memo`／`sys_menu.menu_memo` 欄在、`casbin_rule` 治理三欄與 `sys_casbin_policy_archive` 14 欄在、casbin 釘 2.20.0（與 rev5 同版）。user 另令：本檔必列「BACKLOG 哪些條目隨本刀收」（§2）。棄案：底座推 006（deleteMenu 後判定面殘留舊授權直到重啟＝島 H2 帶已知違憲窗入憲，`rev5:005` grilling G1 已否決）；005＋006 合一刀（約 27 支、兩島同次入憲、rev5 已判太大） |
| Q2 getAllPages | **併入 005（17 支、ROUTES 22→39）** | upstream 選單 modal 的「頁面」下拉靠 `GET /systemManage/getAllPages`；rev5 005 歸 006 支撐讀、期間 modal 開啟即打 404 被靜默吞（`pages \|\| []`）、下拉只剩自身 routeName（upstream 自動補進）。rev5 006 實作＝顯示域 route_name 全集依 `(order, id)` 排序、約 20 行、零 grant 面；seed 26 政策列（R_SUPER、GET）已在。棄案：照 rev5 留 006＝列已知態、CDP 對照排除 |
| Q3 分頁 `current` 逾界語意（BL-00116） | **clamp 入契約與活書**（維持 as-built） | 004 的 IP 規則清單端點對 `current` 加了契約未宣告的上界 clamp（10^7、擋 OFFSET 溢位）；005 帶三個新分頁端點、此規則須一次定成跨端點慣例。定案：current clamp [1, 10^7]、逾界回空頁且回應 `current`＝上界值；分頁參數收斂抽成 `envelope` 層共用 helper、四端點同用一份常數（IP 規則清單端點一併改引＝機械改、既有測試釘住）；規則寫進活書 08 §8.2 API 慣例＋各端點契約＋contract test 逐端點一案；零錯誤集變動、零前端改動。棄案：逾界回業務錯誤 2222（動 MSG_KEYS 與三檔 locale、四端點 contract 改形、前端跳 toast） |
| Q4 未知 `wbip_type` 上 wire（BL-00095） | **won't-fix ADR＋前端小守** | Q3 改引共用 helper 動到 `handler::ip_rule` 清單端點可執行碼＝BL-00095 依「動 X」定義到期。定案：未知類型只能源自直改庫（非支援路徑），後端清單照原樣上 wire（不過濾、不改契約與快照），前端 ip-rule 頁對映射取不到的值退回顯原字串（一行守；該頁為 rev6 新檔、零授權成本），存取閘既有 skip＋告警不變；立 by-design 決定（ADR 形由 G1 定）、收刀 `backlog_done`。棄案：清單端過濾＋告警（管理員看不到也刪不掉、total 語意另定）；DTO 加 `unknown` 表示法（動 004 契約快照、typings、前端映射三面） |
| Q5 表頭插槽形（BL-00117） | **共用元件加 prop、兩頁改用** | 共用元件 `TableHeaderOperation` 的 default 插槽覆寫成空時 Vue 改渲染自帶備援鈕（新增＋批刪）、無權者反而看到寫入口；rev5 解法＝外層 `v-show`＋內層 `v-if` 疊寫（menu 頁 005 即用、ip-rule 頁 `rev5:B-099` 006 補），只靠碼註守。定案：`src/components/advanced/table-header-operation.vue` 附加 `showAdd?`／`showDelete?` 兩 prop（預設 true、既有呼叫端零行為變化＝憲法 §III.2「附加 prop＋安全預設」紀律），menu 頁與 ip-rule 頁改傳 prop、不再覆寫 default 插槽，備援鈕陷阱結構性消失 ⇒ BL-00117 收。代價＝用途 (ii) 名單多一支共用元件（約 2 處修改型）、upstream rebase 多一個衝突面；UI 零差異。棄案：照抄疊寫＋碼註（不變式仍零機器守）；疊寫＋view-render-guard 補腿（綁模板字面的脆弱 lint） |

**grill 輪拍板紀錄（user 2026-09-22 發起 grill-with-docs：以 BACKLOG 分類體檢為主核本刀是否把可處理項納全；一輪 frontier 四題；首選項皆為建議）**

| 題 | 拍定 | 要點 |
|---|---|---|
| G1 BL-00095 之 ADR 形 | **新 ADR supersede ADR-00040** | ADR-00040（IP 域已知態、六款）已 accepted 不可改，BL-00125（款 2 dev 二態實為三態）與 BL-00120（零 rev5 出處）皆為其對沖、觸發皆＝下一支翻案該 ADR 之刀；005 恰要立同一面的 by-design 決定。定案：新檔 `supersedes: [ADR-00040]`、六款重述（款 2 改三態；款 6 對齊 as-built 並以 BL-00105 六型鍵集斷言兌現）＋新款「清單端點未知 `wbip_type` 照原樣上 wire、前端退顯原字串」＋frontmatter 補 rev5 出處；RUNBOOK §16.1／活書 06 島 F ⑧／活書 11 三處鏡像字面同批改三態。收 BL-00095＋BL-00125；BL-00120 改條文只剩 ADR-00039 半（ADR-00039 不為補出處單獨翻案）；ADR 總數 4→5。棄案：併入 005 已知態 ADR 一款（IP 域已知態散住兩支）；獨立單款 won't-fix ADR（三支 IP 域已知態分散） |
| G2 BL-00082 連帶到期 | **納入：boot 體檢＋案** | BL-00082 觸發＝「修改 `config::load_trust_model` 或信任模型契約」；005 體檢單元收 BL-00115 時刪 `docs/ops/reference-src/trust-model-config.md` 手抄 toml 區塊＝字面上修改信任模型契約。定案：`config::load_trust_model` 載入時驗 `connecting_ip_header` 為合法 HTTP 標頭名，非法→結構化告警（附改寫建議）且該標頭視為缺席（與「單集合壞只清該集合」同形、只縮小信任）；一正一反案；約 30 行；其 quickstart grep 殘留屬凍結史料不動。user 2026-09-21 對維護批之「不納入」裁定屬彼時脈絡、刀內連帶到期即收。棄案：改條文註「刪鏡像區塊非契約語意變更、不視為到期」。★「視為缺席」措辭與契約「省略＝預設名」撞詞、降級歸類未定——經第二輪 R1-Q8 釘死為 rev5 形（工程判斷 17） |
| G3 ILIKE 共用件射程（BL-00096） | **收攏三份含 `sys_ip_rule`、順收 BL-00096** | rev6 `facade/sys_ip_rule.rs` 自持一份轉義 ILIKE，005 getRoleList 之 roleName／roleCode 模糊搜尋為第二份；rev5 至 007 第三份（sys_user）才以 `rev5:B-138` 收攏。定案：`facade/mod.rs::ilike_contains` 單一源（轉義 `%`／`_`／`\`）、sys_role 與 sys_ip_rule 改引；改引動到 `sys_ip_rule` facade 可執行碼 ⇒ BL-00096 到期並同批收（四寫端與 `find_by_id_for_update` 改具型簽章 `&DatabaseTransaction`、production 兩呼叫點已傳交易＝機械改、該檔測試約三十處裸連線呼叫者改自開交易）。user 2026-09-21 對維護批之「不納入」裁定理由（005 不動 sys_ip_rule）自此不成立。棄案：兩份並存、BL-00096 條文補「到期時同批收攏 ILIKE 三份」。★第二輪實核更正：005 後 ILIKE 為兩份、三個呼叫點（cidr／role_name／role_code）；BL-00096 改簽章牽動三檔 40 處裸連線測試呼叫（工程判斷 21） |
| G4 BL-00111 立腿 | **體檢單元立腿、收 BL-00111** | 全樹十二支掃描腿「切到第一道行首 `#[cfg(test)]` 為止」，失守形 (a)＝測試模組之後再宣告 top-level item（負面斷言型恆綠無聲）；005 新增多支帶測試模組的大檔、且很可能再加同形腿。定案：先 grep 既有閘確認無同形，再立一支「測試模組閉合後零 column-0 top-level item」lint（射程＝`src/**/*.rs`；**行掃描形**：行首 `#[cfg(test)]` 之後任何行首 `pub fn`／`fn`／`struct`／`enum`／`impl`／`const`／`static`／`mod`／`use`／`macro_rules!` 即紅；刻意不用 char 級解析＝不成 BL-00108 第三份）＋植入反例變異自證；失守形 (b) 已由行首形切點排除。棄案：維持「看」；不立腿只改條文。★第二輪實核：上列判準字面落地首日即誤紅（item 級 `#[cfg(test)]`、第二個測試模組、字串常值內的行首碼）且漏抓帶可見性前綴之 item；判準改寫見工程判斷 18，並同檔收 BL-00111 子項② |

**grill 第二輪拍板紀錄（user 2026-09-23 發起 grill-with-docs：重做 SDD 前再核本檔與分類體檢，能納入本刀者全數考量；R1 十題＋R1-Q4b 追問＋R2 一題；除 R1-Q1 外皆選建議項）**

| 題 | 拍定 | 要點 |
|---|---|---|
| R1-Q1 首輪 clarify 三題 | **交 clarify 重新出題** | 首輪 SDD 之 clarify 已問過三題（清除常量性時反查後代、in-use 計數口徑、批刪含查無 id），答案只存於首輪成品、未入本檔；user 選重做時由 clarify 重新出題，另加 rev5 as-built 未問過之「批次內重複 id 先去重」一題（§5 列為 clarify 必問候選四題）。棄案：直接帶入本檔為既定 |
| R1-Q2 刪角色後判定面 | **實際歸檔 >0 才同步** | 角色碼只在未刪範圍唯一 ⇒ 刪 R_X → 同碼重建 R_X → 指派使用者，舊 R_X 之記憶體授權被繼承（`rev5:B-093`，rev5 至 007 才以指派寫端自帶同步閉合）。定案：deleteRole／batchDeleteRole 軟刪成功且實際歸檔 ≥1 列時，commit 後觸發判定面同步（觸發矩陣 3→5 支、同受 RELOAD_SERIAL）；補端到端案「刪除→同碼重建→直種指派→判定面零命中」。005 新建角色零授權、seed 角色被 seeded 守門擋 ⇒ 本刀生產面實際不觸發、006 授權後才生效。翻 rev4／rev5「deleteRole 免同步」論證（ADR③ 改寫）。棄案：維持免同步＋立 BL 指 007；維持免同步只記 ADR |
| R1-Q3 更新時空字串語意 | **新 ADR supersede ADR-00015** | ADR-00015 決定 1「空字串亦為設值」與 rev5 形（`rev5:B-102`：可空欄 `""`＝清空落 NULL、名稱欄 `""`＝不動）正面衝突。定案：立 ADR⑥ 逐款重述 ADR-00015——002 設定寫端續行 `""` 落 `""`；角色／選單可空文字欄 `""`＝清空落 NULL（create 與 update 同形、防 `""` 與 NULL 並存）；NOT NULL 名稱欄 `""` 與顯式 null 同拒 `nameRequired`（rev5 為靜默不動，改拒較誠實）。ADR 五支→六支。棄案：服從 ADR-00015 現文 |
| R1-Q4 治理清單不帶 size | **不帶 size＝回全部頂層** | 回應 `size`＝實得頂層數、`current` 回 1；帶 size 照常 clamp [1,100]。原工程判斷 5 屬 wire 回應語意變更＝拍板級（`rev5:B-131` triage 亦判 user 可見拍板）、補問定案。附帶：前端共用表格把回應 size 回寫每頁筆數，首載與頂層列數增減時各多一發請求（畫面不變；rev5 首載亦 10→100 多一發）。棄案：size 固定回 10（size 與實得筆數不符）；沿 rev5 預設 100 截斷 |
| R1-Q4b 全取例外之限縮 | **明名入口＋白名單＋機器守** | 通用分頁 helper 只有「缺席→1／10」一種語意；另立明名入口（如 `page_or_all`），白名單恰 getMenuList/v2；名冊測試釘「全樹恰一處生產呼叫」、四個清單端點各一支 contract 案釘缺席行為（三支回 10、選單回全部）；活書 08 §8.2 寫通則＋例外表，新增例外須改白名單＋測試＋ADR 款。邊界：只有完全沒帶 size 才全取；`size=0` 仍 clamp 成 1；壞形依既有規則整串收斂成預設＝視同沒帶。棄案：只在選單 handler 內分支 |
| R1-Q5 受保護選單停用 | **拒停用受保護列** | seed 受保護 8 列（首頁、系統管理目錄、使用者／角色／選單管理、使用者詳情、系統設定、授權歸檔）原只擋刪除；停用目錄會令子樹從側欄收縮、路由判定為不存在 ⇒ 超管 UI 自鎖（推論、未實測）。定案：updateMenu 對受保護列拒「轉為停用」，拒因復用 `biz.menu.protectedMenu`（零新鍵、譯文改涵蓋刪除／停用／改父）；島 H3 入憲時增補（rev6 增補、ADR① 註明）。棄案：不擋、記已知態＋RUNBOOK 復原程序；關鍵欄全鎖 |
| R2-Q1 受保護選單改父 | **受保護列之父選單不可改** | R1-Q5 之繞道：把受保護列改掛到普通目錄 X 再停用 X。已核 seed：受保護列之祖先皆受保護、受保護旗標不開放寫入 ⇒ 受保護列 parentId 不可改即令整條自鎖路徑結構性封死（一道欄位檢查、不掃樹）；拒因同鍵。棄案：允許搬家＋兩支掃樹守門；不封記已知態 |
| R1-Q6 外連網址 href | **後端限 http(s)** | 前端路由守衛遇 `meta.href` 直接 `window.open`（已核碼）；005 起超管可自由寫 href ⇒ `javascript:` 網址成為高權帳號外洩後的持久化面（推論、未於瀏覽器實證）。定案：add／updateMenu 之 href 空字串→NULL、有值須以 `http://`／`https://` 起首（不分大小寫），違反回 2222 新鍵 `biz.menu.hrefInvalid`。棄案：照 rev5 直傳記已知態；只前端守（出用途 (ii) 九檔） |
| R1-Q7 按鈕碼清單 buttons | **驗形制＋長度** | rev5 對 buttons 零驗（已核碼）：合法 JSON 但非陣列、或成員缺 code ⇒ 被當零碼 ⇒ 舊碼全數判移除 ⇒ 不可復原歸檔；碼長無上界而授權表欄 125 字元。定案：buttons 須為 null 或陣列；成員為物件、`code` 為非空字串 ≤100 字元、同陣列不重複；不限字元集；違反回 2222 新鍵 `biz.menu.buttonsInvalid`。拒因鍵總數 22→24、MSG_KEYS 19→43。棄案：只驗陣列＋code 非空；照 rev5 直傳 |
| R1-Q8 BL-00082 非法標頭名 | **rev5 形：只 warn、不計降級** | 標頭名保留原字面、模型不改（查標頭自然失配＝該覆蓋層停用、絕不回退預設 `CF-Connecting-IP`）；boot 發一則結構化 warn（指名 tunnel 或 cdn[i]、附原字面與改寫建議）、不計入 `ip_domain_degraded_total`、不帶 degraded 欄、不觸發 `obs016-ipgate-degraded`；降級來源名冊維持恰八；加反案「非法名時 CF-Connecting-IP 不被讀取」；活體契約失敗語意表加一列。棄案：第九降級來源（首輪形）；歸既有集合被清來源 |
| R1-Q9 角色頁授權彈窗 | **接受、照實況改寫已知態** | 菜單權限彈窗開啟即呼叫 getMenuTree 與 getAllPages ⇒ 005 後顯示「真選單樹＋寫死勾選 id 1～21（不代表真授權）＋真首頁下拉＋送出假成功零寫入」；按鈕權限彈窗仍全假。兩鈕照舊可點、彈窗一行不動（G3）；ADR④ 與 CDP 已知態觀察改寫成此實況、實際開窗觀察。棄案：006 前隱藏兩鈕 |
| R1-Q10 hideInMenu 釋義 | **運行期改值屬業務資料** | 活體契約 `docs/ops/reference-src/schema-definition.md` 載 `hide_in_menu` 白名單「rev6 對應釋義隨 menu 域刀重審」。定案：憲法 §I.2「不啟用」只約束 seed／demo 之可見性治理；超管運行期經選單管理改 hideInMenu 屬業務資料、不算啟用隱藏機制；6 列白名單只描述 seed。釋義寫進 ADR③，該契約句改現在式並指向 ADR。棄案：改指 006 附回填；hideInMenu 不開放寫入 |

**既定不問（承 rev5 已驗證結論施工、報備備查；出處＝`rev5:005` brainstorm §3 十五題、grilling 六題、clarify 三題與實作期親決）**

1. 拆刀縫 α、島 H→005、島 G→006、島 I→007（`rev5:005` #1）；前後端同刀（#2）。
2. deleteRole／batchDeleteRole 入選單序列化域（#8）；歸檔表三自由度全不動（#10）；`constant` 可寫＋父鏈常量性守門（#11）＋restoreMenu 第四腿常量父鏈重驗（`rev5:ADR 0051`、`rev5:B-095`）。
3. restore 鈕不按鈕碼 gating（#12）；回收桶 toggle 形（#13）；static meta 不維護、DB 唯一真源（#14）。
4. 判定面同步基建在 005、006 純消費（G1；rev5 稱「熱重載」）；新建／復原選單於 006 前無法授予可見性＝已知態（G2）；前端修改型檔清單照實拆列、兩顆授權 modal 一行不動（G3）；getMenuTree 在 005（G4）；memo 兩欄兌現、列表不濾受眾（G5）；in-use／self-role 兩腿生產面窗接受（G6）。
5. 絕版判定聯集域＝未刪含停用（clarify Q1）；addMenu 同鍵雙層守門（Q2）；getAllRoles 照交付（Q3）。★rev6 實況更正：「零 UI 消費者」不成立——upstream 使用者頁新增 drawer（本刀不動）開啟即呼叫 `fetchGetAllRoles`，005 後其角色下拉顯示真角色、提交仍假成功（007 接真）；改記入 ADR④ 已知態。
6. `nameRequired` 第十鍵、`multiTab` 可寫、`routeNameInvalid` 鍵（rev5 實作期 user 親決）；`AuditOperation` 零新 variant、標的表由 `entity_table` 區分（`rev5:005` T005）。
7. 治理清單分頁列凍結形（UI 位置不動、頁碼 1／每頁 0／整列上鎖、prefix 續顯真實筆數；`rev5:ADR 0060`、rev5 user 親決 2026-08-25）。
8. rev5 對 rev4 之 11 項防回歸差異（`rev5:005` research R2）照帶：restore 冪等→業務錯誤、上下文缺席→拒寫 5000、AuditOperation 小寫封閉詞彙、純 i18n key 拒因、`fetchGetAllRoles` 殘留不帶、SELF_SERVICE_ROUTES 不帶、zh-tw 不上 runtime、rev4 域鎖字面不帶、deleteRole 入域、find_by_keys 不搬、「終態」句翻案。

**工程判斷（主線自拍、報備備查；RULES 名詞段判準下非拍板級）**

1. **藍本取 rev5 HEAD 形**：005 收刀後對本域檔的修補自首版帶入——`rev5:ADR 0051`（restore 第四腿）、`rev5:ADR 0060`（分頁列凍結 UI）、RELOAD_SERIAL＋其交錯時序 harness（`rev5:B-105`、rev5 006 T020 之 `#[cfg(test)]` 注入式 seam；「先 commit 慢 rebuild 蓋回」窗機器證）、`rev5:B-098`（wire-schema 對 `Api.RoleAdmin`／`Api.MenuAdmin` 之裁判）、`rev5:B-102`（空字串→NULL 組合面三案；★其語意改由 ADR⑥ 承載、名稱欄 `""` 改拒＝R1-Q3）、`rev5:B-100`（toggle 切換清勾選）、`rev5:B-132`（menu 頁 pageSize 歸位；碼註洩漏鏈以 rev6 語境重寫）、`rev5:B-137`（R_SUPER 宣告單源；只取此半、sys_user 側屬 007）、`rev5:B-138`（`ilike_contains` 共用件；G3 提前）、`rev5:B-094`／`rev5:B-108`／`rev5:B-127`／`rev5:B-123`③（handler 共用件：泛型三態、`blank_to_none`、status 雙向映射；facade 時戳 `now_ts`；見工程判斷 22／23）、`rev5:f841b04` 之 `violated_constraint`（23505 收窄單源）、`rev5:c0067ae`（wire-schema 新增受審 definition；rev6 數以 extract 為準＝工程判斷 32）。§0 輸入行之 commit 清單補 `rev5:932ba8c`（006 U5 之 T020 交錯時序 seam＝`rev5:B-105` 處置）與 `rev5:8f31bbc`（006 U8 之 getAllPages＝Q2 藍本）；已列各顆多為混雜 commit，逐顆×逐段三分（帶／不帶／rev6 已有等價）於 plan research 凍結。剔除 006（grant 面、reason gate 五值、getAllButtons／getAllEndpoints、policy-archive、endpoint modal、三顆 modal 接真）、007（`Identity` sid、no-escalation、user 域）、008（audit）增量；逐檔剔除清單於 plan research 凍結、烤入 implementer prompt。
2. **域鎖 key 字面**＝`0x7265_7636_6D65_6E75`（ASCII `rev6menu`；rev5 對 rev4 同法換世代名）。
3. **wrapper／typings**：`service/api/rev6-role-admin.ts`（role 6＋roleHome 2）、`rev6-menu-admin.ts`（menu 7＋getMenuTree）；`typings/api/rev6-role-admin.d.ts`／`rev6-menu-admin.d.ts` 開 `Api.RoleAdmin`／`Api.MenuAdmin` 獨立命名空間（createdBy enrich 形、id number＋2^53 守衛；004 慣例；`deleted` 導出布林只用於選單列——角色無回收桶、角色列不上軟刪欄）；getAllPages 走 upstream barrel 既有 `fetchGetAllPages`（`system-manage.ts` 零改、回應為 upstream 既有 string[]、零新型）。★upstream 未改動之消費者（使用者頁 drawer 之 `fetchGetAllRoles`、菜單權限彈窗之 `fetchGetMenuTree`／`fetchGetAllPages`）走 upstream typings（`Api.SystemManage.AllRole`／`MenuTree`）——wire 形須同時與之相容、受審時一併斷言。
4. **PageRes 既在 envelope**（`maint-backlog-86-90` U1 提前上移）⇒ rev5 FR-002「上移」項消；分頁 helper（current／size clamp 與預設 1／10）落同檔、`handler::ip_rule` 改引（Q3 連動 Q4）。★語意逐位照 IP 規則清單 as-built：缺席→預設 1／10；顯式 0→clamp 下界（`&size=0&current=0` 回 (1,1)）；查詢串壞形（如 `size=abc`）→整串收斂為預設頁。helper 落 `serialize_opt_i64_number_guarded` 之前（該 fn 為 `no_wire_literal_lives_in_envelope_production_code` 之生產區末錨）或同批換錨。getMenuList/v2 之全取例外走明名入口（R1-Q4b）。getDeletedMenus 缺席 size 由 rev5 之 100 改 10（隨共用 helper；前端恆帶 size、UI 零差異；對 rev5 之翻案、§1 列）。upstream 分頁器 `onFetched` 只回寫 itemCount 與 pageSize、不回寫 current（首輪實核）⇒ BL-00116 所慮「以回應 current 回寫而跳頁」結構性不發生（Q3 佐證）。
5. **getMenuList/v2 `size` 缺席＝回全部頂層**（★第二輪查證判屬 wire 回應語意變更＝拍板級，已升為 R1-Q4／Q4b 由 user 拍定；本條留作施工細節）：treeTable 無參一次取全樹；有值 clamp [1,100]——翻 rev5「預設 100 並 clamp」形、消掉 `rev5:B-131`（頂層 >100 列不可達）之債而不入 BL；缺席時回應 `size`＝實得頂層數、`current` 忽略回 1；UI 零差異（分頁列凍結形不變）；契約明寫、contract test 釘；ADR④ 記為對 `rev5:ADR 0060` 殘餘一之處置、並註明前端回寫 pageSize 引起的額外一發重取（CDP 對照排除「請求次數」維度）。
6. **BL-00109 之機器錨落 python 側**：rust-api 容器看不到 base-web 工作樹（dev compose 零 bind mount），故錨住 `tools/wire-schema.py` 加腿（讀 `base-web/packages/axios/src/options.ts` 之 `paramsSerializer` 與 `base-web/packages/axios/package.json` 之 qs 版本、對賬 rust 側各支首屏查詢串常數之前提）＋self-test；rust 常數改為引用該前提的說明。★第二輪查證：pre-commit 以 `check --staged-gate` 呼叫 wire-schema，只在 typings 或快照兩側有變動時才續跑——BL-00109 要抓的漂移（改 axios 設定或 qs 版本）恰走「兩側零變動、跳過」那條路 ⇒ 新腿 MUST 為純讀檔判定、放在 staged-gate 短路與容器探測**之前**無條件執行（self-test 另釘「staged-gate 判跳過時此腿仍執行」）；**不擴**收窄 pathspec（擴之即連動 pre-commit submodule-sync 段字面＝BL-00074 到期）；「base-web 工作樹改了 axios 設定而未 commit」之形記入 RUNBOOK §12 該列已知邊界。另：每支新帶查詢串之 query 型（getRoleList、getDeletedMenus、getRoleHome 之 id）各配一支首屏真串抽取案（getMenuList/v2 另釘無參形）；前端 qs 把 null 渲染成 `k=`，故角色清單 `status` 取字串形、`Some("")` 視為不濾（承 rev5）。
7. **BL-00106 收法**＝收刀體檢單元直接立腿、不再以「三形仍零」為由延到下一刀。★第二輪更正：`rev5:B-111` 只守兩形（泛型實例化、手寫 impl），巨集生成形無前例。故分三件：①照 rev5 立泛型 wire 型名冊（`Res`、`PageRes`），斷言其與樹上全部泛型 `Serialize` 型集合**恰等**（防日後新泛型信封靜默逃逸）＋實例化掃描；②手寫 `impl Serialize` 絆線；③巨集生成形屬 rev6 新設計——以 BL-00107 之普查等式（`derive(Serialize)` 錨數＝解析成功數；`parse_item` 遇 `$n` 元變數整項放棄 ⇒ 巨集生成型必使等式轉紅）同一機制兌現、附巨集植入反例。三件各配變異自證；該 lint 檔頭「已知不抓的邊界」與「與 rev5 藍本的差異」段同批改寫。
8. **契約落點**：刀內契約住 `specs/005-role-menu-crud/contracts/`（`wire-role-admin.md`／`wire-menu-admin.md`／`msg-keys.md` 候選鍵表；ADR-00041 決定 6）；跨端點分頁規則（current／size clamp 與預設、逾界語意、getMenuList/v2 缺席例外）住活書 08 §8.2 API 慣例＋各端點 contract test；004 IP 契約凍結不改、其與 as-built 之差由活書句與 git 史解釋（BL-00116 收）。
9. **已知態走 ADR、目標零新 BL**：005 域已知態合一支「本刀已知態與 by-design」ADR④，各款一律寫成「觀察路徑→症狀」（側欄點擊／直打網址／展開態與收合態；`rev5:L-046`：只記最好驗的症狀＝煙測驗一條沒人走的路），由 CDP 實際操作後定稿：①角色頁兩顆授權彈窗——菜單權限彈窗顯示真選單樹＋寫死勾選 id 1～21（不代表真授權）＋真首頁下拉、送出假成功零寫入；按鈕權限彈窗全假（R1-Q9）②policy-archive 選單項死項（BL-00045 已承載；「點擊 404」之症狀 rev6 未證、以實測定稿，不符即以 errata 改 BL-00045 描述）③新建／復原選單側欄不現（含 R_SUPER）④新增或復原之按鈕碼在 006 前無人持有（`getUserInfo` 按鈕碼取自判定面、前端 `hasAuth` 無超管旁路）⑤新建選單在治理清單顯示原始 i18nKey（`route:` 樹零新增、`$t` 查無鍵回退字面）⑥治理清單三列（system-settings／policy-archive／audit）顯 route 裸鍵（各頁進場刀帶入譯文時解除；BL-00045 條文補「選單管理頁」曝光面）⑦getAllRoles 之既有 UI 消費者＝upstream 使用者頁 drawer（下拉真角色、提交假成功，007 接真）；roleHome 零 UI 消費者⑧治理清單分頁列凍結＋`size` 缺席全取（含額外一發重取）。rev5 凍結時仍開放之 `rev5:B-145`（getAllRoles 只回活性且啟用 ⇒ 007 drawer 看不到持有的停用角色）寫入款⑦翻案觸發器（007 進場時複核）。IP 域已知態（BL-00095 未知類型上 wire＋ADR-00040 六款重述）住 G1 之 supersede ADR⑤。
10. **測試守衛形**＝rev6 004 G6 之水位形（無 SequenceResetGuard；`IpRuleRowsGuard` 前例）：守衛表集＝`sys_role`／`sys_menu`／`casbin_rule`／`sys_casbin_policy_archive`＋`sys_user_role`（皆不在 `RUNTIME_APPEND_TABLES`＝gate2 逐列比對）。★第二輪更正：arm 時**現讀** id 水位與序列 `(last_value, is_called)`、Drop 先刪 `id >` 水位列再 setval 回現讀值（RL-0031；寫死值遇 dev 庫走查殘列會令下一次 nextval 撞 PK）；`sys_role_id_seq=(3,true)`／`sys_menu_id_seq=(78,true)`／`casbin_rule_id_seq=(163,true)`／`sys_casbin_policy_archive_id_seq=(1,false)` 只作 gate2 之 seed 態期望（守衛自證測前提），不作還原目標。`sys_user_role` 為複合 PK、兩條 FK 皆 ON DELETE RESTRICT ⇒ 固定清理序＝指派列先於 `sys_role`（及 `sys_user`）；in-use／self-role 案以 seed 使用者掛合成角色構造（免造 `sys_user`；真需造時另掛使用者守衛）。測試造列一律顯式大 id，號段依表與檔於 `test_kit.rs` 以單一常數表登記（防跨檔撞號）；`casbin_rule` 造列走真 Enforcer `add_policy`＝nextval 取 id、免 setval 路徑不成立。稽核斷言一律取 `OpLogRowsGuard` 水位窗、不寫絕對計數（`rev5:L-056`／`rev5:B-122`：序列還原使 create 測 id 跨執行定值）。tests/ crate 取不到 src 側 `test_kit`，contract 真 seed app 面所需庫態守衛另立於 `tests/common`（沿 `RestorePlan`／`run_restore` 形）。
11. **`handler/common.rs` 重評**（BL-00113）：第三個帶操作稽核寫端進場單元執行；三件同批——①`operator_from_context`／`json_or_default` 兩件簽章②「`tracing::error!` 留呼叫點」切法③★測試側刻意不合併之 `table_state`／`audit_table_state` 同族兩份（含 005 若新增之第三份；第二輪補——條文明列、原漏）；結論落該檔 doc＋收單訊息；既有 `each_domain_keeps_its_own_log_literals` 守不得因重評併掉兩域 target、其名冊擴及 `handler/role.rs`／`handler/menu.rs`。★角色／選單寫端缺席拒寫事件形照 rev6 004（`refused = "request_context_absent"`、不帶 degraded 欄、不增計 `ip_domain_degraded_total`、target 取各域自有字面〔plan 定〕、不借 `security.ipgate`）——**不照抄** `rev5` HEAD 之 `audit_operator`（帶 degraded 欄）；`production_code_emits_no_degraded_field` 射程擴及兩新 handler。同批改寫散在 `handler/common.rs`／`handler/ip_rule.rs`／`handler/mod.rs` 之「005 第三份進場時重評」預告句為現在式結論，並改正 `common.rs` 檔頭關於 rev5 是否「逐字預警過同一個坑」的自相矛盾兩句；`handler/mod.rs` 之域數同改。
12. **ADR 待立六支**（刀分支內落、序號接續現行最大號；全數 draft 於 plan 期）：①憲法 Amendment ②判定面同步 rebuild-swap（翻 002 刀碼內「boot 載入即終態」諸句——`enforce.rs` 檔頭與 init doc、`main.rs`、`state.rs`「運行期只有讀鎖」；★明引已 accepted 之 ADR-00014 決定 4「運行期重載屬後刀」括號句、定性為**兌現其預告、不 supersede**；含 reload 契約、硬禁令＋casbin 2.20.0 版本鎖、RELOAD_SERIAL、ABBA 三失效條件、觸發矩陣五支〔R1-Q2〕、★單一 rust-api 行程前提＋多行程／多實例部署之翻案觸發〔屆時須比照 ipgate 門鈴補跨行程通知〕、★耗盡窗內 H2 記憶體半邊不成立之明記與恢復〔下次成功同步或重啟〕）③A1 域行為（deleteRole 入域＋R1-Q2 之「實際歸檔才同步」改寫原免 reload 論證＋島 G 行為 G1/G3/G4/G5 由 ADR 承載、條文隨 006 入憲＋archive 三自由度 won't-use 與 `rev4:ADR 0049` 翻案觸發條款過境＋R1-Q10 hideInMenu 釋義＋R1-Q5／R2-Q1 受保護選單守門之行為面；翻案觸發器記 007「使用者軟刪寫端須定軟刪是否清指派」）④本刀已知態與 by-design（工程判斷 9）⑤IP 域已知態 supersede ADR-00040（G1：六款重述、款 2 三態、款 3 寫入者集對齊 as-built〔IP 規則四寫端＋解鎖＋角色／選單寫端、設定寫端仍不落列〕、款 6 對齊 as-built、新款未知類型上 wire、rev5 出處補齊；款號對照表寫進本文）⑥部分更新語意 supersede ADR-00015（R1-Q3：逐款重述；002 設定寫端續行、角色／選單可空欄 `""`→NULL、名稱欄 `""` 拒）。★親決時點（第二輪更正「003／004 前例」之誤引：003 Q5 射程只限 Amendment、004 之已知態 ADR-00040 於治理單元 T064 才 accepted）：U0 拆兩顆——Amendment 顆只含憲法＋ADR①＋README 憲法版本鏡像＋generate（憲法 §V.2 步 4「獨立 commit」、003／004 同形）；ADR②③⑥（施工前提）另起一顆。ADR④⑤ 內容要到 as-built 才定（已知態實際觀察、款 6 鍵集斷言、前端退顯原字串），留 draft 至 U15 治理單元才 user 親決並 accepted（004 T064 形）；期間新發現的已知態直接併入 draft。
13. **components.d.ts 重算**（menu modal 引入 NTreeSelect 父選擇器）＝憲法 §III 生成檔紀律（`rev5:ADR 0052` 之條款 rev6 創世已入憲）、`tools/fork-delta-lint.py` 檔頭判準豁免；不入用途 (ii) 名單。★rev6 首次重算此檔、而生成檔紀律零機器守（手改也綠）⇒ menu modal 單元加機器斷言：`git -C base-web diff -- src/typings/components.d.ts` 只准出現 NTreeSelect 之兩行增列（`GlobalComponents` 介面內一行、全域 `const` 區一行；兩形字面不同）、其餘任何差異即紅；重算檔與 modal 同 commit 入帳、收刀前 `git -C base-web status --porcelain` 零殘留。
14. **CDP 對照排除清單**＝rev5 22080 之 006～008 增量（三顆授權 modal 接真、policy-archive 頁、endpoint-auth-modal、user 頁、audit 頁）＋ADR④ 已知態各款＋治理清單三列 route 裸鍵＋「請求次數」維度（R1-Q4 之額外重取）；已知態步驟一律實際操作觀察、不得以推論代替（`rev5:L-054`）。★判準承 004 clarify 之 user 拍板：結構清單逐項全等（表格欄位集合與列序、搜尋器項目、按鈕集合與權限顯隱、抽屜／彈窗欄位與校驗訊息、分頁列形、回收桶流程、toast 文案；005 另含已刪模式寫入口不現、memo 欄），間距／字體／顏色只記不擋。★拒因步驟挑前端不先擋的守門（codeExists、seededProtected、hasChildren、protectedMenu 等），並以網路請求事件證明請求確實發出（`rev5:L-047`：upstream 表單 required 規則使部分拒因在 UI 零請求零 toast、與後端掛掉同形）。★user 頁與 role 頁表頭另對 22089 upstream example 驗新增／批刪鈕仍在（共用元件加 prop 之回歸面、user 頁不在 22080 對照內）。★CDP 操作一律派 opus agent（user 2026-09-20 明令；2026-09-23 起編排全角色 opus，見 §6）。
15. **詞彙（domain-modeling；本 repo 詞彙之家＝活書 12 系統術語、不另立 CONTEXT.md；as-built 時落）**：治理域／顯示域、序列化域、絕版（button 碼不再屬任何未刪選單之聯集）、常量父鏈、reason gate；**「選單回收桶」**（getDeletedMenus／restoreMenu 面）與**「授權回收桶」**（006 之 policy archive）分立不互用；casbin 面一律**「判定面同步」**（碼識別字維持 `reload_enforcer`）、IP 規則集沿活書既有**「熱重載」**（門鈴 `ipgate:invalidate`），兩詞不互用。★現行活書 08 §8.3 末句「回收桶＝憲法 §I.7 島 G／I」與本條撞詞 ⇒ as-built 時改為「授權回收桶」；同段「寫後全量重載」與活書 04 同句併改為判定面同步之 as-built 敘述。
16. **getAllPages 落點**＝`handler/menu.rs`（依域：顯示域 route_name 全集屬選單域）；rev5 HEAD 住 `handler/role.rs` 係 006 U8 三支支撐讀同住之歷史、不承。
17. **BL-00082 之體檢形**（G2；★第二輪 R1-Q8 釘死）：`config::load_trust_model` 對 tunnel 與每筆 cdn 之 `connecting_ip_header` 以 `axum::http::HeaderName::from_bytes` 驗合法性（★`http` 非 server crate 直接依賴、沿既有 `axum::http::` 再匯出形＝零新依賴，不得另加 `http` 依賴）；非法＝回傳新 `TrustLoadKind`（`degraded_source` 回 None、與 UnknownKey 同形）＝boot 一則結構化 warn（指名 tunnel 或 cdn[i]、附原字面與改寫建議）、不計降級、不帶 degraded 欄；欄位型別不改、原字面保留（`HeaderMap::get` 自然失配＝該覆蓋層停用）、★絕不回退預設名；`IP_DOMAIN_DEGRADED_SOURCES` 維持恰八；案＝一正一反＋反案「非法名時 `CF-Connecting-IP` 不被讀取」＋對拍「非法名時覆蓋層結果與修改前逐格相同」；活體契約 `trust-model-config.md` 失敗語意表加一列（節標題之計數同改）、活書 06 島 F ① boot 步同批改；RUNBOOK §16.2 人工核對義務句改為「boot 體檢兜底、人工核對留給 prod 首部署」。
18. **BL-00111 lint 腿形**（G4；★第二輪改寫判準）：新 lint 檔住 `rust-api/server/tests/`、行掃描（不用 char 級解析＝不成 BL-00108 第三份）。原判準字面（射程全樹、行首 `#[cfg(test)]` 之後零行首 item 關鍵字）實測首日誤紅（`auth/enforce.rs`／`state.rs` 之 item 級 `#[cfg(test)]` 後接生產碼、多測試模組檔之第二個 `mod`、`main.rs` 測試常值 SKELETON 內之行首 `async fn main`）且漏抓（關鍵字表缺 `pub(…)`／`async`／`type`／`trait` 等頭形）。改為：①射程＝BL-00111 條文 grep 判準現算之切面腿所在檔（今為九檔；腿檔集以同 grep 自我對賬、防名冊漂移）②判準＝在與十二支腿**同一切點**（首個行首 `#[cfg(test)]`）之後，每個 column-0 item 之屬性鏈 MUST 含 `#[cfg(test)]`（第二個測試模組合法；生產 item 落切點後＝紅）③頭形 regex 納入可見性前綴與 `async`／`const`／`unsafe`／`extern` 修飾④掃描視圖用 `tests/common` 既有 Drop 視圖（註解與常值換白、換行保留）以避開字串常值、不另立解析組。★同檔另立子項②之行掃描腿：生產區零 `use crate::obs::… as`／`obs::{… as …}` 別名匯入（BL-00111 ①② 同收、整條 `backlog_done` 才成立）。變異自證涵蓋：item 級 cfg(test) 不紅、第二測試模組不紅、常值內行首碼不紅、`pub(super) fn` 植入必紅、別名匯入植入必紅。既有十二支腿之 docstring 補指本腿為其射程守；BL-00108 反向確認補「`entity_behavior_lint.rs` 零 diff」。
19. **rules.yml `obs016-casbin-reload-anomaly` 錨對齊**：該規則錨在 `casbin_reload_total{outcome=~"retry|exhausted"}`，005 U3 引入該 metric 後規則即活——metric 名與 label 值集以 rules.yml 既有字面為準、`obs.rs` 預註冊三 outcome；該規則註解標「（島 G1）」而 rev6 島 G 尚未入憲 ⇒ 改指島 H2＋ADR②。屬 BL-00093 ②之部分兌現、該條不收（觸發＝觀測 profile 首起或存取軌跡刀）；★第二輪改為**改條文加戳記**（②註「casbin-reload-anomaly 之事件錨已由 005 刀對齊」、其餘規則照舊待稽核；BL-00035 子項戳記前例）——只記收單訊息則下一位處理 ② 者自帳本看不出已對過錨。
20. **用途 (ii) 新增型塊數**：rev5 as-built 為兩語 locale 各 4 塊（role 標籤／role memo placeholder／menu `page:` 樹補／menu memo placeholder）、`app.d.ts` 4 塊；rev6 Amendment 範圍欄一律寫「實數以標記為準」（憲法表外宣告 1）。★第二輪更正：plan 期與 U0 時 (ii) 標記為零、無數可取 ⇒ U0 範圍欄寫 rev5 as-built 預估（形制與 BL-00118 改寫後之量法句一致、修改型不寫處數）；前端單元出口以 grep 逐檔斷言新增型塊數等於預估，不等即停手升級主線（user 定當刀 PATCH 或比照 BL-00118 滯後）。各新增塊（兩語 locale、`app.d.ts`）標記落地後做「拔標記必紅」變異自證——`rev5:B-162`：`fork-delta-lint` 遇塊內任一移除行 `};` 即整塊判修改型並跳過，插入位置不當即零鑑別。

**工程判斷（第二輪增補、主線自拍、報備備查；源自八鏡查證 findings）**

21. **BL-00096 之實際射程**：`sys_ip_rule` 四寫端與 `find_by_id_for_update` 改具型交易簽章後，以裸連線呼叫之測試點分布三檔共 40 處（`facade/sys_ip_rule.rs` 測試模組 32、`handler/ip_rule.rs` 測試模組 5、`ipgate/mod.rs` 測試模組 3；以 grep 現算為準）⇒ U1b 涉檔與 fix 允許清單補 `ipgate/mod.rs`（僅測試模組）；ipgate 測試植列改自開交易後須 commit（走另一連線之門鈴重載才看得到）；`sys_ip_rule.rs` 模組 doc「一律收泛型連線」之設計理由段與測試支數句同批改寫。
22. **facade 共用件同批上提**（G3 之同一理由）：23505 約束名收窄（「只認活性唯一索引、不收全部 23505」）與 `now_ts` 在 `sys_ip_rule` 私有、005 之 sys_role（codeExists 兜底）與 sys_menu（addMenu 兜底）會各抄一份 ⇒ 隨 ILIKE 收攏同批上提 `facade/mod.rs`（`violated_constraint`＝`rev5:f841b04` 形；`now_ts`＝`rev5:B-123`③），sys_ip_rule 改引（零行為變更之機械改）、兩新 facade 首版直接消費。★ILIKE 共用件承 rev5 正典與 rev6 現形：不另寫 ESCAPE 子句（PostgreSQL 預設 escape 即反斜線）、佔位符 `$1`、欄參數取 `&'static str`（編譯器守只收字面）——首輪成品寫成帶 ESCAPE 子句屬誤、重做不承。`facade/mod.rs` 模組 doc「一張表配一支模組」原則因 `sys_casbin_archive` 同寫兩表須改寫。
23. **handler 共用件 HEAD 形首版即入 `handler/common.rs`**：泛型三態、`blank_to_none`、`db_status_to_wire`／`wire_two_value_to_db` 兩向 status 映射首次出現即落 common（`rev5:B-094`／`rev5:B-108`／`rev5:B-127` 之收攏終態；免同刀內重演兩份拷貝）；`handler/system_settings.rs` 之私有 String 專用三態——改引泛型件（先依 RL-0070 grep 掃描閘）或於 doc 註明保留理由；與 BL-00113 重評排同一單元。
24. **拒因鍵 24 與落地時序**：`biz.role.*` 10＋`biz.menu.*` 14（rev5 as-built 12〔含 `nameRequired`／`routeNameInvalid`〕＋R1-Q6 `hrefInvalid`＋R1-Q7 `buttonsInvalid`）⇒ MSG_KEYS 19→43、三檔 locale `backend:` 子樹各 +24。★新鍵 MUST 隨「首個發出它的單元」落地（`error.rs` 之 msg_key 常數＋MSG_KEYS＋三檔 locale backend 子樹＋`app.d.ts` backend 型節同單元、兩 pin 同一顆外層 commit）——排在寫端之後會被 msg-key-gate 擋（構造點鍵 ∉ MSG_KEYS），排在寫端之前會撞 contract 名冊雙向閘方向二（名冊每鍵 ≥1 發出點；003 刀 U2 前例）；U12 只留 wire 面。contract 真 seed app 面須逐鍵實打出 24 鍵（多數需先造庫態）＝§8 獨立工作量。
25. **判定面同步之機器守**（`rev5:005` 同刀立之，rev6 零等價）：新 lint（行掃描或重用既有剝註解件、不成第三份 char 級解析組）三道集合恰等守恆——①`reload_enforcer` 生產呼叫檔集＝名冊（U3 空冊、接線單元擴為 `handler/menu.rs`＋`handler/role.rs`〔R1-Q2〕，紅轉綠即接線訊號）②`.enforcer.write()` 寫鎖取得檔集＝空冊（家檔 `enforce.rs` 豁免）③`load_policy`／`Enforcer::new` token 只許出現於 `enforce.rs`；各附植入反例變異自證。另補成功路徑 swap＋`outcome=ok` 案、三支併發 reload 於 RELOAD_SERIAL 下全數完成之完成性案；判定呼叫收斂閘（`rev5:B-044`）同檔一併帶入或於 plan research 記不帶理由；呼叫端不持讀鎖、須 commit 之後之時序語意維持 review 承載（`rev5:B-119`、LL-00029 烤入 review lens）。
26. **觸發矩陣五支、同一字面**：deleteMenu／batchDeleteMenu「軟刪成功且實際歸檔 ≥1 列＝觸發」、updateMenu「buttons 絕版歸檔實際發生＝觸發」、deleteRole／batchDeleteRole「軟刪成功且實際歸檔 ≥1 列＝觸發」（R1-Q2）；★不得寫成「成功即觸發」（`rev5:B-104`：rev5:ADR 0049 出生即誤、as-built 三支一律以 if archived 為門；005 新建選單零授權、零歸檔是常態路徑）。ADR②、契約、spec FR 三處同字面；觸發條件特性案承 rev5 案單（Rejected／NoOp／NotFound／無 buttons 變更）並補「刪零政策列標的＝成功且 `casbin_reload_total` 零增」。
27. **寫端實作不變式**（rev5 踩坑釘死、烤入 U2 與寫端各單元 implementer prompt）：①歸檔掃描 MUST 早於標的列軟刪 UPDATE（兩域；`insert_archived` 以 v0 反查活性角色填 role_id——反序則全列 role_id 落 NULL；`rev4` 藍本恰是先軟刪後歸檔）②絕版判定 MUST 排除標的自身（掃描時自身仍持舊 buttons，不排除則絕版恆假、零歸檔、測試恆綠）③歸檔掃描依 v2 維度過濾（route_name 與按鈕碼值域重疊，失守即跨維連坐撤除）；對應案＝`role_id == Some(role.id)`、非絕版移除零歸檔、跨維同字面誘餌列 (live,archived)=(1,0)，皆配變異紅證④凡擁有交易之入域寫端 fn（含兩支 batch），失敗腿一律顯式 `rollback` 再回錯＋一支源碼釘（LL-00026：以 `?` 上拋只靠 drop 收場時，current-thread 測試斷言失敗會與守衛 DELETE 互鎖、整輪無訊息卡死）。
28. **addRole 同 code 雙層守門**：addRole 不進域 ⇒ 同 code 併發只剩唯一索引擋；先驗 `codeExists`、再以 `sys_role_code_active_uniq` 之 23505 兜底收斂到同鍵（收窄到索引名、主鍵 23505 不收；工程判斷 22 之單源件），補一支併發案（漏兜底＝DbErr→5000）。
29. **同層排序入契約**：getMenuList/v2（頂層切片＋子樹）與 getMenuTree 之同層序＝`(order ASC NULLS LAST, id ASC)`、頂層分頁依此序切（`list_governed` 無 ORDER BY、PG 不承諾回列序；`rev5:B-115` 前例）；facade 測加一腿：同交易內 UPDATE 擾動實體回列序後兩次呼叫逐位相等。
30. **rev5 沉默語意明文化**：status／menuType／iconType 恰二值嚴格 parse、值域外（含空串）＝缺席（add 取預設、update 不動、清單不濾），停用護欄以「≠1」判——寫進 wire 契約並以 contract test 釘；回收桶門＝頁級授權（復原守門重驗為唯一權威），刪 rev5 spec 殘留之「列級可復原性」字面（與不帶 `restorable` 旗標互斥）。
31. **R1-Q5／R2-Q1／R1-Q6／R1-Q7 施工細節**：受保護列之 updateMenu 拒「status 轉停用」與「parentId 變更」兩腿（拒因同鍵 `protectedMenu`、各一正一反；受保護旗標本身不在寫端 DTO）；href 之 scheme 驗證 add 與 update 同形；buttons 形制驗證先於絕版計算（壞形在進入「舊碼−新碼」之前即拒、零歸檔）；三者皆列 spec 之 rev5 翻案清單。
32. **wire 受審面**：受審名冊＝005 新增之全部 wire 型（回應＋請求＋query；實數以 extract 為準、預期含 rev5 之十二支＋`RoleHomeRes`／`UpdateRoleHomeReq`；rev6 自 BL-00097 起寫端請求型亦入裁判，delete／batchDelete 之 id 與 ids 請求型一旦立具名型即入）＋`PageRes`（BL-00112）。「序列化鍵集＝快照 properties 鍵集」斷言做成表驅動 helper、涵蓋全部受審讀型（既有七支含 `IpRuleRecord`＋本刀新讀型＋`PageRes`；`MenuRoute` 之快照為交叉型佔位、走具名豁免附理由）——否則 ADR⑤ 重述款 6「逐型承擔」當場成假述。可選：加「`Api.RoleAdmin`／`Api.MenuAdmin` 下之快照 definition ⊆ 受審 ∪ 具名豁免」完備性斷言，一次封住 BL-00112 類無聲缺席。★本檔各處「十二 definition」有兩義（rev6 現行 `wire_schema.rs` 之十二支 DEF 常數 vs `rev5:c0067ae` 新增之十二支），一律改寫為不綁數字的描述。
33. **ROUTES +17 之連動釘值**：router +17 那顆外層 commit 同批改外層 `tools/docsync/tests/test_references.py` 之真 repo 釘值測（兩份清單 +17 列、註解「現行二十二」；pre-commit 只在 staged 含 `tools/docsync/` 時才跑之＝不同批改即一路綠到有人跑 docsync test）、`router.rs` 測試逐列釘值與「現行二十二條」、`tests/contract.rs` 檔頭「case 集恰二十二」（`二十二` 在 rust-api 共 7 處）；該檔納入接線單元之跨 repo 允許清單（只准增列釘值）、收尾跑 `python3 tools/docsync test`。
34. **BL-00115 對賬之三態**：rust 案經 `APP_TRUST_MODEL_PATH` 讀交付檔——env 缺席＝具名跳過；env 在而讀檔失敗（ENOENT，含 LL-00033 單檔 bind mount 懸空）＝紅、訊息附 RUNBOOK §2 之 force-recreate 重建命令；讀得到＝逐欄對賬。活體契約 dev 交付形一節刪手抄 toml 區塊時，其正下方「只填此項…二態」殘句同批改寫（指回交付檔、改三態＝BL-00125 鏡像之一）。凡改 `deploy/trust-model.dev.toml`（含純註解）之單元，單元序 MUST 先重建 rust-api 容器再跑容器測試。
35. **CDP 走查還原面＝必擴**（原「若擴四表」條件式作廢）：①CDP 寫入步驟一律對走查自建之角色與選單；seed 列只讀②移除面觀察（刪除／buttons 移除→歸檔＋判定面同步）以 psql 對自建選單植顯式大 id 之政策列（如 R_ADMIN 之選單維／按鈕維），再經 UI 操作、觀察 Admin 側不重啟即失效③擴 `tools/walkthrough-baseline.py` restore：`sys_role`／`sys_menu`／`casbin_rule` 刪 `id >` 基準上界列＋setval 回 snapshot 現讀值、`sys_casbin_policy_archive` 依水位刪＋setval；工具自測逐字釘住④以 SQL 還原動過 `casbin_rule` 後 MUST 重啟 rust-api（casbin 無門鈴、記憶體判定面不會自行換版；BL-00094 之 casbin 同形），工具輸出補救句；RUNBOOK §9c 同批改。此工具單元排在 CDP 單元之前。
36. **as-built 改寫義務**（RL-0011 四形種子：名稱／「隨…進場」與「待 005」形／facade 與 handler 名冊／舊數量詞；各單元收尾以 `python3 tools/docsync errata <詞>` 枚舉、改完復掃；允許清單以現算結果為準）。已知命中面——碼內：`enforce.rs` 檔頭與 init doc、`main.rs`、`state.rs`（「即終態」「不提供運行期重載」「運行期只有讀鎖」）；`facade/sys_menu.rs`／`sys_role.rs` 檔頭「本刀只落讀面」「…不搬」與測試支數句；`facade/mod.rs` 成員名冊與「一張表配一支」；`test_kit.rs` 收窄集外業務表句；`model/audit.rs`「寫入時機恰五處」與寫入者列舉；`sys_operation_log.rs` 只提規則四寫端；`obs.rs` 檔頭宣告面現況（補 `casbin_reload_total`）；`envelope.rs`「唯一生產者」句；`router.rs`／`contract.rs` 之「二十二」；`handler/mod.rs`／`handler/ip_rule.rs`／`handler/common.rs` 之 005 重評預告句與「頁上界就地住本檔」；`route.rs` 檔頭「seed 常量列零、後端回 []」。外層：活書 04（寫後全量重載）、06（島 F ① boot 步、島 F ⑧ 三態、「島 A～F」計數）、08 §8.1（memo「UI 兌現隨對應 UI 刀」）／§8.2／§8.3（回收桶撞詞）／常量路由 `[]` 句／巨集邊界與 ADR-00040 款 6 指針、10 §10.2（新增島 H 情境一則：刺激＝重建失敗注入、回應＝保留舊面絕不全拒、量測＝`casbin_reload_total` 三 outcome、守門＝同步測案名；段首「島 G～J 隨其進場刀」句與「A～F」計數改寫）、11（款 3 寫入者集、款 5 指針）、12（「A～F 入上表」與 frontmatter）；RUNBOOK §9c／§11／§12／§13／§16.1／§16.2；`docs/ops/reference-src/schema-definition.md`（hide_in_menu 釋義句、memo §5 預告句）與 `trust-model-config.md`；`deploy/trust-model.dev.toml` 之「二態」註解；`rules.yml` 之「（島 G1）」註；現在式面指向 ADR-00040 之 10 行＋BACKLOG 條文 BL-00043／BL-00091 內之指針改指 ADR⑤。
37. **RUNBOOK 觀測維運兩條**（承 rev5 同刀、依 rev5 形重打字；只寫實跑過的命令＝RUNBOOK 檔頭紀律）：§11 補選單域 advisory 鎖觀測（pg_locks classid／objid 拆讀 64-bit key、bigint 直比恆假，指向 `sys_casbin_archive.rs` 觀測 helper doc）與 `casbin_reload_total` 三 outcome 判讀；§13 補 `casbin-reload-anomaly`（outcome=exhausted）處置＝查結構化 log 之 cause、修復 DB 連線後重啟 rust-api、驗計數。005 起 `obs016-casbin-reload-anomaly` 首次有事件源＝維運面不得無處置。
38. **前端補正**：ip-rule 頁改為 `:show-add="hasAuth('ipRule:add')" :show-delete="false" @add="handleAdd"`、不再覆寫 default 插槽、刪除隨 prop 形失效之疊寫碼註（seed 下 ip-rule 頁與 `ipRule:*` 只授 R_SUPER ⇒「無權者看不到新增」CDP 不可達，改以模板靜態斷言守）；未知 `wbip_type` 之退顯守放在標籤映射取值**與 `$t` 呼叫之前**（取不到即回原字串、不呼叫 `$t`）。共用元件新 boolean prop 之預設 true MUST 以 `withDefaults`（或解構預設）承載——Vue boolean casting 下純型別宣告之 `showAdd?: boolean` 未傳時為 false、既有 role／user／menu 呼叫端會靜默失去新增與批刪鈕；review 必核。menu modal 父選擇器 add／addChild／edit 三模式皆顯、選項樹前插合成頂層節點 id 0（`parentId=0` 豁免父驗證之唯一 UI 入口）、補 `page.manage.menu.form.parentRoot` 鍵；role drawer 編輯態 roleCode 鎖定、role-search 重置後即重查（rev5 as-built 三項 UI 行為）。
39. **constant 首度可寫之端到端**：005 起後端 `getConstantRoutes`（Public 免登入）首次可能回非空、前端 AUTH-WIRING(a) 併入分支首次真的合併 ⇒ quickstart 與 CDP 各加一步（建頂層常量選單→登出狀態下該列可見、builtin 五條仍在、路由可達→刪除並還原）；contract 補「有常量列時非空」一案（由守衛清列），既有真庫案「seed 下回 []」與守衛之相交處理於 plan 定。
40. **島 H 入憲字面**：H1 終態成員句不得帶刀集外刀名與未來式（GT-05 會擋、user 親決後再改字即須二次確認）——改寫為「授權治理刀之寫端」一類描述形＋現在式條件句（「該等端點不存在期間本條 vacuous 成立；端點落地即入域、零修憲」）。ADR① 記載已依憲法 §I.7 承襲指針表尾句完成跨島刻意不一致之重審——結論＝跨島註不動（同步失敗方向由島 H 序言委任 ADR② 承載、與島 F 之 F2 保留上一份良好規則集同向）；是否把同步失敗方向提前寫進島 H 條文屬可選強化、plan 期評估。H3 增補受保護選單不可停用、不可改父（R1-Q5／R2-Q1）。
41. **淨流量確定值**：依 `tools/docsync/events.py` 之 metrics 口徑，在「005 收刀前開放窗不再有其他 misc 事件、005 本身 `backlog_add` 為零」兩前提下，005 收刀之 rolling-3 淨流量＝13（w3 +24、w4 +1、w5 −12），仍超標、結構上不可能於本刀回到 ≤0（需單刀淨收 ≥32）；首個可達 ≤0 之窗＝006 收刀（需 w6 ≤11）。揭露型／新欠型分型於收刀事件 notes 記。
42. **BL-00116 補述**：wire 快照自 typings 抽出、表達不了數值界 ⇒ 上界改由 contract test 逐端點釘；IP 契約零語意前進、依 ADR-00041 決定 2 之收錄判準不抽活體家（plan research 載此評估）；U1b 在 `handler/ip_rule.rs` 模組 doc 與 `router.rs` 該列註解加指針（分頁規則指活書 08 §8.2、未知類型指 ADR⑤）。
43. **BL-00125 鏡像以 errata 現算**：「二態」之現在式命中不止三處（另有 `deploy/trust-model.dev.toml`、`trust-model-config.md`、活書 06 守門欄描述、`middleware/mod.rs` 與 `handler/auth/login.rs` 兩處測試 doc）⇒ ADR⑤ 單元允許清單以 `python3 tools/docsync errata 二態` 結果逐處分流（改三態／史料不動／accepted 不動）、改完復掃；兩處碼註排入該單元之子庫 commit。

## 1. rev5 承襲盤點（沿用項照已驗證結論施工、翻案項用新設計；CLAUDE.md §2）

| rev5 項目 | 處置 | rev6 落點 |
|---|---|---|
| 五個 user story（US1 角色全生命週期／US2 選單樹／US3 回收桶與復原／US4 判定面同步／US5 roleHome）與優先序 | 沿用；getAllPages 入 US2（Q2） | 本刀範圍 |
| 16 支端點契約形（`rev5:005` contracts wire-role-admin／wire-menu-admin） | 沿用其形、rev6 座標改寫（+getAllPages、`current` 上界入契約、getMenuList/v2 `size` 缺席全取〔R1-Q4〕、getDeletedMenus 缺席 size 100→10〔**翻案**、隨共用 helper〕、同層排序入契約、status 類恰二值嚴格 parse 明文化；工程判斷 4／29／30） | `specs/005-role-menu-crud/contracts/` |
| 選單域 advisory 序列化域（島 H1；鎖序、txn 首動作、pg_locks 機器證兩坑） | 沿用；key 字面翻 `rev6menu` | 新 `facade/sys_casbin_archive.rs` 域鎖底座 |
| casbin rebuild-swap＋keep-last-good＋RELOAD_SERIAL（含 `rev5:B-105` 交錯時序 harness）＋觸發矩陣恰移除面三支＋deleteRole 免 reload | 沿用（HEAD 形）；★**翻案**：deleteRole 家族實際歸檔才同步（R1-Q2、矩陣五支）；帶入 rev5 同刀之三道名冊守恆（工程判斷 25） | `auth/enforce.rs`；ADR② |
| 授權歸檔寫入面＋reason gate（三值 `role_soft_delete`／`menu_soft_delete`／`menu_button_removed`） | 沿用；五值＝006 | `facade/sys_casbin_archive.rs` |
| role 守門矩陣（seeded→in-use→self-role；停用雙護欄；code 形制不可變；批次 no-partial；空陣列提前 no-op） | 沿用；addRole 23505 兜底（工程判斷 28）；in-use 口徑、批刪查無與重複 id 語意交 clarify（R1-Q1） | `facade/sys_role.rs`＋`handler/role.rs` |
| 選單狀態機五條（治理域／顯示域分層、防環、parent 三處一致、不可變錨欄、constant 父鏈、同鍵重建零繼承、child-first、復原不回灌） | 沿用；restore 第四腿自首版即入；清除常量性之反面腿交 clarify（R1-Q1）；★rev6 增補：受保護列不可停用、不可改父（R1-Q5／R2-Q1）；href 限 http(s)、buttons 驗形制＋長度（R1-Q6／R1-Q7）；寫端實作不變式（工程判斷 27） | `facade/sys_menu.rs`＋`handler/menu.rs` |
| 治理清單分頁列凍結形（`rev5:ADR 0060`） | UI 沿用；後端 `size` 預設 100 → **翻案**缺席全取（工程判斷 5） | menu 頁＋getMenuList/v2 契約 |
| 表頭插槽疊寫（`rev5:B-099` 形） | **翻案**：共用元件 prop（Q5） | `table-header-operation.vue`＋menu／ip-rule 兩頁 |
| getAllPages 於 006 支撐讀、住 `handler/role.rs` | **翻案**：提前入 005（Q2）、依域住 `handler/menu.rs`（工程判斷 16） | `handler/menu.rs` |
| ILIKE 共用件 `ilike_contains`（`rev5:B-138` 於 007 第三份時收攏） | **提前**至 005 收攏（G3；005 後兩份三呼叫點）、連帶 BL-00096 具型簽章；同批上提 `violated_constraint`／`now_ts`（工程判斷 22） | `facade/mod.rs`＋`sys_role.rs`＋`sys_ip_rule.rs` |
| IP 域已知態載體＝單支 ADR（rev6 ADR-00040 為其對應） | **翻案**：新 ADR supersede ADR-00040（G1） | ADR⑤ |
| memo 兩欄（`role_memo`／`menu_memo`；G5） | 沿用 | 列表欄＋drawer／modal textarea |
| i18n 鍵集：`biz.role.*` 10 鍵＋`biz.menu.*` 12 鍵（含 rev5 後補 `nameRequired`／`routeNameInvalid`）＋page 樹 9 鍵（role：roleMemo／form.roleMemo；menu：menuMemo／showDeleted／restore／confirmRestore／restoreSuccess／form.menuMemo／form.parentRoot） | 沿用 HEAD 鍵集（★第二輪更正：menu 實為 12 鍵、page 樹漏 parentRoot）；另增 `biz.menu.hrefInvalid`／`biz.menu.buttonsInvalid`（R1-Q6／R1-Q7）⇒ 拒因 24 鍵；落地時序見工程判斷 24 | `error.rs` MSG_KEYS 19→43；三檔 locale；`app.d.ts` |
| 譯文權威＝各刀 `contracts/msg-keys.md` | **翻案**（已由 ADR-00039 定）：三檔 locale backend 子樹各為該語之家；`msg-keys.md` 只作候選鍵表 | 三檔 locale |
| 測試守衛（rev5 005 期＝SequenceResetGuard 尚在） | **翻 rev5 005 期形、承 rev6 004 G6**：水位形＋arm 時現讀之序列還原（RL-0031）＋`sys_user_role` FK 清理序 | `tests/common`＋`test_kit.rs`（工程判斷 10） |
| `handler/common.rs`（rev5 005 期無此檔、`rev5:B-094` 後補八件） | rev6 既有兩件直接消費＋第三寫端三件重評（BL-00113）＋HEAD 形共用件首版即入（工程判斷 23） | `handler/common.rs` |
| `PageRes` 上移（`rev5:005` FR-002） | rev6 已在 envelope、項消 | — |
| 已知態三組＋getAllRoles／roleHome 零 UI 消費者窗 | 沿用；走 ADR④；★第二輪依 rev6 實況改寫並擴為八款（授權彈窗真樹假勾選、getAllRoles 之 upstream drawer 消費者、按鈕碼無人持有、原始 i18nKey、三列裸 route 鍵等；工程判斷 9） | 本刀已知態 ADR |
| 部分更新之空字串（`rev5:B-102`：可空欄 `""`→NULL、名稱欄 `""`→不動） | **翻案**：立 ADR⑥ supersede ADR-00015（R1-Q3；名稱欄 `""` 改拒 `nameRequired`） | ADR⑥＋`handler/role.rs`／`handler/menu.rs` |
| rev5 後刀增量（006 grant 面、reason gate 五值、getAllButtons／getAllEndpoints、policy-archive、endpoint modal、三顆 modal 接真；007 sid／no-escalation／user 域；008 audit） | 不帶 | — |
| `rev5:005` 收刀後本域修補（工程判斷 1 清單） | 沿用（HEAD 形自首版即入） | 各對應檔 |

## 2. BACKLOG 觸發項處置（動工前掃描、CLAUDE.md §2；41 開放＋4 滯後逐條對觸發欄；以 BACKLOG 現文為準）

| 分類 | 條目 | 005 的哪一步 |
|---|---|---|
| 收・拍板已定 | BL-00116 逾界 clamp 入契約與活書 08 §8.2（Q3）｜BL-00095 by-design 入 ADR⑤＋前端顯原字串（Q4、G1）｜BL-00125 ADR-00040 款 2 三態隨 ADR⑤ 重述、鏡像字面同批（G1；★處數以 errata 現算、不止三處＝工程判斷 43）｜BL-00117 共用元件加 prop（Q5）｜BL-00082 `load_trust_model` 標頭名體檢＋案（G2＋R1-Q8）｜BL-00096 `sys_ip_rule` 四寫端具型簽章（G3、隨 ILIKE 收攏連帶；三檔 40 處＝工程判斷 21）｜BL-00111 測試模組閉合 lint 腿（G4；①②同收＝工程判斷 18） | 004 域連帶單元（U1b）／治理面單元／前端單元／體檢單元 |
| 收・必到期 | BL-00105 六型「序列化鍵集＝快照 properties 鍵集」斷言（或具名豁免；★射程擴及本刀新讀型＝工程判斷 32；ADR⑤ 款 6 同批對齊）｜BL-00112 `PageRes` 進受審 definition 名冊｜BL-00109 qs 序列化前提機器錨（python 側、staged-gate 短路之前無條件跑；工程判斷 6）｜BL-00113 `common.rs` 三件重評（含測試側 `table_state` 家族；工程判斷 11）｜BL-00110 7777 腿 denylist 鍵 RAII（005 動 tests/common 守衛即到期；複用 `RestorePlan.keys` 腿） | wire 面單元／第三寫端單元／測試基建單元 |
| 收・Amendment 併入 | BL-00098 島 E 兩句｜BL-00118 量法句與範圍欄數字｜BL-00119 §I.7 段首增補計數句 | U0 主線 Amendment |
| 收・收刀前承載體檢單元 | BL-00106 泛型實例化名冊腿（工程判斷 7）｜BL-00107 型級解析普查案（釘錨數＝解析成功數）｜BL-00123 `request_context_absent` 每請求總數案（`with_local_recorder` 套 app 級不可行即改 serial 差值形）｜BL-00115 dev 信任模型三鏡像（契約檔 toml 區塊刪、改指交付檔；toml⇔`dev_trust_model()` 由 rust 案經 `APP_TRUST_MODEL_PATH` 對賬、三態判準＝工程判斷 34）｜BL-00111 lint 腿（工程判斷 18） | 專用體檢單元（吃一次容器 cargo 週期、排收刀全量測試同批） |
| 改條文（不收） | BL-00120：ADR-00040 半由 ADR⑤ 補齊 rev5 出處、條文改為只剩 ADR-00039（不為補出處單獨翻案）｜BL-00093：②之 casbin-reload 規則錨對齊屬部分兌現、★條文加戳記（工程判斷 19）｜BL-00045：條文補「選單管理頁治理清單顯 route 裸鍵」曝光面（ADR④ 款⑥；工程判斷 9） | ADR⑤ 單元／U3／治理面單元 |
| 不觸發・反向確認入 spec | BL-00047 零建表｜BL-00028 零新設定鍵｜BL-00048 不帶 no-escalation（ADR② 兌現 ADR-00014 決定 4 之預告、不 supersede ⇒ 其後果段對沖面不動）｜BL-00074 兩半皆不觸發（`FRONTEND_MSG_CONSUMERS` 仍 1 筆；`.githooks/pre-commit` 之 submodule-sync 段零 diff——BL-00109 腿不擴收窄 pathspec）｜BL-00108 兩半皆不觸發（零第三份 char 級解析組；`entity_behavior_lint.rs` 零 diff——005 零 entity／migration 變更）｜BL-00123 之「新增發射腿」半邊不觸發（角色／選單寫端拒寫不推 `request_context_absent` 計數；其總數案仍由體檢單元收） | spec 反向確認清單（收刀體檢時 grep 現算） |
| 射程外・Out of Scope 指承載 | 006：BL-00045（policy-archive 頁）｜007：BL-00031／BL-00065／BL-00091／BL-00124｜008：BL-00027／BL-00043／BL-00044／BL-00045（settings／audit 頁）｜000-r3 等：BL-00002／BL-00035／BL-00036／BL-00039／BL-00101／BL-00102｜觀測：BL-00084／BL-00093｜滯後卷 BL-00029／BL-00049／BL-00064／BL-00067 | spec Out of Scope 逐條指承載 |

預估：開放 41 → 22（收 19、改條文 3：BL-00120／BL-00093／BL-00045）；`backlog_add` 目標零（已知態走 ADR④／ADR⑤；R1-Q2 選實際歸檔才同步＝零新 BL）。★淨流量確定值見工程判斷 41（兩前提下 005 收刀＝13、仍超標；首個可達 ≤0 之窗＝006 收刀）。收刀 `backlog_done` 清單＝BL-00082／BL-00095／BL-00096／BL-00098／BL-00105／BL-00106／BL-00107／BL-00109／BL-00110／BL-00111／BL-00112／BL-00113／BL-00115／BL-00116／BL-00117／BL-00118／BL-00119／BL-00123／BL-00125。揭露型／新欠型分型於收刀事件 notes 記（體檢 §7 判準）。★Out of Scope 另補 rev5 凍結時仍開放、rev6 零承載之兩條：軟刪×歸檔之事後對賬掃描（`rev5:B-025` 殘餘②→背景 job 刀）、`sys_casbin_policy_archive` 之 retention／清理政策（`rev5:B-016` 之歸檔表一面→須明確納入憲法 §I.7 島 J 或 C4-E2 資料血緣疊圖之承載；005 為該表首個寫入者）。

## 3. 設計（十節、user 已核可；grill 兩輪修訂已併入）

### §1 目標與範圍

**入刀**：US1 超管管理角色全生命週期（列表分頁＋名稱／代碼模糊＋狀態篩選＋memo 欄；新增／編輯／刪除／批刪；三層守門）｜US2 超管管理選單樹（treeTable 治理域、新增目錄／選單、父選擇器自 getMenuTree、頁面下拉自 getAllPages、buttons／constant 可寫、防環與父驗證、不可變錨欄、刪除連動歸檔＋判定面同步、child-first 批刪）｜US3 選單回收桶與復原（toggle 換源 getDeletedMenus、復原四腿重驗、不回灌）｜US4 刪除後殘留授權即時失效（rebuild-swap、keep-last-good、絕不全域拒絕）｜US5 角色首頁指定（寫端不驗一致、讀端兜底既有 `resolve_home`）。量級：ROUTES 22→39（`/systemManage/{getRoleList,getAllRoles,addRole,updateRole,deleteRole,batchDeleteRole,getMenuList/v2,getMenuTree,getAllPages,addMenu,updateMenu,deleteMenu,batchDeleteMenu,getDeletedMenus,restoreMenu,getRoleHome,updateRoleHome}`、全政策保護、授權態照 seed 逐列）、MSG_KEYS 19→43、憲法 1.4.0→1.5.0、ADR 六支。另含 004 域連帶四件（分頁 helper 改引、ILIKE 收攏＋BL-00096、BL-00082 boot 體檢、ADR-00040 supersede）與 002 域一件（ADR-00015 supersede＝R1-Q3；002 設定寫端行為零變化）。

**零 migration、零 seed 變更（事實）**：Q1 要點所列；`migration/src` 維持恰 `m0001`／`m0002` 兩支；seed 78 列選單、163 列政策、3 列角色不動；`list_active` 等寫死 78 之既有測試零改動。⇒ BL-00047／BL-00028 不觸發。

**不入刀**：三維授權治理六支、getAllButtons／getAllEndpoints、授權回收桶讀端與 restorePolicy、結構性封死、島 G 入憲、兩顆授權 modal 做真、policy-archive 頁（006）｜user 域一切（007）｜稽核頁（008）｜no-escalation 本體（BL-00048）｜通用節流 seam（BL-00124）｜列表排序能力（BL-00044 同族）。

### §2 憲法 Amendment 射程（MINOR 1.4.0→1.5.0、一筆；ADR①）

- **§I.7 島 H 五條**：以 rev5 v1.7.0 字面（`rev5:ADR 0048`）為底、不取 v1.10.0 終態（後者含 006 之 reason gate 五值兌現句）；rev6 座標改寫＝H1 終態成員句寫成描述形＋現在式條件句（「授權治理刀之寫端」；「該等端點不存在期間本條 vacuous 成立；端點落地即入域、零修憲」——不帶刀集外刀名與未來式、否則 GT-05 擋；工程判斷 40）、序言「G 位保留給授權治理刀之島 G，兩島對偶（H2↔G3、H1/H5↔G5、H3↔G4）」、★H3 增補「受保護選單 MUST NOT 可停用、MUST NOT 改父」（R1-Q5／R2-Q1；rev6 增補、ADR① 註明）；常數（advisory key、上溯上限、route_name 形制）留活書；ADR① 記依 §I.7 承襲指針表尾句之跨島重審結論。承襲指針表 H 列尾註「rev6 已入憲 v1.5.0」；MAJOR 射程句「六島」→「七島」。
- **島 E 補兩句**（BL-00098）：解鎖端點之操作稽核 MUST 先於解鎖標記寫入；操作者上下文缺席 MUST 拒寫 5000、不得以佔位位址補足稽核列（與島 F 之 F3① 同向）。措辭 plan 期定、user 親決時定稿。
- **§III.2 用途 (ii)「role／menu 管理頁 CRUD 接真」恰 9 檔**（檔級硬邊界）：`src/views/manage/role/index.vue`／`role/modules/role-operate-drawer.vue`／`role/modules/role-search.vue`／`src/views/manage/menu/index.vue`／`menu/modules/menu-operate-modal.vue`（五支修改型、逐行 `原行:`）＋`src/components/advanced/table-header-operation.vue`（附加 `showAdd?`／`showDelete?` 兩 prop、預設 true；修改型）＋`src/locales/langs/{en-us,zh-cn}.ts`（新增型圈界、`page:` 樹 `manage.role`／`manage.menu` 既有子命名空間之資料級補鍵 9 鍵；範圍欄寫 rev5 as-built 預估各 4 塊、實數以標記為準＝工程判斷 20）＋`src/typings/app.d.ts`（新增型、`Schema.page` 對應型節；同上）。紀律欄：兩顆授權 modal 明文不入名單（本刀出現任何 diff＝紅、tasks 帶 `git diff` 零輸出斷言）；`menu/modules/shared.ts` 兩向 diff 零改不入；兩語鍵集 MUST 相等；`route:` 樹零新增（role／menu route 鍵 upstream 既在）；路由外掛產物四檔零變動（不新增 view 頁）；`components.d.ts` 重算走 §III 生成檔紀律。後端拒因鍵落三檔 locale `backend:` 樹與 `app.d.ts` backend 型節＝既有 I18N-WIRING (ii)(iii) 授權內、不隨本款擴列。
- **BL-00118**：表外宣告 1 量法句改為與活書 08 同一量法（圈界塊／單行形分列、修改型 `原行:` 數）、既有列範圍欄數字依現算對齊；**BL-00119**：§I.7 段首「rev6 增補恰四處」句改為以 Amendment log 為準之敘述。
- **硬序**：Amendment accepted 前不動任何 base-web 既有檔（backend 24 鍵落 locale 既有檔亦受閘）；純後端單元可先行；慣例沿 003／004＝U0 排最前；Amendment 顆只含憲法＋ADR①＋README 憲法版本鏡像＋generate（§V.2 步 4、工程判斷 12）。

### §3 後端架構與模組邊界（承 rev5 as-built HEAD 形、重打字、rev5 出處帶前綴）

`handler/role.rs`（role 6＋roleHome 2＝8 支）、`handler/menu.rs`（menu 7＋getMenuTree＋getAllPages＝9 支）、新 `model/facade/sys_casbin_archive.rs`（域鎖底座兩形薄 fn＋`menu_domain_waiter_count` 觀測 helper、歸檔寫入面、reason gate 三值單點 fn、移除面掃描：deleteRole 全三維含 protected／deleteMenu 跨角色 menu 維＋獨有 button 碼／updateMenu 絕版碼）、`facade/sys_menu.rs`（治理域讀端 `list_governed`、樹寫端狀態機、守門、`paginate_top_level`）、`facade/sys_role.rs`（CRUD 寫端、鎖讀 helper、`SEEDED_ROLE_IDS`／`SUPER_ROLE_CODE` 常數＝單一宣告源）、`facade/mod.rs`（`ilike_contains`／`violated_constraint`／`now_ts` 共用件；`sys_role`／`sys_menu`／`sys_ip_rule` 引用；工程判斷 22）、`facade/sys_ip_rule.rs`（改引共用件＋四寫端與 `find_by_id_for_update` 具型簽章＝BL-00096；連動 `ipgate/mod.rs`／`handler/ip_rule.rs` 測試模組＝工程判斷 21）、`config.rs`（`load_trust_model` 標頭名體檢＝BL-00082、R1-Q8 形）、`auth/enforce.rs`（`rebuild_enforcer`／`reload_enforcer`、`RELOAD_SERIAL`＋T020 形注入式 seam、`RELOAD_MAX_ATTEMPTS=3`／`RELOAD_RETRY_BACKOFF_MS=50`、metrics `casbin_reload_total{ok|retry|exhausted}`；檔頭、init doc、`main.rs`、`state.rs` 之「終態」諸句改寫指 ADR②）、`envelope.rs`（分頁 helper：current clamp [1,10^7]、size clamp [1,100]、預設 1／10＋明名全取入口；`handler::ip_rule` 改引；工程判斷 4／R1-Q4b）、`router.rs`（+17、`ROUTES_COUNT` 22→39、contract case_key 逐條；外層 `test_references.py` 釘值同批＝工程判斷 33）、`handler/common.rs`（第三寫端三件重評＋HEAD 形共用件＝工程判斷 11／23）、`model/audit.rs`（零新 variant 釘；寫入時機列舉改寫）、`obs.rs`（預註冊 reload 三 outcome、與 rules.yml 錨對齊；降級來源名冊維持恰八）、新 lint 兩支（判定面同步三道名冊守恆＝工程判斷 25；測試模組閉合＋obs 別名匯入＝工程判斷 18）。

### §4 序列化域與判定面同步

- **序列化域**：`SELECT pg_advisory_xact_lock($1)` 走 raw Statement（守 `entity_access_lint`）、xact 級自動釋放、零逾時零重試；key＝`0x7265_7636_6D65_6E75`（＝8243124669107236469，與 per-user 鎖之 bigserial uid 不可能碰撞）；進域＝addMenu／updateMenu／deleteMenu／batchDeleteMenu／restoreMenu＋deleteRole／batchDeleteRole（addRole／updateRole／roleHome 不進域）；固定鎖序 `advisory → 歸檔表列 → sys_role 列 → sys_menu 列 → casbin_rule`、域鎖 MUST 為 txn 首動作、不下沉 facade fn；與 per-user advisory 鎖（`login.rs`、uid 為 key）結構性無 ABBA（key space 不碰撞＋鎖集合零交集）、三失效條件入 ADR②；機器證＝逐寫端各一支 pg_locks NOT-granted 等待測（64-bit key 拆 classid／objid、bigint 直比恆假；非 pg_blocking_pids）；★擁有交易之入域寫端失敗腿一律顯式 rollback＋源碼釘（LL-00026；工程判斷 27④）。
- **判定面同步**：另建全新 Enforcer 四步鏡像 init（model→adapter→new→load_policy）、任一步失敗整體 Err 不產實例、成功才 write 鎖內一行 move-assign；`RELOAD_SERIAL` 包 rebuild＋swap 含重試全程互斥（封「後 commit 先 swap 蓋回舊快照」窗；交錯時序機器證＝`rev5:B-105` T020 形 `#[cfg(test)]` 注入式 seam、生產建置零存在）；keep-last-good＋結構化告警＋metrics 三 outcome（rules.yml `obs016-casbin-reload-anomaly` 既錨於 retry／exhausted、字面對齊）；耗盡仍失敗＝維持舊面持續告警。觸發矩陣五支（★第二輪：deleteMenu／batchDeleteMenu／deleteRole／batchDeleteRole＝軟刪成功且實際歸檔 ≥1 列才觸發；updateMenu＝buttons 絕版歸檔實際發生才觸發；不得寫成「成功即觸發」＝`rev5:B-104`；R1-Q2／工程判斷 26）、於交易 commit 之後、以 `if archived` 為門；被拒／無作用／標的不存在／標的零政策列＝結構性不觸發；addMenu／restoreMenu 零觸發。★前提與殘窗（ADR② 明記）：單一 rust-api 行程（多行程／多實例部署＝翻案觸發）；耗盡窗內 H2 記憶體半邊不成立、addMenu 同鍵重建與 restoreMenu 可經殘留面繼承，恢復靠下次成功同步或重啟（RUNBOOK §13 處置＝工程判斷 37）。★硬禁令＋版本鎖：絕不對 live enforcer 裸呼 `load_policy`（casbin 2.20.0 clear-then-load、空 policy 在 MODEL_CONF 下＝含 R_SUPER 全 deny、唯重啟可救；升版必重核）；呼叫端不得持 `state.enforcer` 讀鎖呼叫。四支測試：失敗注入（壞 conn ⇒ 舊面續 allow R_SUPER）／裸呼必轉紅負向自證／觸發條件特性鎖定（含零政策列刪除＝零 reload）／移除面端到端（DB＋in-memory 雙斷言；含角色維「刪除→同碼重建→直種指派→零命中」）；另補成功路徑 swap＋ok、三併發完成性案與三道名冊守恆 lint（工程判斷 25）。

### §5 選單與角色狀態機、分頁、稽核、拒因

- **選單**：治理域（未刪含停用）／顯示域（啟用未刪）分讀，治理候選誤用顯示域＝「停用靜默升級為永久撤銷」必配負向測試；防環（上溯上限常數）；parent 三處一致（新增／改父／復原＝父存在且未刪、停用不擋、`parentId=0` 豁免）；`routeName`／`menuType` 出現即拒（值不比對）；constant 父鏈守門於 create／update／restore 三處（restore 第四腿：標的常量時驗全祖先、非常量零驗；update 清除自身常量性時之反面腿交 clarify＝R1-Q1）；★受保護列 updateMenu 拒「轉停用」與「改 parentId」（同鍵 `protectedMenu`；R1-Q5／R2-Q1）；href 空字串→NULL、有值限 http(s)（`hrefInvalid`；R1-Q6）；buttons 須 null 或陣列、成員 code 非空字串 ≤100 字元不重複、壞形在絕版計算前即拒（`buttonsInvalid`；R1-Q7）；歸檔掃描先於標的列 UPDATE、絕版判定排除自身、依 v2 維度過濾（工程判斷 27）；addMenu 同鍵雙層守門（域內先驗顯式拒＋23505 兜底同一拒因）；同鍵重建零繼承雙封；deleteMenu 守門固定序（protected→未刪子項不論啟停）；batch child-first no-partial 單 txn（查無 id 與重複 id 語意交 clarify＝R1-Q1）；空陣列提前 no-op（零副作用、零稽核、不取域鎖）；幽靈父收縮語意不動；restoreMenu 成對清空軟刪欄＋原 status 保留、零 casbin 寫零同步。
- **角色**：三層守門固定序 seeded→in-use（`others = total − operator_is_member`、拒因回誠實總掛載）→self-role；停用雙護欄（不可停用自身所屬、R_SUPER 恆禁）；code 形制 `^[A-Za-z0-9_]{1,64}$`＋活性唯一＋不可變（出現即拒）；addRole 同 code 雙層（`codeExists` 先驗＋索引名收窄之 23505 兜底；工程判斷 28）；部分更新三態（ADR⑥ 取代 ADR-00015：可空文字欄 `""`＝清空落 NULL）、全 None 提前 no-op；NOT NULL 名稱欄送顯式 null 或 `""`＝`nameRequired`（兩域同式；R1-Q3）；in-use 口徑交 clarify（R1-Q1）；deleteRole 掃 `v0=role_code` 全三維含 protected 列 archive-move（`role_soft_delete`）、實際歸檔 ≥1 列即 commit 後同步判定面（R1-Q2）、單向無 restore；roleHome 寫端不驗可見樹一致。
- **分頁**：共用 helper 四端點同用（getRoleList／getDeletedMenus／getMenuList/v2／IP 規則清單；缺席→1／10、顯式 0→(1,1)、壞形→整串預設＝IP 清單 as-built）；getMenuList/v2 `size` 缺席＝回全部頂層、回應 `size`＝實得頂層數、`current` 忽略回 1（R1-Q4；走明名入口、白名單恰此一支＝R1-Q4b）、有值 clamp；getMenuList/v2 與 getMenuTree 同層序 `(order ASC NULLS LAST, id ASC)` 入契約（工程判斷 29）；getDeletedMenus 穩定排序 `deleted_at DESC, id DESC`、不帶 `restorable` 旗標；getRoleList `id ASC`、模糊欄走 `ilike_contains`；getAllRoles 僅活性且啟用、無 memo；getMenuTree 治理域輕量樹 `{id,label,pId,children}`；getAllPages 顯示域 route_name 全集 `(order ASC NULLS LAST, id ASC)`。
- **稽核與拒因**：每寫端同交易恰一列（`sys_operation_log::write_in_txn`）、`AuditOperation` 沿 add／update／delete／restore、`entity_table` 區分、batch 逐標的一列——spec FR 寫成三寫端（IP 規則、角色、選單）一致要求；操作者自 `common::operator_from_context`、缺席拒寫 5000（`tracing::error!` 帶各域 target 留呼叫點）；body 收斂走 `common::json_or_default`；角色／選單寫端缺席拒寫事件形照 rev6 004（`refused`、不帶 degraded 欄、不增計、target 取各域字面；工程判斷 11）；拒因全純 key 一因一鍵、復用 2222／5003／4040／5000、13 碼矩陣零觸碰；`biz.role.*` 10 鍵（codeInvalid／codeExists／codeImmutable／notFound／seededProtected／inUse／cannotDeleteSelfRole／cannotDisableSelfRole／superCannotDisable／nameRequired）＋`biz.menu.*` 14 鍵（notFound／routeNameExists／routeNameImmutable／menuTypeImmutable／parentNotFound／cycleDetected／hasChildren／protectedMenu／constantParent／restoreConflict／nameRequired／routeNameInvalid＝rev5 as-built 12；hrefInvalid／buttonsInvalid＝R1-Q6／R1-Q7；★第二輪更正原「11 鍵」之誤數）；新鍵隨首個發出單元落地（工程判斷 24）。

### §6 前端

- 修改型＝用途 (ii) 9 檔逐行 `原行:`；新增型新檔＝兩支 wrapper＋兩支 typings（工程判斷 3）；getAllPages 走 upstream barrel。
- **role 頁**：列表接真（分頁＋搜尋＋狀態）、memo 欄＋drawer textarea（placeholder「管理員可見」）、刪除／批刪接真（拒因 toast 由共用攔截層轉譯 `backend.biz.role.*`、頁內只看 error）；drawer 編輯態 roleCode 鎖定、搜尋重置後即重查（rev5 as-built）；「菜單權限／按鈕權限」兩鈕與其彈窗一行不動（G3）——005 後菜單權限彈窗顯示真選單樹＋寫死勾選＋真首頁下拉、送出假成功（已知態、R1-Q9、006 接真）。
- **menu 頁**：treeTable 接真（治理域、無參一次取全樹、分頁列凍結形照 `rev5:ADR 0060`：`itemCount: undefined`＋`pageCount: 1`＋自備 prefix——`itemCount` 與 `pageSize: 0` 並存會算出 `Infinity` 頁數凍死瀏覽器）、回收桶 toggle（prefix slot NSwitch 綁 `showDeleted`、資料源換 getDeletedMenus、操作欄整欄換復原、復原鈕無按鈕碼 gating）、切換清勾選（`rev5:B-100`）、pageSize 歸位（`rev5:B-132`）、memo 欄＋modal textarea、已刪模式以 `:show-add`／`:show-delete` 關寫入口（Q5）；modal：父選擇器 NTreeSelect 消費 getMenuTree（add／addChild／edit 三模式皆顯、選項樹前插合成頂層節點 id 0＋`form.parentRoot` 鍵）、`fetchGetAllRoles` 殘留不帶、頁面下拉走 getAllPages（Q2）、`multiTab` 可寫、hideInMenu 可寫（R1-Q10）、編輯態 `routeName`／`menuType` 鎖定。
- **ip-rule 頁**（rev6 新檔、免授權）：疊寫改 `:show-add="hasAuth('ipRule:add')" :show-delete="false" @add="handleAdd"`、不再覆寫 default 插槽（★只寫 show-delete 會令無權者看到備援新增鈕且點了無作用；工程判斷 38）；type 標籤對映射取不到之值退顯原字串、守在映射與 `$t` 之前（Q4）。
- **共用元件**：`showAdd`／`showDelete` 預設 true 以 `withDefaults` 承載（純型別宣告下未傳＝false、既有呼叫端會靜默失鈕；工程判斷 38）；user 頁與 role 頁表頭對 22089 驗鈕仍在。
- **i18n**：`page:` 樹補 9 鍵（`manage.role.{roleMemo,form.roleMemo}`、`manage.menu.{menuMemo,showDeleted,restore,confirmRestore,restoreSuccess,form.menuMemo,form.parentRoot}`；兩語鍵集相等）；`backend:` 樹 24 鍵三檔同補（隨首個發出單元）；`app.d.ts` page／backend 兩型節；`msg-key-gate`／`fork-delta-lint`（修改型只在 9 檔）／`wire-schema check`／`view-render-guard`／`route-artifact-gate`（零變動＝冪等綠）全過。static meta 不維護（spec 一句紀律）。

### §7 治理面

- ADR 六支（工程判斷 12）；契約落點（工程判斷 8）；as-built 改寫義務清單（工程判斷 36、以 errata 現算為準）；活書 as-built（feature branch 內改成現在式）：§5 模組（handler／facade 新檔、enforce 同步面）、§6 新情境「選單域生命週期——島 H」（移除面→歸檔→commit→rebuild-swap→swap；失敗 keep-last-good）＋島 F ⑧ 三態字面（ADR⑤）、§8.2 API 慣例（跨端點分頁規則、ROUTES 39、MSG_KEYS 名冊指針）、§8.4 fork-delta（用途 (ii) 逐用途 as-built、共用元件 prop）、§10 島 H 品質情境一則＋「A～F／G～J」計數句、§11 已知態列（指 ADR④／ADR⑤、IP 域三態字面、寫入者集）、§12 詞彙（工程判斷 15）＋「A～F 入上表」句，另 04／06 島 F ①／08 §8.1／§8.3 見工程判斷 36。RUNBOOK：§16.1 dev 分界三態（ADR⑤）、§16.2 標頭名核對句（BL-00082）、§12 碼面閘表（BL-00109 腿之「守什麼」與已知邊界）、§9c 走查還原面（必擴＝工程判斷 35）、§11／§13 觀測維運兩條＋同步耗盡處置（工程判斷 37）。reference-src：`schema-definition.md`（hide_in_menu 釋義＝R1-Q10、memo 預告句）、`trust-model-config.md`（失敗語意表＋dev 交付形殘句）。BACKLOG 條文：BL-00120 改為只剩 ADR-00039；BL-00093 ② 加戳記；BL-00045 補曝光面。
- 收刀簿記：`backlog_done` 19、`backlog_add` 目標零、`adrs` 六支、`arch_impact` 以收刀時 `git diff --stat <merge-base>..HEAD -- docs/arc42/` 現算之節號為準（預估 §5／§6／§8／§10／§11／§12；該欄 append-only 且不在勘誤欄集、寫錯改不回）。

### §8 測試與驗收

- **rust（容器內 `cargo test --workspace -- --test-threads=1`）**：contract 22→39 逐端點 case＋授權態矩陣（Admin 對寫端 5003、對 getRoleList 通；R_USER_COMMON 對 getAllRoles 通）＋真 seed app 面逐鍵實打 24 鍵（名冊雙向閘；工程判斷 24）；守門矩陣（role 三層固定序、停用雙護欄、menu 守門序、constant 父鏈 create＋restore、不可變欄出現即拒）；序列化域逐寫端機器證；判定面同步四支＋RELOAD_SERIAL 交錯時序案（T020 形）；零繼承端到端（DB＋in-memory 雙斷言、★先種 live 授權再測防恆綠；選單維與角色維各一）；分頁 helper 四端點案（逾界＝上界值、缺席各自語意〔三支 1／10、選單清單全取〕、`&size=0&current=0` 回 (1,1)、壞形整串預設、全取入口恰一處生產呼叫之名冊案）；`ilike_contains` 轉義案＋`sys_ip_rule` 具型簽章後既有案全綠（BL-00096）；`load_trust_model` 標頭名體檢一正一反＋反案與對拍（BL-00082）；wire-schema 新增受審型（實數以 extract 為準）＋鍵集全等表驅動斷言（BL-00105 六型＋本刀新讀型）＋`PageRes` 名冊（BL-00112）＋各新 query 型首屏真串抽取案；i64 守衛 lint 名冊擴＋泛型實例化名冊腿（BL-00106）；測試模組閉合 lint 腿＋obs 別名匯入腿＋變異自證（BL-00111 ①②）；判定面同步三道名冊守恆（工程判斷 25）；寫端不變式三案（工程判斷 27）；同層排序擾動案（工程判斷 29）；受保護列兩腿／href／buttons 拒因案（工程判斷 31）；守衛表集（含 `sys_user_role` FK 清理序）＋自證測（工程判斷 10）；BL-00110 RAII；`each_domain_keeps_its_own_log_literals` 不變；`schema-gate` 三閘綠。
- **python**：`tools/wire-schema.py` 加 qs 序列化前提腿（BL-00109；staged-gate 短路之前無條件跑）＋self-test；外層 `tools/docsync/tests/test_references.py` ROUTES +17 列；`tools/walkthrough-baseline.py` restore 擴面＋自測（工程判斷 35）；體檢單元之 BL-00107／BL-00123／BL-00115 案。
- **前端**：`pnpm typecheck`、`fork-delta-lint`（各新增塊拔標記必紅之變異自證）、兩顆 modal `git diff` 零輸出斷言、`components.d.ts` 只准 NTreeSelect 兩行增列之斷言、ip-rule 頁 `show-add` 綁權限之模板靜態斷言、`msg-key-gate`、`wire-schema check`、`route-artifact-gate`。
- **CDP 三方對照**（127.0.0.1:22080 vs 32080、必要時加 22089；★一律 opus agent）：判準＝結構清單逐項全等（工程判斷 14）；role 頁列表／搜尋／新增／編輯／刪除／批刪／memo；menu 頁樹／新增（父選擇器三模式、頁面下拉）／編輯（buttons 移除→歸檔＋同步：對自建選單植顯式大 id 政策列後操作）／刪除／批刪／回收桶 toggle／復原／memo／常量選單端到端（工程判斷 39）；ip-rule 頁表頭 prop 形零差異；user／role 頁表頭對 22089；ADR④ 已知態各款逐項實際操作觀察（觀察路徑→症狀）；拒因步驟以網路請求事件證請求發出；排除清單見工程判斷 14。寫入步驟只對自建列；真登入走查前後 `tools/walkthrough-baseline.py` snapshot／diff／restore（RUNBOOK §9c；restore 擴面＋動過 `casbin_rule` 即重啟 rust-api＝工程判斷 35）。
- **收刀前承載體檢單元**：BL-00106／BL-00107／BL-00110／BL-00111／BL-00123／BL-00115 落地（BL-00123 總數案至少打一支角色或選單寫端、釘住「其餘端點 2 格」）；反向確認 BL-00047／BL-00028／BL-00048／BL-00074（兩半）／BL-00108（兩半）零觸發現算。

### §9 單元切分草案（tasks 定稿於 SDD；此處只定骨、約 20 支）

U0 ★主線、拆兩顆：①Amendment 顆（憲法＋ADR①＋README 憲法版本鏡像＋generate）②ADR②③⑥ 親決 accepted 另顆（硬閘；ADR④⑤ 留 draft 至 U15）→ U1 Setup（handler 骨架、router +17＋外層 `test_references.py` 釘值同批、`AuditOperation` 零新 variant 釘）→ U1b 004 域連帶（分頁 helper 落 envelope＋全取明名入口＋`ip_rule` 改引、`ilike_contains`／`violated_constraint`／`now_ts` 上提＋BL-00096 具型簽章〔三檔 40 處、含 `ipgate/mod.rs` 測試模組〕、`load_trust_model` 標頭名體檢＝BL-00082；review 烤入 004 域防回歸與「零 wire 行為變更」判準）→ U2 域鎖底座＋ABBA 機器證＋歸檔寫入面＋reason gate（寫端不變式①②③）→ U3 rebuild-swap＋RELOAD_SERIAL（四測＋T020 交錯案＋成功路徑／三併發完成性＋三道名冊守恆 lint；rules.yml 錨對齊；「終態」諸句改寫）→ U4 測試基建（守衛表集含 `sys_user_role`＋自證測＋`tests/common` 側庫態守衛＋BL-00110 RAII＋大 id 號段登記）→ U5 治理域讀端七支（getRoleList／getAllRoles／getMenuList/v2／getDeletedMenus／getMenuTree／getAllPages／getRoleHome；同層排序＋擾動案）→ U6～U7 role 寫端（facade TDD→handler＋router；第三寫端進場時 BL-00113 三件重評＋HEAD 形共用件；`biz.role.*` 10 鍵與三檔 locale 隨本單元落地、雙 pin 同顆；deleteRole 實際歸檔才同步＝R1-Q2）→ U8～U10 menu 寫端＋回收桶＋constant 守門＋受保護列兩腿＋href／buttons 驗證＋reload 接線（`biz.menu.*` 14 鍵依發出點分批隨單元落地）→ U11 零繼承端到端（選單維＋角色維）→ U12 wire 面（新增受審型＋鍵集全等表驅動＋BL-00105／BL-00112／BL-00109＋首屏查詢串錨）→ U13～U14 前端（role 頁＋memo／menu 頁＋toggle＋memo＋父選擇器三模式＋prop（withDefaults）＋ip-rule 三件＋未知類型守）→ U15 治理面（活書、RUNBOOK、reference-src、dev toml〔改後先重建容器〕、ADR④⑤ 親決 accepted＋ADR⑤ 之 supersedes 補入與 ADR-00040 轉 superseded、as-built 改寫義務清單、BACKLOG 三條改條文）→ U16 收刀前承載體檢（BL-00106／BL-00107／BL-00111／BL-00123／BL-00115）→ U16b 走查還原工具擴面（`walkthrough-baseline.py`＋RUNBOOK §9c；排在 CDP 之前）→ U17 全量閘＋CDP 三方對照 → 收刀簿記。高風險共享檔序列鏈（同檔任務不標 [P]）：`facade/sys_menu.rs`、`facade/sys_role.rs`、`facade/sys_casbin_archive.rs`、`facade/mod.rs`、`handler/role.rs`、`handler/menu.rs`、`handler/common.rs`、`router.rs`、`tests/contract.rs`、`error.rs`＋三檔 locale＋`app.d.ts`（隨寫端單元分批）、`envelope.rs`（U1b）、`facade/sys_ip_rule.rs`＋`ipgate/mod.rs`＋`handler/ip_rule.rs`（U1b）。★fix 允許清單另納跨 repo 連動釘值檔（外層 `tools/docsync/tests/test_references.py` 等）。編排慣例：★2026-09-23 起全角色 `opus[1m]` xhigh、prompt 首行帶 DEEP_THINK（user 令；骨架改動另走輕量軌、TDD 發射前完成）；防呆六件套。

### §10 風險

1. **rebuild-swap 失手＝含 R_SUPER 全 deny、唯重啟可救**（最高風險件）：硬禁令＋版本鎖＋失敗注入測＋裸呼必轉紅負向自證；RELOAD_SERIAL 封交錯窗＋T020 形交錯時序機器證自首版帶入。
2. **共用元件 prop＝新 upstream 衝突面**：附加 prop 預設 true、既有呼叫端零改、修改型標記兩處；rebase 時原行同步。
3. **業務表逐列比對＋序列 setval 期望值**：測試殘列或序列推進即 gate2 紅；守衛表集（含 `sys_user_role`、FK RESTRICT 清理序）＋自證測、造列顯式大 id（號段登記）、arm 時現讀序列還原（RL-0031）；`casbin_rule` 造列走真 `add_policy` 必 setval。
4. **治理清單 `size` 缺席全取＝與 rev5 契約字面不同**（R1-Q4）：契約明寫、contract test 釘、全取入口恰一處生產呼叫之名冊案防擴散；UI 零差異（額外一發重取、CDP 排除請求次數維度）。
5. **`common.rs` 第三寫端重評**可能改兩件簽章：既有兩域字面守釘住 target 不併。
6. **rev5 後刀增量混入**（006 grant 面／007 sid／no-escalation／008 audit）：藍本取 HEAD 形時逐檔剔除、防回歸清單烤入 implementer prompt。
7. **seed 78 寫死測試與 `ROUTES_COUNT`**：零 seed 變更；ROUTES 22→39 同 commit bump。
8. **CDP 對照基準已含 006～008**：排除清單逐項、已知態實際操作觀察（`rev5:L-054`）。
9. **004 域連帶四件擴大 review 面**（分頁 helper 改引／ILIKE＋BL-00096 三檔 40 處測試改交易／BL-00082／ADR-00040 supersede 之鏡像以 errata 現算）：獨立單元 U1b 與 U15 承載、review prompt 烤入 004 域防回歸與「零 wire 行為變更」判準；`sys_ip_rule` 既有案全綠為 U1b 出口。
10. **新鍵與 msg 閘的時序夾擊**：拒因鍵排在寫端之後＝msg-key-gate 擋、排在之前＝contract 名冊雙向閘擋 ⇒ 鍵隨首個發出單元落地、雙 pin 同顆（工程判斷 24）。
11. **CDP 走查毀 seed**：移除面觀察若動 seed 選單即 archive-move seed 政策列、gate2 永紅且 SQL 還原不令判定面換版 ⇒ 寫入步驟只對自建列、restore 擴面＋重啟 rust-api（工程判斷 35）。
12. **受保護選單 UI 自鎖**：停用或改父受保護列可令管理區從 UI 消失 ⇒ 兩腿守門（R1-Q5／R2-Q1）；走查劇本不得嘗試於非測試帳號繞道。
13. **判準字面落地即誤紅**（BL-00111 lint、觸發矩陣「成功即觸發」）：施工前以現樹實跑模擬判準、再定稿（工程判斷 18／26）。

## 4. 憲法 §IV 九題預答（供 `/speckit-plan` Constitution Check 起手）

1. base-web 為權威：PASS——role／menu 頁為 upstream demo 面、其 fetch 標的正是本刀補齊對象；wire 型別開獨立命名空間、demo 頁欄定義同批改（修改型）。2. base-web inline：**涉及——授權以 Amendment 先行取得**（用途 (ii) 9 檔；backend 24 鍵走既有 I18N (ii)(iii)）。3. casbin：PASS——本刀對選單域只做 CRUD 資料面、可見性授權屬 006、零 seed 改動；已知態③（新建選單側欄不現）＝本項誠實結果。4. §I.3：PASS——13 碼零觸碰、2222 復用、id number＋2^53 守衛、`PageRes` 四欄不變、跨端點分頁規則入活書 08 §8.2；IP 規則清單未知類型上 wire＝ADR⑤ by-design。5. 前代拷貝：零拷貝、重打字消化、rev5 出處帶前綴；差異點表（§1 翻案列＋工程判斷 1 剔除清單）入 research。6. §II：零抵觸；翻碼內舊拍板（`enforce.rs` 檔頭與 init doc、`main.rs`、`state.rs` 之「終態」諸句）立 ADR②、並明引 ADR-00014 決定 4 之「運行期重載屬後刀」為被兌現之預告（不 supersede）；翻 ADR-00040 以 ADR⑤ supersede；翻 ADR-00015 以 ADR⑥ supersede（R1-Q3）。7. ★軌道：**涉及——授權以 Amendment 先行取得**，屬既有軌道加用途、非補完。8. 新表：零、零 migration；消費五表皆 001 基線既有。9. 行為島：**涉及——島 H 隨本刀 MINOR 入憲（H3 增補受保護選單不可停用／不可改父）、島 E 補兩句**；依 §I.7 承襲指針表尾句完成跨島重審（ADR① 記）；state-machine 鏡頭（選單／角色／判定面三矩陣）。

## 5. 給 `/speckit-specify` 的輸入摘要

- feature 名＝`005-role-menu-crud`；user 故事核心＝超管建／改／刪角色與選單、選單回收桶與復原、刪選單（或刪已授權角色）後授權在判定面立即失效且失敗時服務不中斷、角色首頁指定；17 支端點、零 migration、憲法 1.5.0、ADR 六支；另含 004 域連帶四件與 002 域 ADR-00015 supersede（治理 US 或 FR 段承載）。
- 直接輸入：本檔＋§2 處置表所列 BL 條目（收 19／改條文 3／反向確認／Out of Scope）＋`rev5:005-role-menu-crud` spec 之 US1～US5、FR-001～FR-051、SC、Edge Cases（rev6 座標改寫：17 支與 ROUTES 39、`rev6menu`、`size` 缺席全取〔僅 getMenuList/v2〕、共用元件 prop 形、BL-00095 by-design、rev5 後補鍵集自首版即入〔拒因 24 鍵〕、`PageRes` 既在、rev6 水位守衛形、getAllPages 入 US2 且住 menu 域、譯文權威三檔 locale；★第二輪加：deleteRole 家族實際歸檔才同步、受保護列不可停用／改父、href 與 buttons 形制驗證、空字串語意依 ADR⑥、status 類恰二值嚴格 parse、getDeletedMenus 缺席 size 10、同層排序入契約、回收桶門＝頁級授權〔刪 rev5 spec 之「列級可復原性」陳舊字面〕、已知態依 ADR④ 八款實況）。
- FR 措辭要求：操作稽核落列寫成 IP 規則／角色／選單三寫端一致要求（體檢 §0.2 #4）；角色／選單寫端缺席拒寫不帶 degraded 欄、不增計（工程判斷 11）；分頁規則寫成跨端點 FR（含 getMenuList/v2 缺席例外之白名單性質）；判定面同步觸發條件寫成「實際歸檔 ≥1 列」同一字面（工程判斷 26）；`sys_ip_rule` 具型簽章與 `load_trust_model` 標頭名體檢以「零 wire 行為變更」FR 承載（後者含「非法名不得回退預設名」）。★spec 須含「★ 軌道逐處登記」表（憲法 §III.2 機制骨架：位置／軌道·用途／型別／改動內容／upstream 衝突風險；風險判準承 004；收錄用途 (ii) 九檔＋`components.d.ts`，並把 `table-header-operation.vue` 標為本刀新衝突面）。
- ★clarify 必問候選（R1-Q1：首輪 clarify 已問過前三題、user 選重做時重新出題；一題一問、rev5 as-built 為各題建議項）：①update 清除自身常量性時，若治理域全深後代（含停用、不含已刪）存常量後代，是否拒並回 `constantParent`（rev5 T024-fix 反面腿）②deleteRole 之 in-use 計數是否含停用或已軟刪使用者之指派（rev5 `count_by_role` 不濾；self-role 同口徑含停用角色；007 須定軟刪是否清指派）③批次刪除清單含查無（不存在或已軟刪）id 時是否整批拒回 `notFound`④批次清單內重複 id 是否先去重再處理（rev5 as-built 靜默去重、只落一列稽核）。
- ★原 clarify 候選轉工程判斷（specify 直接抄、clarify 不再以此出題）：①`handler/common.rs` 重評結論落點（工程判斷 11）②分頁 helper 落點與四端點改引（工程判斷 4）③BL-00109 錨落 python 側（工程判斷 6）④BL-00106 收法（工程判斷 7）⑤契約落點（工程判斷 8）⑥四表守衛與 setval 值（工程判斷 10）⑦CDP 排除清單與模型（工程判斷 14）⑧`menu-operate-modal` 頁面下拉走 getAllPages、父選擇器走 getMenuTree（Q2、G4）⑨getMenuList/v2 缺席語意（R1-Q4／Q4b 已由 user 拍定）⑩BL-00082 體檢形（R1-Q8 已拍、工程判斷 17）⑪BL-00111 lint 腿形（工程判斷 18）⑫getAllPages 落點（工程判斷 16）⑬第二輪工程判斷 21～43 全數（specify 直接抄；其中屬 user 可見者皆已經 R1／R2 拍板）。

## 6. 隨做隨記

- ADR 待立六支（刀分支內落、序號接續現行最大號）：Amendment／判定面同步 rebuild-swap／A1 域行為／本刀已知態與 by-design／IP 域已知態 supersede ADR-00040（G1）／部分更新語意 supersede ADR-00015（R1-Q3）。plan 期全數產 draft；親決時點依工程判斷 12（U0 拆兩顆：Amendment 顆只含 ADR①；ADR②③⑥ 另顆；ADR④⑤ 留至 U15）。supersede 形承 ADR-00032 翻 ADR-00016 前例（新檔 `supersedes`、明列各款原意續行或改寫、款號對照表、現在式引用改指新檔）。★草稿期 `supersedes: []`（proposed ADR 宣告 supersedes 會令 generate 立即回填舊 ADR 之 `superseded_by`、GT-04 隨即要求舊 ADR 轉 superseded）；accepted 那一顆同批補 `supersedes`、改舊 ADR status。
- 收刀 `backlog_done` 19 條（§2 清單）；改條文三條：BL-00120（只剩 ADR-00039）、BL-00093（② 加戳記）、BL-00045（補選單管理頁曝光面）；`backlog_add` 目標零；淨流量確定值見工程判斷 41、分型記收刀事件 notes（體檢 §7）。
- 流程坑（首輪 SDD 實踩）：①docsync lint 之 GT-05 只掃 tracked＋staged——新增 ADR／spec 檔先 `git add` 再跑 lint（或以 pre-commit 為準）②rev6 刀集（specs 目錄＋events feature 欄導出）外之刀名一律寫成提及形（反引號或「」），前代刀號一律帶冒號前綴（`rev4:009-…`）③proposed ADR 之 supersedes 時點見上條。
- NOTES「下一步」：specify 起手後同批改為 005 進行中；收刀時改 006。
- 依賴：零新依賴（casbin 2.20.0 既釘；NTreeSelect 為 naive-ui 既有元件、零新套件；標頭名判別用 `axum::http::HeaderName`＝既有 axum 再匯出、★不得另加 `http` 直接依賴）——毋須版本拍板。
- TDD 發射前置：★user 2026-09-23 令編排全角色改 `opus[1m]` xhigh、implementer prompt 首行亦帶 DEEP_THINK——改骨架（`tools/orchestration/_sk_head.js` 模型家常數、`_sk_main.js` implementer prompt、`EXAMPLE-dual-implementer.mjs` 快照、`harness-test.mjs` 斷言、README 模型字串句）另走輕量軌、於 TDD 發射前完成；CDP 單元照舊 opus（user 2026-09-20 明令）。
- 活書：§3 設計 §7 所列各節於 feature branch 內改成現在式；C4-L2 拓樸不變（零新容器）。
- 本檔 grill 修訂（G1～G4、工程判斷 16～20、§2 處置表 41→22）另顆落地於 default（004 前例 `bfaba0e` 形）；第二輪修訂（R1／R2 十二題、工程判斷 21～43 與各節逐處改對）同形另顆落地於 default。
