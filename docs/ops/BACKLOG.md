<!-- next: BL-00077 -->
# BACKLOG — 待辦

條目形 `- BL-NNNNN｜<product／governance>｜<一句話>｜<觸發條件（必填、須可到期）>`；配號取檔頭 next 後 bump、號碼永不回收；完成即刪列、git 即史（RL-0050）。
滯後項另居 `BACKLOG-DEFERRED.md`（user 拍板暫不排程；配號計入 GT-05 家族、STATE 分開計數）——查待辦全帳須兩卷併看。開放條數不設上限（觀測值、只報表；RL-0052／ADR-00011）；觸發條件寫「何時該做」、ADR 翻案觸發器寫「決定何時失效」，兩者不混。
觸發條件以「動 X」為條件者（「下次動 X 時」「下一支／首個動 X 的單元或刀」等各形），「動」＝改到 X 的可執行碼（X 本身是測試設施時含其測試碼）；純註解／doc 改寫、或只在 X 所在檔的測試模組增案而 X 本體未改，皆不構成到期。

- BL-00002｜governance｜GT-10 對 `docs/arc42/09-architecture-decisions.md` 的兩腿在首個 AI-ADR 落地（「目前無」句移除）後互斥：子項名冊腿要 `rad_ai_map` 鍵 ⊇ E5 七欄、第八腿要值＝同檔 `###`，而 E5 子項住 ADR body 的 `####`→ E5 改由 ADR 檔面守或豁免 §9 兩腿（工具改動、一正一反自證）｜觸發：首個 AI-ADR 開寫前
- BL-00027｜product｜讀端 wire 集合≠registry 鍵集：`check_type_consistency` 第③臂讓宣告集外之列（setting_type 在認識集）照常上 wire——沿 rev5 讀端只驗認識集、spec FR-009 射程外（`rust-api/server/src/validation.rs` 已自陳）；若要求「registry 集合＝wire 集合」須另立拍板｜觸發＝設定頁 view 刀進場、或 registry 鍵集首次變動時
- BL-00028｜governance｜002 刀 U3 兩份手工轉錄雙表零機器互鎖：handler `SEED_EXPECTED`↔`migration` m0002 `SEED_SYSTEM_SETTINGS`、`validation.rs` `REGISTRY`↔data-model §3——測試側只擋日後單邊漂移、擋不住初次轉錄同錯（U3 審查以 python 解析字面機器比對過一次＝全等）；改以自真源字面解析驅動期望表或加跨檔對賬測｜觸發＝m0002 seed 或 data-model §3 任一變動時
- BL-00031｜product｜settings registry 跨鍵不變式零守：`password_min_length ≤ password_max_length`、`login_throttle_captcha_after ≤ login_throttle_max_fails`、`ip_captcha_after ≤ ip_max_fails` 等成對鍵各自獨立驗證、可寫成互斥值（002 刀 validation 只驗單鍵型別與界）｜觸發＝三對不變式之消費側各自進場、可分刀落地：004 ip-trust-anchor（`ip_*`）／007 user-password-admin（`password_*`）（消費側決定不變式方向與拒因；`login_throttle_*` 一對已由 003 auth-session 之 `throttle::load_settings` 整組退常數消費＝矛盾值不採用、registry 側該對不變式仍零守）
- BL-00035｜governance｜事件帳守衛群（000-r2 L1-06／L1-07／P3-P1）：①`summary`／`reason` 與 `notes` 同樣全文進 MILESTONES／STATE，卻無 `notes_gt06_risks` 那道真源側守衛，且不在 `ERRATUM_FIELDS`＝寫壞無補救 ②events.jsonl 自稱 append 型單一事實源，但無任何閘比對 HEAD 版、既有列可被靜默改寫（erratum 機制的整個前提在機器面零守；形制照 GT-04 的 HEAD 對比腿）③000-r1 review 事件 notes 的分流計數自相矛盾（稱 confirmed 86、列出項相加不符），事件 append-only 且 notes 不可更正、只能於報告記 ④單筆事件驗證失敗時，錯誤應以該筆 ERROR 指名、下游閘（GT-03 在途判讀等）不得對無效事件續作判讀（003 刀簿記實證：feature_close `summary` 超 300 字使整筆事件無效，GT-03 隨之假報在途 27 筆＋`test_real_repo_appendix_matches_events` 紅——GT-02 雖擋、徵狀卻先落在下游閘）——四者皆屬既有 GT-02／GT-03 補腿、不占閘數｜觸發：下次動 events schema 或 GT-02 腿時
- BL-00036｜governance｜閘↔規則對賬與 RL 承載群（000-r2 L3-04／L3-03／L2-04）：①閘 docstring 的 `rule=` 欄與 RULES `carrier` 欄零機器對賬——carrier=lint 四條無任何閘指名（★GT-03 指名 RL-0053 而其 carrier 實為 checklist 之實例已於 000-r2 D 修單改為 `rule=RL-0055`、本項只剩「無機器對賬」本身） ②RL-0051「每條閘一正一反自證、變異打在判準上」carrier=lint 卻無任何閘或測試實作，12 閘的正反測試存在性只靠人工枚舉 ③RL-0015 標 carrier=lint 但 GT-06 之 `TENSE_WARN` 只掃其同義集五詞中的三詞且僅 WARN，「隨…刀進場／尚無…」兩形零覆蓋、活書 11 處命中而 lint 全綠（★擴詞會使既有 11 處轉紅、須同批改對）｜觸發：000-r3 之前，或任一條 carrier 欄改動時
- BL-00039｜governance｜探針 hops 口徑未入骨架指示（000-r2 P5-P1；★原①編排骨架 `_vars`／`_context`／`_allowed` 五常數零斷言已於 maint-backlog-40-39-37 收：`_sk_head.js` 三常數兩形共用、`_sk_main.js` 兩常數〔拼接序使 head 取值落在 const 暫時死區〕、harness-test 十五→十八案）：探針 hops 口徑未明示「`ls`／`find`／`grep`／`git log` 等存在性反證不計入」，使應查無題的自報跳數系統性膨脹（000-r2 之 P5：自報口徑 1/7 vs 開檔口徑 6/7）；grader 側口徑（增益跳不計為繞路）亦只住 000-r1 報告 note 欄、未入骨架指示｜觸發：下次動 `tools/orchestration/**` 或 000-r3 發射前
- BL-00042｜governance｜跨刀活體契約仍住 spec 目錄（000-r2 C1-3；兩鏡 real 確認／decided 不確定、主線裁定轉 BL）：RUNBOOK §10 之 Day-1 三步與演進登記檔形制契約仍引 `specs/001-schema-baseline/contracts/{gates,schema-evolution}.md`，與 ADR-00012 為同型缺陷所立的「跨刀活體不住 spec 目錄」原則相牴觸；語意張力＝ADR-00010 決定 4／5 明文指派 contracts/ 為契約權威，ADR-00012 的原則未擴用亦未具名豁免 contracts/。抽取決策本身屬拍板級、須立 ADR（provenance 引 ADR-00012 同型）｜觸發：下一支帶 migration 的刀開寫前
- BL-00043｜product｜稽核 log 寫入面零承載（002 刀 Out of Scope；003 開刀前承載體檢）：`sys_operation_log`／`sys_access_log` 自 002 延後（該刀 spec 明文零寫入）後，接手刀與觸發在現在式全帳零登記——BACKLOG 兩卷／NOTES／RULES／RUNBOOK 皆零命中，憲法 §I.7 承襲指針表島 J 只涵蓋 retention 與 reporting、不含寫入面；兩表結構已隨 001 基線在庫｜觸發＝008 audit-settings-pages 刀進場，或任一刀首次需要寫稽核 log 時
- BL-00044｜product｜設定清單列表排序零承載（002 刀 Out of Scope；003 開刀前承載體檢）：16 鍵讀端一次回全集、無排序參數；rev5 以 `rev5:B-027` 承載該延後項，rev6 未帶進任何現在式面（BACKLOG 兩卷／NOTES／RUNBOOK／ADR 全集零命中，ADR 內「排序」命中皆非本義）｜觸發＝設定頁 view 刀（008 audit-settings-pages）進場，由其消費側決定要不要排序與排序欄白名單
- BL-00045｜product｜四支管理頁 view 於 base-web 尚缺、選單點擊 404（002 刀 Out of Scope；rev5 由 `rev5:B-008` 承載）：`manage_system-settings`／`manage_policy-archive`／`manage_audit`／`manage_ip-rule` 四列選單之 `component` 指向的 view 在 upstream `example` 基線不存在，選單與 casbin 政策已隨 001 基線先行；該已知態原僅由 `docs/ops/reference-src/schema-definition.md` §8 一句承載、且該句自我延後（「條目隨 UI 刀立」），開刀前掃 BACKLOG 觸發欄結構上掃不到；★另一面（003 auth-session U11 CDP 對照走查實證）：側邊欄**展開態**該四列選單標題於 rev6 顯 i18n 裸鍵（`route.manage_*`）、rev5 顯簡中——四鍵之 `route` 樹譯文由頁進場刀以憲法 §III.2 頁進場型 inline 與 view 同批帶入；收合態零差異｜觸發＝008 audit-settings-pages 刀進場（該刀範圍由其 brainstorm 定、可翻）
- BL-00046｜governance｜entity 關聯宣告腿零機器守衛（003 開刀前承載體檢）：資料面兩拍板「無 DB FK 之邏輯關聯不建 ORM 關聯宣告」與「ActiveModelBehavior 恆空」共掛 `rust-api/server/tests/entity_behavior_lint.rs` 一個機器錨，但該測只守後者（掃 impl 站點數＝表 entity 檔數、非空實作即紅），關聯宣告腿零機器承載、只靠人工紀律｜觸發＝下次動 `rust-api/entity/**` 或 `entity_behavior_lint.rs` 時
- BL-00047｜governance｜schema 閘表數斷言之改對義務失去指派對象（003 開刀前承載體檢）：`tools/schema-gate.py` 的 `len(tables) != 14` 硬編碼，ADR-00012 決定 6 指派「隨 002 首次加表時由該刀改對」，而 002 已零加表收刀；現在式面（RUNBOOK §10 加表清單／BACKLOG／NOTES）零承載，義務懸空｜觸發＝首支帶 create table 之 delta migration 的刀開寫前
- BL-00048｜governance｜ADR-00014 後果段對 no-escalation 本體形的預告與前代 as-built 不符（003 開刀前承載體檢；user 拍板 2026-09-07：走 BL 對沖、不翻案）：該 ADR 後果段寫「本體刀只填掛點函式體、零簽章變更、零呼叫端改動」，而前代 as-built 之真本體＝`rev5:server/src/auth/no_escalation.rs` 的 body 級純函式（規則＝`T ⊆ A ∧ N ⊆ A`）、落 `rev5:007-user-password-admin`、呼叫端遍及使用者域寫端十檔；照該預告估工會把跨檔工作估成填一個函式體。ADR body 不可變（GT-04），本條即其對沖｜觸發＝no-escalation 本體刀（006 authz-governance）brainstorm 起手前
- BL-00058｜governance｜憲法 §III.2 ★BASE-WEB-I18N-WIRING (iii) 範圍欄「`src/typings/app.d.ts`（1 處，修改型）」與 as-built 相反：純插入 `backend` 必填型節、無 upstream 既有行被動、`原行:` 無可填，落地形＝新增型 START／END 圈界（`fork-delta-lint.py` 19 處修改型不含該檔；活書 05／08 已記新增型）；範圍欄之「修改型」為全表通用粗標籤（(b)／(c)／LOGIN-CAPTCHA(i) auth store／LOGOUT-UX(i) 四處同形皆兼含新增型圈界、名冊斷言射程僅修改型）、(iii) 之特殊處＝零修改型；ADR-00026:49 條文全文同字（accepted body 不可變＝GT-04）、`specs/003-auth-session/spec.md` fork-delta 風險表亦標修改型；碼面無誤、無閘會紅、只能人工落帳｜★user 2026-09-14 拍板＝留帳：觸發＝下一次 §III.2 Amendment 同批修正（PATCH 級、§V.2、新 ADR、ADR-00026 body 不動；射程＝只改 (iii) 一欄或一併精確化五欄、屆時再定）
- BL-00062｜contract｜`POST /auth/logout` 之 5000 出口（`RequestContext::from_headers` Err、`internal()` 之 DbErr／commit 失敗）未進 `contracts/wire-auth.md` 情境表（三列皆 0000、與 login／refresh 兩表列 5000 之體例不一），且 DB 故障窗內「有效票→5000／垃圾票→0000」構成弱 token 有效性 oracle（FR-010 立論僅降級期成立）；候選＝列入契約表、或把 DbErr 折成 0000 no-op｜觸發＝下次動 logout 或補 wire 契約鏡像時
- BL-00064｜governance｜base-web 第二 HTTP 客戶端 `src/service-alova/request/**` 未接 I18N-WIRING(i)（錯誤文字顯裸 msg key）且自帶獨立 refresh 路徑（不共用 axios 側 `refreshTokenPromise` 單飛、直寫 localStg 憑證對），與島 A rotation／reuse 偵測潛在衝突；003 全刀零承載；現況 dev 僅 `/auth/error` demo（恆 4040）走真後端故不可達｜觸發＝首個動 demo 頁或 alova 路徑的刀（可與收刀既定的 `/auth/error` 排程錨同批）
- BL-00065｜security｜會話面零帳號活性閘（島 I 增量、007 域；rev5:007 同位五腿未搬）：refresh 無鎖內活性重驗、`enforce_mw` 與 getUserInfo 亦不查 `status`／`deleted_at` ⇒ 停用或軟刪帳號之既有會話不被撤且可無限續發新對；現只住 `refresh.rs` 模組 doc「★三件不搬」與本條｜觸發＝007 user-password-admin（首個提供停用／軟刪寫端者）brainstorm 起手前納入範圍
- BL-00066｜product｜L1 lock 一經武裝即以武裝當下的窗值固定 TTL、命中不重讀 L2（`throttle/mod.rs` ① 早退、③ 一次算定），三門檻鍵放寬（調高 `max_fails`／調短 `window`）在鎖存續期間至多 min(窗,900) 秒後才生效＝有界已知態（data-model §7「立即」為規劃期措辭）；候選＝命中後仍以 L2 現值複核、或以 lock 值之寫入時戳重算 TTL｜觸發＝下次動節流三區或設定鍵熱更新面時
- BL-00067｜product｜`/auth/error` demo 端點排程錨：base-web upstream 兩張 demo 頁經 `fetchCustomBackendError` 打 `GET /auth/error`（003 ROUTES 16 條不含、現恆 4040）；延後理由＝拍板級衝突（回吐 client 任意 code／msg 撞保留碼 `9999` 三錨與憲法 §I.3 信封不變式）＝憲法 §I.1「v1 從簡只能是交付排程」（003 spec Out of Scope）｜觸發＝首個動 demo 頁或決定 demo 頁去留的刀（與 BL-00064 service-alova 同批）
- BL-00070｜governance｜src 測試模組仍有六支自持「oneshot＋解封包」殼，與 test_kit `oneshot_json`（body 上限／JSON 解析／panic 措辭的單一份）並存：`handler/route.rs` `integration_tests::get_json`（須帶 query、bearer 可選＝組好 req 後可委派）、`auth/enforce.rs` `probe`、`router.rs` `send`、`handler/system_settings.rs` `post_probe`／`post_update_bytes`／`probe_endpoint`（system_settings 寫端助手於 maint-backlog-pre-004 A1 裁定不在 BL-00052 射程）——同形邏輯各持一份＝改一處漏一處（maint-backlog-pre-004 A1 碼品質審查升級）；候選＝逐支核同形者改委派 `oneshot_json`、非 JSON 信封形者具名留存並於 test_kit 該 doc 列明｜觸發＝下次動上列任一測試模組的可執行碼時
- BL-00071｜governance｜base-web 我方 inline 散文與 rev5 同檔逐字重疊（`tools/comment-overlap.py` 豁免 fork-delta 三型後量）：`src/store/modules/route/index.ts` 100.0%（「併入 static 常量集而非取代…五條 builtin 常量路由」一段 100 字元）、`src/views/_builtin/login/modules/pwd-login.vue` 5.6%（「呼叫本函式→故障持續即無限迴圈…」一段 42 字元；值為標記豁免收窄為只剝 token 後之量）——憲法 §I.5 第 3 款「註解一律重寫」未達；改寫落在 fork-delta 標記處（route/index.ts＝修改型標記行之續行散文；pwd-login.vue＝新增型 START／END 圈界內）、須同守 fork-delta 標記紀律（`原行:` 載荷與標記 token 不動）（maint-backlog-pre-004 A2b 豁免後重量新發現）；候選＝兩段 rev6 語境重寫至豁免後 <5%、base-web commit＋pin bump｜觸發＝下次動上列任一檔的可執行碼、或下一支 base-web 註解改寫批
- BL-00072｜test｜`main.rs` boot 鏈之 `config::assert_jwt_secrets_distinct` 呼叫接線零機器守（ADR-00031 只拍板斷言本身、呼叫位置與射程）：函式為 `pub fn`、刪掉呼叫行不觸 dead_code、全測照綠，arc42 05 之 boot 期 fail-loud 敘述只靠讀碼維持；同形另見 `cache::connect` 之 main.rs 單一呼叫點（maint-backlog-pre-004 final holistic review）；候選＝server 側靜態掃描案以 `include_str!` 讀 main.rs、斷言恰一處呼叫且位於七支 getter 之後、建連之前（附合成反例自證、RL-0051）｜觸發＝下次動 `rust-api/server/src/main.rs` 的可執行碼時
- BL-00073｜governance｜`tools/comment-overlap.py` 對 rust-api 註解內之反引號 code span／路徑字面零豁免，maint-backlog-pre-004 A3 為壓低重疊比把註解內碼字面與路徑拆碎（`envelope.rs` 模組頭契約路徑與三支 serde 用法句、`config.rs` 模組頭、`router.rs` routes_block 測試 doc、`main.rs` serve 註解、`tests/contract.rs` 不變式 (a) doc、`handler/system_settings.rs` SettingItem doc）＝全字面 grep 失效、用法例不可直接複製（語意已保全、只傷檢索性；final holistic review）；候選＝工具對 rust-api 註解之反引號 code span 給豁免（同 base-web 三型豁免思路、一正一反自證），豁免落地且重量仍 <5% 後復原拆碎字面｜觸發＝下一支 rust-api 註解改寫批（含任一單元為 RL-0076 回報改寫 rust-api 註解時）
- BL-00074｜governance｜「外層名冊 ⇔ 子庫工作樹」同步面零 commit 期守：①`tools/msg-key-gate.py` 之 `FRONTEND_MSG_CONSUMERS` 須與 base-web src 消費點同批改，而 pre-commit submodule-sync 段觸發式只認兩子庫 gitlink——只 stage 工具本體、base-web 消費點改動未 commit 時，msg-key-gate（讀工作樹）、submodule-sync（不觸發）、GT-02（只比 gitlink）三道皆綠 ②同形：憲法單獨 staged 時 fork-delta 段讀 base-web 工作樹、同樣無 commit 期守（maint-backlog-pre-004 final holistic review）；候選＝觸發式加工具本體／憲法路徑（SEGMENTS 觸發字面與三處鏡像同批改；代價＝該類 commit 被 base-web 半成品擋下）、或明文記為已知態（won't-fix＝立 ADR 或寫入 RUNBOOK §12）｜觸發＝下次動 pre-commit submodule-sync 段、或下次改 `FRONTEND_MSG_CONSUMERS` 名冊時
- BL-00075｜governance｜走查外殘列（被殺測試留下之會話三表列、序列前進、redis 鍵）清理至 seed 態零工具承載：`tools/walkthrough-baseline.py restore` 對非空基準拒跑（安全帶、不翻案），此類殘列產生時通常無空基準 snapshot 可用（原 BL-00053 條文所列用例、maint-backlog-pre-004 收單時移交本條；final holistic review）；手上有取於空基準之較早 snapshot 檔時 restore 仍可清（拒跑訊息與 RUNBOOK §9c 已附此補救）；候選＝restore 增以凍結 seed 為基準之模式、或 RUNBOOK §9c 寫明手動清理段｜觸發＝下次出現走查外殘列、或下次動 restore 面之可執行碼時
- BL-00076｜governance｜`tools/comment-overlap.py` 之 rev5 對應檔只認同相對路徑：承 rev5-* 改名而來之 base-web rev6-* 新檔（`src/service/api/rev6-auth.ts`／`rev6-settings.ts`、`src/typings/api/rev6-auth.d.ts`／`rev6-settings.d.ts`）恆報 n/a、從未量測——maint-backlog-pre-004 A2b「全 n/a rc 2 與 RL-0076 相容（承 rev5 之檔必有對應檔）」之前提被憲法強制之改名形推翻：單獨量＝rc 2、與他檔混量＝靜默 n/a（final holistic review）；候選＝對應檔映射納 `rev6-`↔`rev5-` 檔名前綴、或允許以參數指名對應檔（RL-0076 回報方式同批改）｜觸發＝下一支 RL-0076 回報涵蓋 base-web rev6-* 檔的單元派發前
