<!-- next: BL-00101 -->
# BACKLOG — 待辦

條目形 `- BL-NNNNN｜<product／governance>｜<一句話>｜<觸發條件（必填、須可到期）>`；配號取檔頭 next 後 bump、號碼永不回收；完成即刪列、git 即史（RL-0050）。
滯後項另居 `BACKLOG-DEFERRED.md`（user 拍板暫不排程；配號計入 GT-05 家族、STATE 分開計數）——查待辦全帳須兩卷併看。開放條數不設上限（觀測值、只報表；RL-0052／ADR-00011）；觸發條件寫「何時該做」、ADR 翻案觸發器寫「決定何時失效」，兩者不混。
觸發條件以「動 X」為條件者（「下次動 X 時」「下一支／首個動 X 的單元或刀」等各形），「動」＝改到 X 的可執行碼（X 本身是測試設施時含其測試碼）；純註解／doc 改寫、或只在 X 所在檔的測試模組增案而 X 本體未改，皆不構成到期。

- BL-00002｜governance｜GT-10 對 `docs/arc42/09-architecture-decisions.md` 的兩腿在首個 AI-ADR 落地（「目前無」句移除）後互斥：子項名冊腿要 `rad_ai_map` 鍵 ⊇ E5 七欄、第八腿要值＝同檔 `###`，而 E5 子項住 ADR body 的 `####`→ E5 改由 ADR 檔面守或豁免 §9 兩腿（工具改動、一正一反自證）｜觸發：首個 AI-ADR 開寫前
- BL-00027｜product｜讀端 wire 集合≠registry 鍵集：`check_type_consistency` 第③臂讓宣告集外之列（setting_type 在認識集）照常上 wire——沿 rev5 讀端只驗認識集、spec FR-009 射程外（`rust-api/server/src/validation.rs` 已自陳）；若要求「registry 集合＝wire 集合」須另立拍板｜觸發＝設定頁 view 刀進場、或 registry 鍵集首次變動時
- BL-00028｜governance｜002 刀 U3 兩份手工轉錄雙表零機器互鎖：handler `SEED_EXPECTED`↔`migration` m0002 `SEED_SYSTEM_SETTINGS`、`validation.rs` `REGISTRY`↔data-model §3——測試側只擋日後單邊漂移、擋不住初次轉錄同錯（U3 審查以 python 解析字面機器比對過一次＝全等）；改以自真源字面解析驅動期望表或加跨檔對賬測（★2026-09-14 體檢收窄：SEED_EXPECTED↔m0002 與 REGISTRY `setting_type` 欄已由真庫案 `assert_sixteen_keys_cell_equal_to_seed` 與讀端 `check_type_consistency` 間接互鎖，真正零互鎖＝REGISTRY `range` 界值↔data-model §3 min／max 兩欄；★2026-09-15 spec-compliance-002 探針附帶觀察併入：seed 值是否落在 REGISTRY `range` 界內亦零機器守——`validation::validate` 只在寫端 handler 呼叫，讀端與 `auth/jwt.rs` 等消費側只解析不驗界（節流兩維的設定載入自 004 刀 U9 起以 `validation::range_of` 驗界、越界整組退常數，不在此列），新 migration 寫入越界 seed 值無任何測試或閘會紅（現行 10 個 number 鍵 seed 值經解析皆在界內）；原觸發「m0002 seed 或 data-model §3 變動」因 m0002 受 ADR-00009 凍結、§3 屬 specs/002 史料面而近乎不可到期，改為下列）｜觸發＝首次新增或改動設定鍵（任一 migration）時
- BL-00031｜product｜settings registry 跨鍵不變式零守：`password_min_length ≤ password_max_length`、`login_throttle_captcha_after ≤ login_throttle_max_fails`、`ip_captcha_after ≤ ip_max_fails` 等成對鍵各自獨立驗證、可寫成互斥值（002 刀 validation 只驗單鍵型別與界）｜觸發＝`password_*` 一對之消費側進場＝007 user-password-admin（消費側決定不變式方向與拒因；兩對節流鍵 `login_throttle_*`／`ip_*` 已分別由 003 auth-session 之 `throttle::load_settings` 與 004 刀 U9 之 `throttle::load_ip_settings` 整組退常數消費＝矛盾值不採用、registry 側這兩對不變式仍零守——★寫端不守屬拍板、非漏守：003 auth-session clarify Q3「002 寫端零改動、矛盾組合由消費側整組退」；004 ip-trust-anchor brainstorm Q4 對兩對節流鍵同判寫端不守、消費側承載＝ADR-00038 決定 4）
- BL-00035｜governance｜事件帳守衛群（000-r2 L1-06／L1-07／P3-P1）：①`summary`／`reason` 與 `notes` 同樣全文進 MILESTONES／STATE，卻無 `notes_gt06_risks` 那道真源側守衛，且不在 `ERRATUM_FIELDS`＝寫壞無補救 ②events.jsonl 自稱 append 型單一事實源，但無任何閘比對 HEAD 版、既有列可被靜默改寫（erratum 機制的整個前提在機器面零守；形制照 GT-04 的 HEAD 對比腿）③000-r1 review 事件 notes 的分流計數自相矛盾（稱 confirmed 86、列出項相加不符），事件 append-only 且 notes 不可更正、只能於報告記 ④單筆事件驗證失敗時，錯誤應以該筆 ERROR 指名、下游閘（GT-03 在途判讀等）不得對無效事件續作判讀（003 刀簿記實證：feature_close `summary` 超 300 字使整筆事件無效，GT-03 隨之假報在途 27 筆＋`test_real_repo_appendix_matches_events` 紅——GT-02 雖擋、徵狀卻先落在下游閘）——四者皆屬既有 GT-02／GT-03 補腿、不占閘數｜觸發：下次動 events schema 或 GT-02 腿時
- BL-00036｜governance｜閘↔規則對賬與 RL 承載群（000-r2 L3-04／L3-03／L2-04）：①閘 docstring 的 `rule=` 欄與 RULES `carrier` 欄零機器對賬——carrier=lint 四條無任何閘指名（★GT-03 指名 RL-0053 而其 carrier 實為 checklist 之實例已於 000-r2 D 修單改為 `rule=RL-0055`、本項只剩「無機器對賬」本身） ②RL-0051「每條閘一正一反自證、變異打在判準上」carrier=lint 卻無任何閘或測試實作，12 閘的正反測試存在性只靠人工枚舉 ③RL-0015 標 carrier=lint 但 GT-06 之 `TENSE_WARN` 只掃其同義集五詞中的三詞且僅 WARN，「隨…刀進場／尚無…」兩形零覆蓋、活書既有命中而 lint 全綠（現值以實掃為準；★其中 frontmatter 之 `隨刀：<…>` 屬 ADR-00006 決定 1 之值形——擴詞升 ERROR 須先定該值形豁免或另立 ADR，其餘命中同批改對）｜觸發：000-r3 之前，或任一條 carrier 欄改動時
- BL-00039｜governance｜探針 hops 口徑未入骨架指示（000-r2 P5-P1；★原①編排骨架 `_vars`／`_context`／`_allowed` 五常數零斷言已於 maint-backlog-40-39-37 收：`_sk_head.js` 三常數兩形共用、`_sk_main.js` 兩常數〔拼接序使 head 取值落在 const 暫時死區〕、harness-test 十五→十八案）：探針 hops 口徑未明示「`ls`／`find`／`grep`／`git log` 等存在性反證不計入」，使應查無題的自報跳數系統性膨脹（000-r2 之 P5：自報口徑 1/7 vs 開檔口徑 6/7）；grader 側口徑（增益跳不計為繞路）亦只住 000-r1 報告 note 欄、未入骨架指示｜觸發：000-r3 發射前（user 2026-09-14 裁定留待此時；動 `tools/orchestration/**` 不提前到期）
- BL-00042｜governance｜跨刀活體契約仍住 spec 目錄（000-r2 C1-3；兩鏡 real 確認／decided 不確定、主線裁定轉 BL）：RUNBOOK §10 之 Day-1 三步與演進登記檔形制契約仍引 `specs/001-schema-baseline/contracts/{gates,schema-evolution}.md`，與 ADR-00012 為同型缺陷所立的「跨刀活體不住 spec 目錄」原則相牴觸；語意張力＝ADR-00010 決定 4／5 明文指派 contracts/ 為契約權威，ADR-00012 的原則未擴用亦未具名豁免 contracts/。抽取決策本身屬拍板級、須立 ADR（provenance 引 ADR-00012 同型）；★射程另含 004 刀落的同型兩份（皆現在式活體面、抽取時一併處置）：`specs/004-ip-trust-anchor/contracts/code-gates.md` 為 RUNBOOK §12 碼面閘表 `tools/view-render-guard.py` 一列之閘契約權威（004 刀 U8）、`specs/004-ip-trust-anchor/contracts/trust-model-config.md` 為 RUNBOOK §16（六集合語意／載入失敗語意／反向代理標頭契約／prod 樣例）與活書 §3 介面表（欄位語意）之權威（004 刀 U11）｜觸發：下一支帶 migration 的刀開寫前
- BL-00043｜product｜存取稽核 `sys_access_log` 寫入面零承載（002 刀 Out of Scope；003 開刀前承載體檢）：該表自 002 延後（該刀 spec 明文零寫入）後，接手刀與觸發在現在式帳本只由本條承載——憲法 §I.7 承襲指針表島 J 只涵蓋 retention 與 reporting、不含寫入面；表結構已隨 001 基線在庫（同批延後的操作稽核 `sys_operation_log` 已由 004 刀 U7／U10 首寫＝IP 規則四寫端＋解鎖端點，系統設定寫端維持不落列＝ADR-00040 款 3；前代的存取軌跡 middleware 依 004 刀 research 防回歸清單不帶）｜觸發＝008 audit-settings-pages 刀進場、或任一刀首次需要寫 access log 時
- BL-00044｜product｜設定清單列表排序零承載（002 刀 Out of Scope；003 開刀前承載體檢）：16 鍵讀端一次回全集、無排序參數；rev5 以 `rev5:B-027` 承載該延後項，rev6 未帶進任何現在式面（BACKLOG 兩卷／NOTES／RUNBOOK／ADR 全集零命中，ADR 內「排序」命中皆非本義）｜觸發＝設定頁 view 刀（008 audit-settings-pages）進場，由其消費側決定要不要排序與排序欄白名單
- BL-00045｜product｜三支管理頁 view 於 base-web 尚缺、選單點擊 404（002 刀 Out of Scope；rev5 由 `rev5:B-008` 承載）：`manage_system-settings`／`manage_policy-archive`／`manage_audit` 三列選單之 `component` 指向的 view 在 upstream `example` 基線不存在，選單與 casbin 政策已隨 001 基線先行（同批第四支 `manage_ip-rule` 已由 004 刀 U8 進場＝view 三檔＋`route`／`page` 兩樹譯文、首開憲法 §III.2 ★BASE-WEB-MANAGE-PAGE-WIRING 軌道）；該已知態原僅由 `docs/ops/reference-src/schema-definition.md` §8 一句承載、且該句自我延後（「條目隨 UI 刀立」），開刀前掃 BACKLOG 觸發欄結構上掃不到；★另一面（003 auth-session U11 CDP 對照走查實證）：側邊欄**展開態**該三列選單標題於 rev6 顯 i18n 裸鍵（`route.manage_*`）、rev5 顯簡中——三鍵之 `route` 樹譯文由頁進場刀以憲法 §III.2 頁進場型 inline 與 view 同批帶入；收合態零差異｜觸發（按頁拆；各頁進場皆經憲法 §III.2 管理頁 ★ 軌道授權）＝`manage_policy-archive`：006 authz-governance 刀進場／`manage_system-settings`・`manage_audit`：008 audit-settings-pages 刀進場（各刀範圍由其 brainstorm 定、可翻）
- BL-00047｜governance｜schema 閘表數斷言之改對義務失去指派對象（003 開刀前承載體檢）：`tools/schema-gate.py` 的 `len(tables) != 14` 硬編碼，ADR-00012 決定 6 指派「隨 002 首次加表時由該刀改對」，而 002 已零加表收刀；現在式面（RUNBOOK §10 加表清單／NOTES）零承載、只由本條承載，義務懸空（改對射程＝該常數＋同檔一切依賴 14 之釘值——真 repo 兩案 `test_real_data_model_parses`／`test_real_repo_green_rc0`、`test_green_path_sql_dispatch_summary_rc0` 之摘要字面「14 親排表逐位全等」、`TestDataModelParse`／`TestDocCheck` 以 `bcdefghijklmn` 合成 14 表之 fixture（常數一改即先在解析自檢拋 GateError）——與 docstring「14 親排表」（實兩處：模組 docstring 與 gate2 函式 docstring），以同檔現況現算為準；★同批另兩面：`docs/ops/reference-src/schema-definition.md` §2 標題之宣告數（ADR-00012 決定 6 原形＝「§2 宣告數 vs 解析數」，候選＝閘改自該標題解析宣告數、不再硬編碼常數）、entity-drift 語境之「14 表」字面（活書 05、`rust-api/entity/src/lib.rs` 模組 doc；依 RL-0011 枚舉）；ADR-00012 決定 6 明言零 delta 時不預先改）｜觸發＝首支帶 create table 之 delta migration 的刀開寫前
- BL-00048｜governance｜ADR-00014 後果段對 no-escalation 本體形的預告與前代 as-built 不符（003 開刀前承載體檢；user 拍板 2026-09-07：走 BL 對沖、不翻案）：該 ADR 後果段寫「本體刀只填掛點函式體、零簽章變更、零呼叫端改動」，而前代 as-built 之真本體＝`rev5:server/src/auth/no_escalation.rs` 的 body 級純函式（規則＝`T ⊆ A ∧ N ⊆ A`）、落 `rev5:007-user-password-admin` U1（`rev5:006-authz-governance` spec 明載該掛點不填）、生產呼叫 9 處分布 2 檔（`guard_no_escalation(`：user.rs 8／throttle.rs 1；提及 no_escalation 之檔 10）；照該預告估工會把跨檔工作估成填一個函式體。ADR body 不可變（GT-04），本條即其對沖；同一預告另住 rev6 `rust-api/server/src/auth/enforce.rs` 模組註與 `no_escalation_check` doc、specs/002 FR-013（兌現時連動面）｜觸發＝no-escalation 本體刀 brainstorm 起手前（006 authz-governance 或 007 user-password-admin 先到者）
- BL-00058｜governance｜憲法 §III.2 ★BASE-WEB-I18N-WIRING (iii) 範圍欄「`src/typings/app.d.ts`（1 處，修改型）」與 as-built 相反：純插入 `backend` 必填型節、無 upstream 既有行被動、`原行:` 無可填，落地形＝新增型 START／END 圈界（`fork-delta-lint.py` 19 處修改型不含該檔；活書 §8.4 已記新增型、§5 只留指向 §8.4 的指針）；範圍欄之「修改型」為全表通用粗標籤（(b)／(c)／LOGIN-CAPTCHA(i) auth store／LOGOUT-UX(i) 四處同形皆兼含新增型圈界、名冊斷言射程僅修改型）、(iii) 之特殊處＝零修改型；ADR-00026:49 條文全文同字（accepted body 不可變＝GT-04）、`specs/003-auth-session/spec.md` fork-delta 風險表亦標修改型；碼面無誤、無閘會紅、只能人工落帳｜★user 2026-09-14 拍板＝留帳：觸發＝下一次 §III.2 Amendment 同批修正（PATCH 級、§V.2、新 ADR、ADR-00026 body 不動；射程＝只改 (iii) 一欄或一併精確化五欄、屆時再定）
- BL-00062｜contract｜`POST /auth/logout` 之 5000 出口（`RequestContext::from_headers` Err、`internal()` 之 DbErr／commit 失敗）未進 `contracts/wire-auth.md` 情境表（三列皆 0000、與 login／refresh 兩表列 5000 之體例不一），且 DB 故障窗內「有效票→5000／垃圾票→0000」構成弱 token 有效性 oracle（FR-010 立論僅降級期成立）；候選＝列入契約表（承載處：`specs/003-auth-session/contracts/` 屬史料面、活體契約放哪與 BL-00042 同題；oracle 留為已知態＝won't-fix 須立 ADR）、或把 DbErr 折成 0000 no-op（wire 可見）；oracle 射程＝驗章通過後之 DB 腿（`from_headers` Err 發生在驗章前、不構成 oracle）｜觸發＝下次動 `rust-api/server/src/handler/auth/logout.rs` 之 handler 可執行碼時（004 若把 source_ip 改取信任錨即到期）
- BL-00064｜governance｜base-web 第二 HTTP 客戶端 `src/service-alova/request/**` 未接 I18N-WIRING(i)（錯誤文字顯裸 msg key）且自帶獨立 refresh 路徑（不共用 axios 側 `refreshTokenPromise` 單飛、直寫 localStg 憑證對），與島 A rotation／reuse 偵測潛在衝突；003 全刀零承載；現況 dev 僅 `/auth/error` demo（恆 4040）走真後端故不可達｜觸發＝首個動 demo 頁或 alova 路徑的刀（可與收刀既定的 `/auth/error` 排程錨同批）
- BL-00065｜security｜會話面零帳號活性閘（島 I 增量、007 域；rev5:007 同位五腿未搬）：refresh 無鎖內活性重驗、`enforce_mw` 與 getUserInfo 亦不查 `status`／`deleted_at` ⇒ 停用或軟刪帳號之既有會話不被撤且可無限續發新對；現只住 `refresh.rs` 模組 doc「★三件不搬」、`user_info.rs` handler doc（刻意不看 `status`／`deleted_at`、要先立 ADR）與本條；★rev5 島 I2 明載「MUST NOT 為此新增每請求活性判定」、`rev5:007` 只加 refresh 鎖內重驗——rev6 若要在 `enforce_mw`／getUserInfo 加判，方向與 rev5 相反、須另拍板｜觸發＝007 user-password-admin（首個提供停用／軟刪寫端者）brainstorm 起手前納入範圍
- BL-00066｜product｜L1 lock 一經武裝即以武裝當下的窗值固定 TTL、命中不重讀 L2（`throttle/mod.rs` ① 早退、③ 一次算定），三門檻鍵放寬（調高 `max_fails`／調短 `window`）在鎖存續期間至多 min(窗,900) 秒後才生效＝有界已知態（data-model §7「立即」為規劃期措辭）；另設定不變時 L1 亦可比 L2 多擋至多近一個窗長（TTL 自武裝起算、最早計入之失敗列出窗時 L2 已解鎖；spec-compliance-003 L2-1）；★spec-compliance-003 L2-2：L1 命中早退不讀三門檻鍵現值，同時抵觸憲法 §I.7 跨島總則「節流三鍵於每次登入嘗試讀現值」（ADR-00026 入憲、非僅 data-model §7 措辭）——user 2026-09-15 拍板＝留帳、至觸發時二擇一收斂：改碼使 L1 命中後仍以 L2＋設定現值複核（候選①，兩面同消；候選②以 lock 值寫入時戳重算 TTL 只消放寬一面），或走 §V.2 Amendment 把 L1 鎖存續期例外寫入跨島總則（ADR-00026 後果段：跨島總則受 MAJOR 閘保護）｜觸發＝004 ip-trust-anchor brainstorm 起手前（該刀重整節流 precheck、加 IP 維），或下次動節流三區或設定鍵熱更新面時（先到者）
- BL-00067｜product｜`/auth/error` demo 端點排程錨：base-web upstream 兩張 demo 頁經 `fetchCustomBackendError` 打 `GET /auth/error`（003 ROUTES 16 條不含、現恆 4040）；延後理由＝拍板級衝突（回吐 client 任意 code／msg 撞保留碼 `9999` 三錨與憲法 §I.3 信封不變式）＝憲法 §I.1「v1 從簡只能是交付排程」（003 spec Out of Scope）｜觸發＝首個動 demo 頁或決定 demo 頁去留的刀（與 BL-00064 service-alova 同批）
- BL-00070｜governance｜src 測試模組仍有六支自持「oneshot＋解封包」殼，與 test_kit `oneshot_json`（body 上限／JSON 解析／panic 措辭的單一份）並存：`handler/route.rs` `integration_tests::get_json`（須帶 query、bearer 可選＝組好 req 後可委派）、`auth/enforce.rs` `probe`、`router.rs` `send`、`handler/system_settings.rs` `post_probe`／`post_update_bytes`／`probe_endpoint`（system_settings 寫端助手於 maint-backlog-pre-004 A1 裁定不在 BL-00052 射程）；另 `obs.rs` metrics fallback 案有第七處自持 oneshot、`router.rs` `send` 之解封包半在同模組 `envelope_json`（★更正：`envelope_json` 實為 JSON 信封解析、可委派 `oneshot_json`；真正的具名留存候選＝`obs.rs` 案〔不讀 body、只驗 recorder render〕與 `send`〔通用送出殼、回包形各案自判〕；`obs.rs` 該測試模組屬本條觸發之「上列任一測試模組」）——同形邏輯各持一份＝改一處漏一處（maint-backlog-pre-004 A1 碼品質審查升級）；候選＝逐支核同形者改委派 `oneshot_json`、非 JSON 信封形者具名留存並於 test_kit 該 doc 列明｜觸發＝下次動上列任一測試模組的可執行碼時
- BL-00072｜test｜`main.rs` boot 鏈之 `config::assert_jwt_secrets_distinct` 呼叫接線零機器守（ADR-00031 只拍板斷言本身、呼叫位置與射程）：函式為 `pub fn`、刪掉呼叫行不觸 dead_code、全測照綠，arc42 05 之 boot 期 fail-loud 敘述只靠讀碼維持；同形另見 `cache::connect` 之 main.rs 單一呼叫點（maint-backlog-pre-004 final holistic review）；候選＝server 側靜態掃描案以 `include_str!` 讀 main.rs、斷言恰一處呼叫且位於七支 getter 之後、建連之前（附合成反例自證、RL-0051）｜觸發＝下次動 `rust-api/server/src/main.rs` 的可執行碼時
- BL-00074｜governance｜「外層名冊 ⇔ 子庫工作樹」同步面零 commit 期守：①`tools/msg-key-gate.py` 之 `FRONTEND_MSG_CONSUMERS` 須與 base-web src 消費點同批改，而 pre-commit submodule-sync 段觸發式只認兩子庫 gitlink——只 stage 工具本體、base-web 消費點改動未 commit 時，msg-key-gate（讀工作樹）、submodule-sync（不觸發）、GT-02（只比 gitlink）三道皆綠 ②同形：憲法單獨 staged 時 fork-delta 段讀 base-web 工作樹、同樣無 commit 期守（maint-backlog-pre-004 final holistic review）；候選＝觸發式加工具本體／憲法路徑（SEGMENTS 觸發字面與三處鏡像同批改；代價＝該類 commit 被 base-web 半成品擋下）、或明文記為已知態（won't-fix＝立 ADR 或寫入 RUNBOOK §12）｜觸發＝下次動 pre-commit submodule-sync 段、或下次改 `FRONTEND_MSG_CONSUMERS` 名冊時
- BL-00075｜governance｜走查外殘列（被殺測試留下之會話三表列、序列前進、redis 鍵）清理至 seed 態零工具承載：`tools/walkthrough-baseline.py restore` 對非空基準拒跑（安全帶、不翻案），此類殘列產生時通常無空基準 snapshot 可用（原 BL-00053 條文所列用例、maint-backlog-pre-004 收單時移交本條；final holistic review）；手上有取於空基準之較早 snapshot 檔時 restore 仍可清（拒跑訊息與 RUNBOOK §9c 已附此補救）；候選＝restore 增以凍結 seed 為基準之模式、或 RUNBOOK §9c 寫明手動清理段｜觸發＝004 ip-trust-anchor 擴 restore 清理面時（該刀 brainstorm 定案：還原面擴及 `sys_ip_rule`／`sys_operation_log` 並加以凍結 seed 為基準之模式），或下次出現走查外殘列、或下次動 restore 面之可執行碼時（先到者）
- BL-00077｜product｜base-web request 層只在 HTTP 200 業務碼分支經 `translateBackendMsg` 轉譯後端 msg：4040（HTTP 404）與 5003（HTTP 403）信封走 axios 原生錯誤攔截、toast 顯示「Request failed with status code 404」類原文，rev5 以 `rev5:B-117` 之 HTTP 層轉譯塊承載、rev6 003 刀未帶入且無承載（spec-compliance-003 L4-1）；現況可達面＝R_SUPER 可見之 `views/function/request` demo 頁經 `fetchCustomBackendError` 打 `/auth/error` 恆 4040；候選＝`src/service/request/index.ts` onError 對非 `BACKEND_ERROR_CODE` 但帶信封 msg 者同樣轉譯（憲法 §III.2 ★BASE-WEB-I18N-WIRING(i) 授權檔內新增型圈界；是否屬用途補完依 §III.2 判準）、CDP 對照 22080 驗收｜觸發＝首個 UI 會碰到 HTTP 403／404 信封的刀（006 authz-governance 或 008 audit-settings-pages 先到者），或 BL-00067 決定 demo 頁去留時
- BL-00078｜governance｜`deploy/grafana-provisioning/alerting/rules.yml` ② `obs016-throttle-suppressed` 在 rev6 零發射點：註解事件錨指向 `throttle/mod.rs` 之 `warn!(…suppressed=N…)`、窗長依據引 `suppressed_breadcrumb`，而 003 刀依 `rev5:R3-3` 判不做（research 防回歸清單 B、spec Out of Scope）、rust-api src 對 `suppressed` 零命中＝規則結構上永不觸發、註解指針指錯（創世拷入；spec-compliance-003 P1-P3）；候選＝004 決定帶入 suppressed 事件或刪除該規則（刪除為預設候選），同批以 `python3 tools/docsync errata suppressed_breadcrumb` 枚舉處置｜觸發＝004 ip-trust-anchor brainstorm 起手前
- BL-00079｜governance｜Workflow 看門狗免重掛形：Monitor 工具單次上限 30 分鐘（harness 硬上限）而 TDD 單元 run 為 2～3 小時⇒每支 run 重掛約 6 次、每次一回合主線；004 刀 U1 後已收「重掛靜默（`--rearm`）＋冒煙掃前數行＋run 結束自動 DONE」三項、每 30 分鐘一次重掛仍在。徹底形＝Monitor 只掛前 30 分鐘（冒煙＋早期故障）、長尾改 Bash 背景任務版看門狗（2026-09-19 探針實證背景任務存活逾 30 分鐘且完成有通知）；代價＝CLAUDE.md §2「launch 與 Monitor 原子成對」字面與 PostToolUse(Workflow) hook 提醒文字須改、長尾模式之 RUNAWAY 須由「告警不退出」改「告警即退出由主線判後重掛」（背景任務只有退出才通知）→ 屬流程規則變更、待 user 裁定｜user 裁定去留時；最遲 004 刀收刀之 final holistic review 前收斂（不做即刪列）
- BL-00080｜governance｜`tools/comment-overlap.py` 註解解析只認 `//`／`///`／`//!`／`/*`／`<!--` 形、不解析 Python／shell 之 `#` 行註解與 docstring：對 `.py`／`.sh` 檔回「比對面為空」rc 2＝RL-0076 量尺對承 rev5 之 python 隨遷工具量不到逐字重疊（004 刀 U3 `tools/walkthrough-baseline.py` 三改、U8 兩支新碼面閘皆此形；U8 以同判準 40 字元之替代量測入單元報告）；處置＝補 `#` 形與 docstring 解析＋自測（原同題另一條 BL-00087 已併入本條、該號不回收）｜觸發＝下次以 RL-0076 量測任一 `.py`／`.sh` 檔時，或下次動 `tools/comment-overlap.py` 之可執行碼時（先到者）
- BL-00081｜product｜`throttle::integration_tests::sliding_window_resets_on_success` 偶發紅（004 刀 U4 實測：同一最終態四次全量紅一次、單跑 8/8 綠；「成功後第 2 敗」期望 2222 實得 1000＝計數少 1）：未驗證假說＝WSL2 牆鐘回撥使第 1 敗列 `created_at` 不晚於成功列、不入 reset-on-success 下界；連帶＝該案 panic 路徑不清 redis 會話鍵（案末 `purge_session_keys` 非 RAII）→留 `session:<sid>:last_activity` 孤兒鍵、walkthrough diff rc 1｜觸發＝004 刀 U9 重寫 `precheck` 與其測試時（一併把該案清理改 RAII、時序斷言改不依賴牆鐘單調），或該案再紅一次（先到者）
- BL-00082｜product｜rev5 boot 有「訪客位址標頭名可用性體檢」（`unusable_header_name`：信任模型 `connecting_ip_header` 若非合法 HTTP 標頭名，`HeaderMap::get` 恆回缺席＝通道回退／邊緣驗證兩覆蓋層靜默失效、零告警），rev6 之 004 刀 spec／tasks 無承載、U4 未帶入（implementer-2 升級）；quickstart §0 之 grep 樣式尚含 `connecting_ip_header`＝rev5 形殘留｜觸發＝下一支修改 `config::load_trust_model` 或信任模型契約之刀或維護批；或首次 prod 部署前，先到者（004 刀 final holistic review 裁定＝本刀不帶入載入面體檢、人工核對義務已載 RUNBOOK §16.2）
- BL-00083｜governance｜`ipgate` 門鈴測試之隱性次序相依：四支案各 spawn 一支永不結束的 watcher 背景任務，其後諸案對 `doorbell_subscribe` 計數的**精確等值**斷言靠 libtest 名稱字典序恰好排在遺留點之前才成立（004 刀 U4 碼品質審查 notes；30/30 實跑綠）；U5～U7 新增 `doorbell_*` 精確等值斷言即可能踩到｜觸發＝下次於 `ipgate/mod.rs` 測試模組新增或改名任一計數斷言案時（改不等式或給 watcher 可停機之測試句柄）
- BL-00084｜product｜IP 存取閘每次阻擋皆發一則結構化 warn、無節流無抑制：被擋來源可用請求速率直接驅動日誌寫入量（`middleware::ip_gate_mw` fn doc 已記此代價；004 刀 U6 碼品質審查 notes）。要壓＝改 004 刀 spec FR-024「每次阻擋 MUST 可見」之粒度或替 `ipgate_blocked_total` 補 label，屬拍板級｜觸發＝可觀測性整刀開刀時、或 dev／prod 日誌量因阻擋告警出現實際問題時（先到者）
- BL-00085｜governance｜IP 存取閘測試面兩處無回歸錨（004 刀 U6 碼品質審查 notes）：①`obs::ipgate_blocked` doc 稱「發射點恰一處」而無機器守（同形前例＝`obs.rs` 之發射字面靜態掃描）②`chain_rejected` 來源仍以判定腿 `real_ip` 過閘（ADR-00037 決定 1）只有 fn doc 敘述、各案上下文一律 `proxy_clean`——補一發 `Confidence::ChainRejected` 之被擋案｜觸發＝下一支動 `middleware/mod.rs` 或 `obs.rs` 可執行碼的單元或刀
- BL-00086｜product｜分頁回應形 `PageRes<T>` 暫寄居 `rust-api/server/src/handler/ip_rule.rs`（004 刀 U7：rev6 首個分頁端點、形承前代「恰一消費者時寄居、第二消費者時上移」之時序）；第二個分頁端點落地時整段上移 `envelope.rs`、欄形與 serde 屬性逐字不動，並同批改寫 `envelope.rs` 檔頭與 `handler::ip_rule` 兩處互指句｜觸發＝第二個分頁端點落地之刀（角色／選單／使用者管理任一清單端點）、或下次動 `envelope.rs` 信封型可執行碼時（先到者）
- BL-00088｜governance｜`tools/route-artifact-gate.py` 兩處微形（004 刀 U8 碼品質審查留帳、不影響判定正確性）：①`run_check` 以 `notes` 是否為空推導綠訊息之「②／②③」字樣、與 notes 目前唯一來源（基線跳過）隱性耦合，日後新增任何 note 即誤述 ②容器於 OBSERVE 段後、REGEN 段前在 docker 層失敗時，容器內 `/tmp/route-artifact-gate.*` 沙盒不會被清｜觸發＝該工具下次被修改之刀或維護批；或沙盒殘留實際被觀測到時
- BL-00089｜governance｜`rust-api/server/tests/common/mod.rs` 測試清理面兩項（004 刀 U10 碼品質審查留帳）：①三支守衛（`LoginAttemptGuard`／`IpRuleWriteGuard`／`UnlockWriteGuard`）各持一份同形 Drop 還原執行殼且細節已分岔——收攏為一支共用殼、三支 Drop 全部委派、doc 與 src 側 `test_kit::run_restore_stmts` 互指 ②`tests/contract.rs` 之 `observed_msgs_real_db` 把會話鍵縮 TTL 排在帶斷言段之後、中途 panic 即在 redis 留 `session:<sid>:last_activity` 鍵（TTL 3900 秒）致走查基準比對報差——兩個 sid 改掛 RAII 清鍵守衛｜觸發＝下一個動 `tests/common` 守衛或 `observed_msgs_real_db` 之刀或維護批；或該殘鍵再被觀測到一次，先到者
- BL-00090｜product｜handler 就地共用件之收攏時點（004 刀 U10 留帳）：`handler::ip_rule` 與 `handler::throttle` 各持同形一份之 body 收斂（`Json` rejection→預設值再交畸形守門）與操作者組裝 `operator_from`（上下文缺席→`5000`）＝第二份、依前代收攏時序（第三份出現時）未達收攏點；測試側 `table_state`／`audit_table_state` 同族兩份刻意不合併（窗界謂詞不同、合併後配置多於邏輯）。第三個帶操作稽核之寫端 handler 進場時，前兩件收攏為 `handler/common.rs`（前代終態形）、測試側同族件同批重評｜觸發＝第三個需操作者上下文與 body 收斂之寫端 handler 落地之刀
- BL-00091｜product｜管理員解鎖無 UI 按鈕與前端包裝：`POST /systemManage/unlockLogin` 為 API-only（ADR-00040 款 1）——base-web 無對應 service wrapper 與按鈕；憲法 §III.2 ★BASE-WEB-MANAGE-PAGE-WIRING (i) 列明文「解鎖按鈕與其包裝不在本次授權」。按鈕的自然位置＝使用者管理頁（按鈕權限碼 `user:unlock` 已在 seed、零 seed 變更；來源維解鎖的入口位置由該刀 brainstorm 定）；落地＝wrapper 走 §III.1 BASE-WEB-WRAPPER 新檔、頁面接線走 §V.2 Amendment 取得授權；帳號維解鎖的 no-escalation 守門另由 BL-00048 承載｜觸發＝使用者管理頁刀（007 user-password-admin）brainstorm 起手前納入範圍
- BL-00092｜governance｜全域 wire i64 守衛 lint 缺席（`rev5:B-111` 同形）：wire 型的 `i64`／`Option<i64>` 欄須逐欄走憲法 §I.3 的合法路之一——number 形掛 `rust-api/server/src/envelope.rs` 的 2^53 守衛 serializer（`serialize_i64_number_guarded`／`serialize_opt_i64_number_guarded`）、或 string 形（`serialize_i64_as_string`）——或明示不序列化，而「有沒有掛上」零機器守——拆掉 `serialize_with` 後 serde 仍發 JSON number、`tools/wire-schema.py` 快照逐位相同、裁判案照綠（快照對守衛屬性結構性無感）；現況靠逐型案個別持有（如 `handler/ip_rule.rs` 之 `record_id_beyond_2p53_fails_loud`）。候選＝`rust-api/server/tests/` 新增全樹靜態掃描 lint：每個 `derive(Serialize)` 型之 i64 欄須掛守衛或具名豁免、附合成反例自證（前代形＝`rev5:server/tests/wire_i64_guard_lint.rs`、重打字不拷貝）；ADR-00040 款 6（wire 裁判面不開嚴格模式）所指的後續工作條目即本條——專項開工時同批重評 `additionalProperties` 全域開啟｜觸發＝下一支新增帶 i64 id 欄 wire 型的刀開寫前（使用者／角色／選單管理任一清單端點），或全域 wire 守衛專項開工時（先到者）
- BL-00093｜governance｜`deploy/grafana-provisioning/alerting/rules.yml` 對 rev6 as-built 之逐條稽核與活體驗證（004 刀 U11 留帳）：①③d `obs016-accesslog-write-fail` 在 rev6 無事件源（存取軌跡中介層未進場、已於該規則註解據實標示）——存取軌跡刀進場時重錨或刪 ②其餘承母版之規則（reaper／retention 等）未逐條對 rev6 碼核過事件錨 ③`obs016-ipgate-degraded` 之新 LogQL（`target=~` 兩 target 形）未於活體 loki 跑過（rev6 stack 未起觀測 profile）｜觸發＝rev6 觀測 profile 首次起動之刀或維護批；或存取軌跡刀，先到者
- BL-00094｜governance｜`tools/walkthrough-baseline.py restore`（含 `--seed`）以 SQL 直清 `sys_ip_rule` 後不按規則換版門鈴：執行中 rust-api 判定面仍持舊規則集、須人工 `PUBLISH ipgate:invalidate`（現由 RUNBOOK §9c 手動步驟承載；004 刀 T047／T067 走查實際踩到）——工具於清理面含 `sys_ip_rule` 且清前列數非 0 時自動補按並回報訂閱者數、自測補案｜觸發＝下次修改該工具之刀或維護批
- BL-00095｜product｜IP 規則清單端點把庫內 `wbip_type` 任意字面原樣上 wire，而 wire 契約、wire-schema 快照與前端型別皆宣告封閉二值（`allow`／`deny`）：寫端守門使正常路徑只產二值，但直改庫或前代遺留之未知類型列（存取閘讀端對其 skip＋告警）於清單上呈契約外之值、前端標籤映射取不到（004 刀 final holistic review）；處置候選＝清單 DTO 對未知類型給明確表示法並同步契約／快照／前端渲染，或清單端過濾並告警｜觸發＝下一支動 `handler::ip_rule` 清單端點或 IP 規則管理頁之刀；或真出現未知類型列之事件
- BL-00096｜product｜`model/facade/sys_ip_rule` 四寫端與 `find_by_id_for_update` 之「MUST 於交易內呼叫」只由碼註承擔、簽章仍收泛型連線；同刀 `sys_operation_log::write_in_txn` 已示範以具型簽章（`&DatabaseTransaction`）把同一要求做成型別保證（004 刀 final holistic review）；改簽章為機械改動（production 兩呼叫點已傳交易），代價＝該檔測試約三十處以裸連線呼叫者須改為自開交易｜觸發＝下一支動 `sys_ip_rule` facade 之刀或維護批
- BL-00097｜product｜wire 快照已收 `Api.IpRule` 之三支寫端請求型與 `IpRuleListQuery`，但 `tests/wire_schema.rs` 只裁判 `IpRuleRecord` 一支：前端請求形與後端請求 DTO 之間零機器錨（002 刀已立「寫端請求型亦入裁判」之先例；004 刀 final holistic review）｜觸發＝下一支動 IP 規則 wire 形或 `tests/wire_schema.rs` 之刀或維護批
- BL-00098｜governance｜管理員解鎖端點之兩條方向性不變式（操作稽核先於解鎖標記寫入；操作者上下文缺席即拒寫 `5000`）於憲法 §I.7 島 E 無條文——現由 contracts、源序守測試與活書承載、凍結面缺席（004 刀 final holistic review）｜觸發＝下一次憲法 §V.2 Amendment 提案時併入（不單獨為此開 Amendment）
- BL-00099｜governance｜`deploy/grafana-provisioning/dashboards/json/backend-msg-dict.json` 為人維護之 msg 譯文字典面板：與 ADR-00039 決定 2「不另立譯文表」相牴觸、無機器對賬（004 刀六新鍵之 zh-TW 欄即漂移、final holistic review 修單已改抄現值），且含前代遺留而 rev6 名冊無之鍵五十餘列；處置候選＝面板只留鍵名並指向三檔 locale、或改由 locale 生成並入 GENERATED_FILES｜觸發＝rev6 觀測 profile 首次起動之刀或維護批；或下一次新增 msg key 之刀，先到者
- BL-00100｜product｜`biz.ipRule.selfLock` 三語主詞不一致：zh-TW「此變更」、zh-CN「此规则」、en「This rule」；後兩者於軟刪端點指涉錯物（刪的是放行規則、擋住連線的不是「此規則」）。顯示文字變更＝user 可見面、待 user 拍板措辭（004 刀 final holistic review；候選＝zh-CN「此变更」／en「This change」）｜觸發＝004 刀 finishing 停點呈 user 裁定；未裁則下一支動 IP 規則管理頁或 locale backend 子樹之刀
