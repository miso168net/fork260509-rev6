---
section: 5
summary: 建構區塊三層白盒；E2 模型登錄視圖的系統層落點
rad_ai: [E2]
rad_ai_stage: 1
rev5_blueprint:
  §5 Building blocks: 承襲（rust-api workspace 四 crate＝migration／entity／sea-orm-adapter／server；server 管線 as-built 見 §5.2、以 rev5 活書 §5 為藍本重打字＝憲法 §I.5）
---
# §5 建構區塊視圖

## 5.1 整體系統白盒

| 區塊 | 內含 | 介面 |
|---|---|---|
| `base-web/` | soybean-admin fork 全樹（upstream 原樣分層；程式碼 fork-delta＝五支新增型新檔〔typings 2／settings service／auth service／zh-tw 錨點 locale〕、inline 修改型 19 處（`.env` 2／`.env.test` 1／`.env.prod` 1＝BASE-WEB-ADAPT、`src/store/modules/route/index.ts` 1＝BASE-WEB-AUTH-WIRING(a)、`src/views/_builtin/login/modules/{code-login,register,reset-pwd}.vue` 各 1＝BASE-WEB-AUTH-WIRING(b)、`src/hooks/business/captcha.ts` 3＝BASE-WEB-AUTH-WIRING(c)、`src/layouts/modules/global-header/components/user-avatar.vue` 1＝BASE-WEB-LOGOUT-UX-WIRING(i)、`src/store/modules/auth/index.ts` 3 與 `src/views/_builtin/login/modules/pwd-login.vue` 2＝BASE-WEB-LOGIN-CAPTCHA-WIRING(i)、`src/service/request/index.ts` 2＝BASE-WEB-I18N-WIRING(i)；另新增型圈界＝user-avatar 2／auth store 5／pwd-login 4／三表單各 1／captcha.ts 2／request 1／`src/typings/app.d.ts` 1＝BASE-WEB-I18N-WIRING(iii)／`src/locales/langs/{en-us,zh-cn}.ts` 各 1＝BASE-WEB-I18N-WIRING(ii)），軌道與標記定形見 §8.4）；fork patch set 另含檔頭標記的分支來源紀錄檔 `x_fork.branch-origin.md` | 外層 gitlink pin；容器 `base-web`（Vite dev）由 front-nginx 反代 |
| `rust-api/` | Cargo workspace：migration（m0001 結構基線＋m0002 seed 基線、承 rev5 逐位元＝憲法 §I.5 例外②）／entity（15 表 sea-orm 投影）／sea-orm-adapter（例外①、vendored casbin adapter）／server（HTTP 服務 crate；模組＝`main`／`lib` boot 鏈、`config`、`state`、`obs`（tracing＋metrics）、`envelope`、`error`、`router`、`auth`、`cache`、`captcha`、`request_context`、`trust`、`ipgate`、`middleware`、`throttle`（cache／captcha／throttle 三支之職責細目見 §5.2、此處不鏡像）、`validation`、`handler`、`model`（`facade`＝entity 存取唯一管道、`password`＝argon2id 驗密純函式）；模組分層、契約測試面與 ROUTES 真表見 §5.2）；分支來源紀錄檔 `x_fork.branch-origin.md` | 外層 gitlink pin；容器 `rust-api` 由 front-nginx 反代、依賴 postgres／redis／migrate |
| `deploy/` | compose 三檔的設定檔（nginx、prometheus、grafana、loki、alloy、trust-model）與 sops 密文；建置檔 `Dockerfile.rust-api`／`Dockerfile.age`；機密與維運 CLI 群（generate-secrets／preflight-secrets／decrypt-secrets、sops.sh、backup-db、setup-reaper-role 等，逐支見 README 文件系統地圖 `deploy/` 段） | 容器 bind mount；機密解密＝age 私鑰（RUNBOOK §15） |
| `tools/` | `docsync`（十二閘＋生成器＋refresh 照相）、`orchestration/`（`_sk_*.js` 單一骨架＝TDD 與 review 兩種主流程共用首段、`assemble.py` 組裝器、harness-test／harness-review、EXAMPLE 成品與 review 單元定義範本、cdp.mjs）、`schema-gate.py`（三閘 schema 驗證）、`entity-drift-gate.py`（entity×快照漂移）、`rust-fmt-gate.py`（rust 格式）、`wire-schema.py`（wire 契約快照）、`fork-delta-lint.py`（base-web fork-delta 標記）、`msg-key-gate.py`（msg key 跨端：`MSG_KEYS` ⇔ 三檔 locale backend 子樹逐檔雙向全等＋Biz 構造點守衛＋前端 msg 字面消費點名冊 `FRONTEND_MSG_CONSUMERS`）、`wf-watchdog.py`、`comment-overlap.py`（承 rev5 檔之註解逐字重疊量測）、`walkthrough-baseline.py`（走查基準 snapshot／diff／restore〔走查後清理＋收尾 diff、基準須為空〕）、`bootstrap.sh`；碼面閘名冊＝RUNBOOK §12 碼面閘表 | pre-commit／PreToolUse hook；Workflow 工具 |
| `docs/` | 活書家族（arc42／c4／compliance／process）、ops 帳本與事件源、generated、史料面（brainstorms／reviews） | generate 名冊（GENERATED_FILES）；GT-01 零漂移 |

容器拓樸與 service 清單見 [C4-L2 容器視圖](../c4/C4-L2-container.md)（數量以該表為準）。

## 5.2 第二層

- **base-web**：upstream `example` 分支原樣分層——`src/views`（頁）、`router`（路由；as-built＝`.env` `VITE_AUTH_ROUTE_MODE=dynamic`〔003 刀 U5 翻自 upstream 基線 `static`、憲法 §II #2 拍板〕）、`store`、`service`／`service-alova`（API 客戶端）、`typings`（wire 契約裁判、憲法 §I.3）、`layouts`／`components`／`hooks`／`locales`／`theme`／`plugins`／`utils`／`constants`／`enum`／`styles`／`assets`；程式碼 fork-delta＝`src/typings/api/rev6-settings.d.ts`／`src/typings/api/rev6-auth.d.ts`／`src/service/api/rev6-settings.ts`／`src/service/api/rev6-auth.ts`／`src/locales/langs/zh-tw.ts` 五支新增型新檔（軌道與標記定形見 §8.4）、inline 修改型 19 處（`.env` 2／`.env.test` 1／`.env.prod` 1＝BASE-WEB-ADAPT、`src/store/modules/route/index.ts` 1＝BASE-WEB-AUTH-WIRING(a)、`src/views/_builtin/login/modules/{code-login,register,reset-pwd}.vue` 各 1＝BASE-WEB-AUTH-WIRING(b)、`src/hooks/business/captcha.ts` 3＝BASE-WEB-AUTH-WIRING(c)、`src/layouts/modules/global-header/components/user-avatar.vue` 1＝BASE-WEB-LOGOUT-UX-WIRING(i)、`src/store/modules/auth/index.ts` 3 與 `src/views/_builtin/login/modules/pwd-login.vue` 2＝BASE-WEB-LOGIN-CAPTCHA-WIRING(i)、`src/service/request/index.ts` 2＝BASE-WEB-I18N-WIRING(i)；另新增型圈界＝user-avatar 2／auth store 5／pwd-login 4／三表單各 1／captcha.ts 2／request 1／`src/typings/app.d.ts` 1＝BASE-WEB-I18N-WIRING(iii)／`src/locales/langs/{en-us,zh-cn}.ts` 各 1＝BASE-WEB-I18N-WIRING(ii)）；fork patch set 另含分支來源紀錄檔 `x_fork.branch-origin.md`，改動一律走憲法 §III 軌道並帶 `rev6-inline` 標記。另有 pnpm workspace `packages/`（工具鏈子套件）。
- **rust-api**：Cargo workspace 四 crate——`migration`（m0001 結構基線＋m0002 seed 基線、程式內容逐位元承襲 rev5 終態＝憲法 §I.5 例外②、ADR-00009）／`entity`（15 表 sea-orm 投影；與 schema 快照逐欄一致由 `tools/entity-drift-gate.py` 守恆——`casbin_rule` 委派 adapter 建基底、依 `rev4:ADR 0015` 雙向豁免，實比對 14 表）／`sea-orm-adapter`（例外①、vendored casbin adapter）／`server`（HTTP 服務 crate；模組分層＝`router`（`ROUTES` 六欄註冊表＝路徑單一來源、16 條由 `ROUTES_COUNT` 常數釘、依 protection 三態分派——Public 不掛驗章、Authed／Policy 兩子 router 整體套 `enforce_mw`、Policy 再逐 route 掛 `require_policy`；兩道 fallback 皆 4040；routed 鏈最外層另掛 `middleware::request_context_mw`——蓋全部路由含 `/health`／`/metrics` 與兩道 fallback、執行先於 `enforce_mw`，每請求恰一次把信任錨結論組成 `RequestContext` 注入 extensions，傳輸層對端缺席即不注入、下游各走缺席分支）→`auth`（`enforce.rs`＝真驗章 middleware `enforce_mw`〔三分碼→denylist 四級降級→`last_activity` 推進→注入 `Identity`〕＋`require_policy` per-route 政策層＋casbin 單一判定進入點 `enforce_role_path_method`；`jwt.rs`＝Claims 八欄、access／refresh 兩把秘鑰分路簽驗（兩鑰相異由 boot 期 `config::assert_jwt_secrets_distinct` fail-loud 守、ADR-00031）、`token_hash`、TTL 公式與 `ttl_from_settings`；判定單點、三分碼與四級降級見 §8.3）→`handler`（八檔＝`auth/{alt_stub,login,logout,refresh,user_info}.rs`＋`captcha.rs`＋`route.rs`＋`system_settings.rs`；★throttle 與 captcha 的接線點都在 `auth/login.rs`：驗密之前依序呼 `throttle::login_input_within_limits` 形制閘與 `throttle::precheck`〔三區＋captcha gate＋降級矩陣；三個拒絕分支早退＝零稽核列零計數桶〕，取題端點 `captcha.rs` 走 `crate::captcha` 無狀態簽題、零 DB 零 redis）→`validation`（16 鍵 registry＋型別一致性守衛）／`model/facade`（entity 存取唯一管道；八支＝`session_event`／`sys_login_attempt`／`sys_menu`／`sys_role`／`sys_token`／`sys_user`／`sys_user_role`／`system_settings`，同層 `test_kit.rs`＝`#[cfg(test)]` 測試設施、不計入）→`entity`；四支支撐模組各一句職責——`cache`＝redis 鍵 builder 六字面（全 crate 唯一鍵渲染點）＋boot 期 `connect` 建連（`main.rs` 單一呼叫點、句柄由 `AppState` 持有）＋GET／SET EX／SET NX／DEL／PUBLISH 原語（PUBLISH＝IP 規則門鈴；SUBSCRIBE 不在此模組、由 `ipgate` watcher 另開專用連線）＋denylist・last_activity・grace 三對專用讀寫，★讀取面以 `Option` 出手把 nil（答得出、無鍵）與 `Err`（答不出）在型別上分開＝島 C fail-closed 的機制底座；`throttle`＝登入失敗節流帳號維唯一（`precheck` 四步三區、`captcha_gate`、三鍵整組退常數的 `load_settings`、七源統一出口 `warn_degraded`；IP 維與 unlock marker 不在本 crate）；`captcha`＝無狀態簽題（`CaptchaClaims` 四欄、`answer_mac`、`issue`／`verify_challenge`；模組名與 extern crate 同名、撞名消歧三規則見該檔檔頭；伺服器零存、唯一的 redis 寫入＝驗題時的消耗標記、落在 throttle）；`obs`＝recorder 恰一份＋序列預註冊顯式 0（序列名冊住該檔 `pre_register_metrics`、發射點與 label 值集同一筆帳、兩側各有測試釘）；業務 route 回應一律 `Res` 信封、錯誤一律 `AppError`（§8.2），信封例外恰二＝`ROUTES` 之 `envelope_exception` 欄標記的 `/health`（純文字）與 `/metrics`（exposition）、紀律上位＝憲法 §I.3；真表由 generate 重算為 `docs/generated/reference/routes.md`；契約測試面＝`tests/contract.rs`／`tests/wire_schema.rs`（機制＝§8.2）、`tests/entity_access_lint.rs`（facade 唯一管道之機器錨）、`tests/entity_behavior_lint.rs`（ORM 關聯與行為層機器錨＝§8.1）、`tests/health.rs`；真 DB 端點案分住各模組之 `#[cfg(test)]` 測試模組（消費者不寫死、以 grep `real_app` 為準；行程級 `DB_SERIAL` 互斥與庫態還原守衛之機制落點皆在 `model/facade/test_kit.rs`、該檔為 `DB_SERIAL` 的單一持有者）；以 rev5 活書 §5 為藍本重打字＝憲法 §I.5）。　★共用測試件＝`model/facade/test_kit.rs`（`#[cfg(test)]` 圈界、寄居 facade 層；名冊分群與逐件內容一律以該檔頭註為準、此處不鏡像（群數與群名不在此複寫、每次擴充即過期）；消費者名冊不寫死、以 grep 為準＝LL-00009）。

## 5.3 第三層

隨各域刀進場；本節以指針列各刀 spec（`specs/<NNN>-<feature>/`），不預載模組拓樸。

## 5.4 E2 模型登錄視圖

目前無 AI 元件（截至 2026-09-03）；本層隨 AI 功能刀填入。流程層＝[P-E2 代理名冊](../process/P-E2-agent-registry.md)。
