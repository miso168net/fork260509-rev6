---
section: 5
summary: 建構區塊三層白盒；E2 模型登錄視圖的系統層落點
rad_ai: [E2]
rad_ai_stage: 1
rev5_blueprint:
  §5 Building blocks: 承襲（rust-api workspace 四 crate＝migration／entity／sea-orm-adapter／server；server 管線 as-built 見 §5.2、以 rev5 活書 §5 為藍本重打字＝憲法 §I.5）
---
# §5 建構區塊視圖

## 5.1 整體系統白盒

| 區塊 | 內含 | 介面 |
|---|---|---|
| `base-web/` | soybean-admin fork 全樹（upstream 原樣分層；程式碼 fork-delta——新增型新檔、inline 修改型、新增型圈界與路由外掛產物檔——之 as-built 逐檔清單、軌道與標記定形住 §8.4，此處不鏡像）；fork patch set 另含檔頭標記的分支來源紀錄檔 `x_fork.branch-origin.md` | 外層 gitlink pin；容器 `base-web`（Vite dev）由 front-nginx 反代 |
| `rust-api/` | Cargo workspace：migration（m0001 結構基線＋m0002 seed 基線、承 rev5 逐位元＝憲法 §I.5 例外②）／entity（15 表 sea-orm 投影）／sea-orm-adapter（例外①、vendored casbin adapter）／server（HTTP 服務 crate；模組＝`main`／`lib` boot 鏈、`config`、`state`、`obs`（tracing＋metrics）、`envelope`、`error`、`router`、`auth`、`cache`、`captcha`、`request_context`、`trust`、`ipgate`、`middleware`、`throttle`（cache／captcha／throttle 三支之職責細目見 §5.2、此處不鏡像）、`validation`、`handler`、`model`（`facade`＝entity 存取唯一管道、`audit`＝操作稽核事件型、`password`＝argon2id 驗密純函式）；模組分層、契約測試面與 ROUTES 真表見 §5.2）；分支來源紀錄檔 `x_fork.branch-origin.md` | 外層 gitlink pin；容器 `rust-api` 由 front-nginx 反代、依賴 postgres／redis／migrate |
| `deploy/` | compose 三檔的設定檔（nginx、prometheus、grafana、loki、alloy、trust-model）與 sops 密文；建置檔 `Dockerfile.rust-api`／`Dockerfile.age`；機密與維運 CLI 群（generate-secrets／preflight-secrets／decrypt-secrets、sops.sh、backup-db、setup-reaper-role 等，逐支見 README 文件系統地圖 `deploy/` 段） | 容器 bind mount；機密解密＝age 私鑰（RUNBOOK §15） |
| `tools/` | `docsync`（十二閘＋生成器＋refresh 照相）、`orchestration/`（`_sk_*.js` 單一骨架＝TDD 與 review 兩種主流程共用首段、`assemble.py` 組裝器、harness-test／harness-review、EXAMPLE 成品與 review 單元定義範本、cdp.mjs）、`schema-gate.py`（三閘 schema 驗證）、`entity-drift-gate.py`（entity×快照漂移）、`rust-fmt-gate.py`（rust 格式）、`wire-schema.py`（wire 契約快照）、`fork-delta-lint.py`（base-web fork-delta 標記）、`msg-key-gate.py`（msg key 跨端：`MSG_KEYS` ⇔ 三檔 locale backend 子樹逐檔雙向全等＋Biz 構造點守衛＋前端 msg 字面消費點名冊 `FRONTEND_MSG_CONSUMERS`）、`view-render-guard.py`（管理頁 `base-web/src/views/manage/**` 零原始 HTML 注入寫法）、`route-artifact-gate.py`（路由外掛產物檔紀律：產出檔集對賬＋重算冪等＋零手改）、`wf-watchdog.py`、`comment-overlap.py`（承 rev5 檔之註解逐字重疊量測）、`walkthrough-baseline.py`（走查基準 snapshot／diff／restore〔走查後清理＋收尾 diff；基準檔模式須為空基準、`--seed` 模式以凍結 seed 為目標態〕）、`bootstrap.sh`；碼面閘名冊＝RUNBOOK §12 碼面閘表 | pre-commit／PreToolUse hook；Workflow 工具 |
| `docs/` | 活書家族（arc42／c4／compliance／process）、ops 帳本與事件源、generated、史料面（brainstorms／reviews） | generate 名冊（GENERATED_FILES）；GT-01 零漂移 |

容器拓樸與 service 清單見 [C4-L2 容器視圖](../c4/C4-L2-container.md)（數量以該表為準）。

## 5.2 第二層

- **base-web**：upstream `example` 分支原樣分層——`src/views`（頁）、`router`（路由；as-built＝`.env` `VITE_AUTH_ROUTE_MODE=dynamic`〔003 刀 U5 翻自 upstream 基線 `static`、憲法 §II #2 拍板〕）、`store`、`service`／`service-alova`（API 客戶端）、`typings`（wire 契約裁判、憲法 §I.3）、`layouts`／`components`／`hooks`／`locales`／`theme`／`plugins`／`utils`／`constants`／`enum`／`styles`／`assets`；我方新增頁＝`src/views/manage/ip-rule/`（IP 規則管理頁：主檔＝搜尋卡＋現役與回收桶混排清單＋逐列軟刪／復原，`modules/` 兩檔＝搜尋卡與新增／編輯抽屜；經 `src/service/api/rev6-ip-rule.ts` 打後端 IP 規則五端點、wire 型別＝`src/typings/api/rev6-ip-rule.d.ts`；四顆操作鈕各接一個按鈕權限碼、只管顯示，安全邊界在後端政策判定；路由條目由 elegant-router 外掛自 views 目錄重算產物檔）；程式碼 fork-delta 之 as-built 逐檔清單住 §8.4（此處不鏡像）；fork patch set 另含分支來源紀錄檔 `x_fork.branch-origin.md`，改動一律走憲法 §III 軌道並帶 `rev6-inline` 標記。另有 pnpm workspace `packages/`（工具鏈子套件）。
- **rust-api**：Cargo workspace 四 crate——`migration`（m0001 結構基線＋m0002 seed 基線、程式內容逐位元承襲 rev5 終態＝憲法 §I.5 例外②、ADR-00009）／`entity`（15 表 sea-orm 投影；與 schema 快照逐欄一致由 `tools/entity-drift-gate.py` 守恆——`casbin_rule` 委派 adapter 建基底、依 `rev4:ADR 0015` 雙向豁免，實比對 14 表）／`sea-orm-adapter`（例外①、vendored casbin adapter）／`server`（HTTP 服務 crate；模組分層＝`router`（`ROUTES` 六欄註冊表＝路徑單一來源、條數由 `ROUTES_COUNT` 常數釘〔真表＝`docs/generated/reference/routes.md`〕、依 protection 三態分派——Public 不掛驗章、Authed／Policy 兩子 router 整體套 `enforce_mw`、Policy 再逐 route 掛 `require_policy`；兩道 fallback 皆 4040；routed 鏈最外層另掛 `middleware::request_context_mw`——蓋全部路由含 `/health`／`/metrics` 與兩道 fallback、執行先於 `enforce_mw`，每請求恰一次把信任錨結論組成 `RequestContext` 注入 extensions，傳輸層對端缺席即不注入、下游各走缺席分支；其內側掛 `middleware::ip_gate_mw`（IP 存取閘：執行序＝注入層→閘→`enforce_mw`，兩道 fallback 亦在閘內；每請求只 `load()` 記憶體內規則集、零外部查詢）→`auth`（`enforce.rs`＝真驗章 middleware `enforce_mw`〔三分碼→denylist 四級降級→`last_activity` 推進→注入 `Identity`〕＋`require_policy` per-route 政策層＋casbin 單一判定進入點 `enforce_role_path_method`；`jwt.rs`＝Claims 八欄、access／refresh 兩把秘鑰分路簽驗（兩鑰相異由 boot 期 `config::assert_jwt_secrets_distinct` fail-loud 守、ADR-00031）、`token_hash`、TTL 公式與 `ttl_from_settings`；判定單點、三分碼與四級降級見 §8.3）→`handler`（域名冊住 `handler/mod.rs` 檔頭、`auth/` 成員名冊住 `handler/auth/mod.rs`——檔數與成員不在此複寫；004 刀進場兩支＝`ip_rule.rs`〔IP 規則五端點：類型守門→網段正規化→寫端防自鎖〔變更後規則集對操作者真實來源跑同一支 `ipgate::would_self_lock`〕→業務列與操作稽核列同交易→commit 後 `ipgate::reload_and_publish`〕與 `throttle.rs`〔管理員解鎖端點 `POST /systemManage/unlockLogin`：標的導出（帳號維＝帳號名原文、來源維＝`throttle::ip_bucket` 之桶）→操作者組裝→操作稽核列單寫（`sys_operation_log::write`）→稽核落定之後才寫該維解鎖標記；不查鎖態、不刪任何鍵、未鎖標的照寫照回 `0000`〕；★throttle 與 captcha 的接線點都在 `auth/login.rs`：驗密之前依序＝`throttle::login_input_within_limits` 形制閘→取請求上下文→轉發鏈逾限拒絕腿→`throttle::precheck`〔兩維三區合成＋captcha gate＋降級矩陣；來源維輸入由同檔 `throttle_source` 組——上下文缺席或位址無桶＝該維整層跳過、顯式放行直讀 `RuleSet::allow`；拒絕分支早退＝零稽核列零計數桶〕）→`validation`（16 鍵 registry＋型別一致性守衛＋crate 內界值存取器 `range_of`＝節流兩維設定載入判越界之單一界值來源）／`model/facade`（entity 存取唯一管道；一表一支、名冊住 `model/facade/mod.rs` 檔頭——成員不在此複寫；其中 `sys_operation_log`＝操作稽核 append-only sink；同層 `model/audit.rs`＝操作稽核事件型〔`AuditOperation` 字面單一宣告源〕；同層 `test_kit.rs`＝`#[cfg(test)]` 測試設施、不計入）→`entity`；支撐模組各一句職責——`trust`＝信任錨純函式（零 I/O 零狀態、全態矩陣可離線窮舉）：`resolve_client_ip` 三層（對端閘→Tier-1 CDN 位置錨〔錨右整段受信才成立〕→最右非受信）＋整鏈受信回退、兩覆蓋 `apply_tunnel_fallback`／`apply_cf_overlay`、溢出短路 `apply_chain_overflow`、轉發鏈唯一切分源 `xff_tokens_rtl`（判定軌 `normalize_xff` 與稽核轉錄軌 `decision_window` 共用）、IPv4-mapped 折疊單點 `to_canonical`、來源信心八態字面單一出口 `Confidence::as_str`、受信集＝跳過集單一導出 `TrustModel::is_trusted`（六集合聯集）；信任模型由 `config::load_trust_model` 於 boot 載入一次、`Arc` 唯讀共享（載入失敗三層語意皆不擋啟動）；`ipgate`＝兩半同檔——判定核（純函式：`RuleSet` 放行／阻擋兩袋、`decide` 判定序③～⑥、`build_ruleset`、`would_self_lock`、結構豁免六段）＋載入面與門鈴（`load_ruleset` boot 初載、`reload_and_publish` 寫端重載＋PUBLISH、`spawn_ipgate_watcher` 專用連線 SUBSCRIBE＋每次訂閱成功後補讀；執行中判定面的 `store` 全 crate 恰一處＝`reread_keeping_last_good`、由該檔靜態掃描案釘住）；「這個位址放不放行」全 crate 只有 `decide` 一份、存取閘與寫端防自鎖同源；`request_context`＝handler 取請求事實的唯一型（四欄私有＋取值器；建構點恰兩個＝`from_trust` 請求路徑〔當場折疊位址、取判定窗〕與 `from_headers` 缺席退路；handler 不自讀轉發標頭）；`cache`＝redis 鍵 builder（全 crate 唯一鍵渲染點；支數與鍵族字面以該檔檔頭為準——節流面只有兩維解鎖標記與 captcha 消耗標記、無鎖定鍵＝ADR-00038）＋boot 期 `connect` 建連（`main.rs` 單一呼叫點、句柄由 `AppState` 持有）＋GET／SET EX／SET NX／DEL／PUBLISH 原語（PUBLISH＝IP 規則門鈴；SUBSCRIBE 不在此模組、由 `ipgate` watcher 另開專用連線）＋denylist・last_activity・grace 三對專用讀寫，★讀取面以 `Option` 出手把 nil（答得出、無鍵）與 `Err`（答不出）在型別上分開＝島 C fail-closed 的機制底座；`throttle`＝登入失敗節流兩維並列（帳號維＋來源維；`precheck` 判定序＝解鎖標記讀取〔兼快取可用性判定〕→帳號維→來源維→合成，兩維皆於每次嘗試讀設定現值並由 PG 滑動窗定案＝ADR-00038；`captcha_gate`；兩維同形、三鍵整組退常數的 `load_settings`／`load_ip_settings`〔越界判準取 `validation::range_of`〕；來源維計數桶單點導出 `ip_bucket`；降級統一出口 `warn_degraded`〔值集單一權威＝`obs.rs` `THROTTLE_DEGRADED_SOURCES`〕；解鎖標記的寫入者＝`handler::throttle`、鍵字面只經 `cache` 兩支 builder）；`captcha`＝無狀態簽題（`CaptchaClaims` 四欄、`answer_mac`、`issue`／`verify_challenge`；模組名與 extern crate 同名、撞名消歧三規則見該檔檔頭；伺服器零存、唯一的 redis 寫入＝驗題時的消耗標記、落在 throttle）；`obs`＝recorder 恰一份＋序列預註冊顯式 0（序列名冊住該檔 `pre_register_metrics`、發射點與 label 值集同一筆帳、兩側各有測試釘）；業務 route 回應一律 `Res` 信封、錯誤一律 `AppError`（§8.2），信封例外恰二＝`ROUTES` 之 `envelope_exception` 欄標記的 `/health`（純文字）與 `/metrics`（exposition）、紀律上位＝憲法 §I.3；真表由 generate 重算為 `docs/generated/reference/routes.md`；契約測試面＝`tests/contract.rs`／`tests/wire_schema.rs`（機制＝§8.2）、`tests/entity_access_lint.rs`（facade 唯一管道之機器錨）、`tests/entity_behavior_lint.rs`（ORM 關聯與行為層機器錨＝§8.1）、`tests/serve_connect_info_lint.rs`（起服務須帶傳輸層對端＝信任錨對端輸入之機器錨）、`tests/health.rs`；真 DB 端點案分住各模組之 `#[cfg(test)]` 測試模組（消費者不寫死、以 grep `real_app` 為準；行程級 `DB_SERIAL` 互斥與庫態還原守衛之機制落點皆在 `model/facade/test_kit.rs`、該檔為 `DB_SERIAL` 的單一持有者）；以 rev5 活書 §5 為藍本重打字＝憲法 §I.5）。　★共用測試件＝`model/facade/test_kit.rs`（`#[cfg(test)]` 圈界、寄居 facade 層；名冊分群與逐件內容一律以該檔頭註為準、此處不鏡像（群數與群名不在此複寫、每次擴充即過期）；消費者名冊不寫死、以 grep 為準＝LL-00009）。

## 5.3 第三層

隨各域刀進場；本節以指針列各刀 spec（`specs/<NNN>-<feature>/`），不預載模組拓樸。

## 5.4 E2 模型登錄視圖

目前無 AI 元件（截至 2026-09-03）；本層隨 AI 功能刀填入。流程層＝[P-E2 代理名冊](../process/P-E2-agent-registry.md)。
