# rev4:016-observability rev4:T003：grafana loki datasource provisioning（冪等；rev3:018 U1 照搬）。
# rev3:research R7 hindsight：① deleteDatasources guard（免重建時 uid 衝突 crash-loop）；
#   ② 顯式 uid: loki（dashboard panel datasource ref 依賴穩定 uid、漂移即 panel 斷）。
apiVersion: 1

deleteDatasources:
  - name: Loki
    orgId: 1

datasources:
  - name: Loki
    type: loki
    uid: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
