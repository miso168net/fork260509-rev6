# .env.example — repo 根 .env 範本（rev4:019-secrets-sops rev4:T024；rev4:FR-017／contracts rev4:P5.1）
# .env 本體 gitignored（.gitignore 既有 `.env` 規則已覆蓋、無另加行）、由 `bash tools/bootstrap.sh`
# 代勞產生；本檔僅示範形制，不寫死本機路徑（per-machine 值不入版控）。
#
# ★產檔約束（compose 原生讀 .env、不做任何形制校驗，故校驗全由讀值方承擔——
#   deploy 四腳本〔generate／preflight／decrypt／setup-reaper-role〕與
#   tools/docsync/gates.py 的 GT-07〔動態載入 deploy/secrets_common.py〕皆嚴格單行解析、
#   違反即 FAIL 拒用；tools/bootstrap.sh 則 warn＋附自癒指引、以 deploy/secrets 回退
#   進行體檢）：
#   1. 單行 KEY=VALUE（無引號、無行內註解）＝bootstrap 產檔形、也是建議書寫形。
#      ★行形（非值本身）從寬（rev4:019 U4）：讀值方的偵測樣式與 compose 對齊，UTF-8 BOM／行首
#      空白／`export ` 前綴／等號兩側空白／CRLF 行尾一律照樣認得並取同一個值——偵測窄於
#      compose 就會漏認該行、靜默回退舊落點（compose 讀新落點、腳本查舊落點）。
#   2. ★docker compose 讀 .env 時「不做 shell 展開」——值裡寫 $HOME 只是三個字面字元、
#      不會變成家目錄；SECRETS_DIR 必須是「絕對路徑字面」（bootstrap 於 shell 側展開後寫入）。
#   3. 值不得含空白與 shell 元字元（腳本只嚴格解析 SECRETS_DIR 一行、不整檔 source——
#      compose 的 .env 允許不加引號的含空白值、井號語意亦與 shell 不同，整檔 source 時
#      含錢字號小括號或反引號之值會被執行）。
#
# SECRETS_DIR：解密後明文機密檔落點（單一事實來源；落點值拍板＝rev4:ADR 0084 統一樹、
#   ext4 語意承 rev4:ADR 0080 決策 2：$HOME/.cache/fork260509-rev6/secrets 之絕對路徑字面、
#   ext4 持久、免開機儀式）。
#   ★消費者聯集＝七處，唯一權威清單＝specs/rev4:019-secrets-sops/contracts/secret-pipeline.md
#   rev4:§P5.1（本檔刻意不重列——重列即再生一份會漏的清單）；落點類變更動手前一律先枚舉該列
#   （rev4:L-174 防法①：清單本身就可能是漏的）。
#   未設時一律回退 repo 內 ./deploy/secrets（向後相容；忘設＝保護失效，由 preflight 與
#   bootstrap 斷言補償、ADR 誠實登記）。
# SECRETS_DIR=/home/USERNAME/.cache/fork260509-rev6/secrets
#
# SOCKET_PROXY_GID：obs profile 之 socket-proxy 所需 docker.sock gid（選填；用途與查法
#   見 docs/ops/RUNBOOK.md §4；不設＝退 compose 預設 1001）。
# SOCKET_PROXY_GID=1001
